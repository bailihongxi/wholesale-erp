# 家电批发进销存 ERP · 开发计划（初版 · 已归档）

> ⚠️ **本文档已被 `DEV_PLAN_V2.md` 取代，仅作历史留档，不要再按此推进。**
> 现行计划见 `DEV_PLAN_V2.md`（产品版本 **V1.0-1**，2026-09-19 发布基线）。

---

> 版本：V1.0
> 日期：2026-09-19
> 配套文档：PRD.md（产品需求文档）
> 用途：本计划为执行开发的直接依据，按阶段推进、按验收点检查。

---

## 一、技术栈选型（已锁定）

| 类别 | 选型 | 理由 |
|---|---|---|
| 前端框架 | Vue 3 + Vite + TypeScript | 轻量、构建快、SPA 可直接部署到 GitHub Pages |
| UI 组件库 | Vant 4 | 移动端优先，电脑浏览器自适应 |
| 路由 | Vue Router 4 | 按角色做路由守卫 |
| 状态管理 | Pinia | 登录态、当前用户、同步状态 |
| 本地数据库 | Dexie.js（IndexedDB 封装） | 浏览器本地存储，刷新/关闭/重启不丢 |
| 云同步 | Supabase 免费版（PostgreSQL + 认证） | 手动上传/拉取，免费额度够小公司用 |
| 部署 | GitHub 仓库 + GitHub Actions 自动部署到 GitHub Pages | push 即发布 |
| 打印 | 浏览器原生 window.print() + CSS @media print | 不引第三方库 |

---

## 二、项目目录结构

```
src/
├── api/            # 数据访问层（本地 Dexie + 云 Supabase，业务代码不直接碰数据库）
├── db/             # Dexie 表定义（对应 PRD 附录 12 张表）
├── stores/         # Pinia（user、sync、permission）
├── router/         # 路由表 + 角色路由守卫
├── utils/          # 单号生成、金额大写、格式化
├── components/     # 通用组件（打印预览、数字键盘、搜索栏）
├── views/
│   ├── auth/       # 登录
│   ├── boss/       # 工作台、报表、商品档案、员工管理
│   ├── purchase/   # 采购单、供应商
│   ├── sales/      # 销售单、客户
│   ├── warehouse/  # 入库、出库、库存、盘点
│   ├── finance/    # 对账、收付款
│   └── dealer/     # 经销商产品目录
└── App.vue
```

---

## 三、分阶段开发任务（共 11 个阶段）

### 阶段 0 · 项目初始化（1～2 天）
- 搭 Vue3 + Vite + TypeScript + Vant 4 脚手架
- 建 GitHub 仓库
- 配置 GitHub Actions 自动部署到 GitHub Pages
- 搭路由骨架和登录页壳

**产出**：网页能在 GitHub Pages 打开
**验收**：手机/电脑能打开空白登录页

---

### 阶段 1 · 数据层 + 登录（2～3 天）
- 用 Dexie 建 PRD 附录中的 12 张表
- 登录页（手机号 + 密码）
- 老板在后台创建员工账号（采购/销售/财务/库房）
- 按角色识别，进入对应工作台

**产出**：登录系统跑通
**验收**：不同角色登录看到不同首页

---

### 阶段 2 · 商品档案 + 库存基础（2～3 天）
- 商品 CRUD（新增/编辑/搜索/列表）
- 商品名称 = 品牌 + 型号 自动组合，不单独存字段
- 多级价格（进价/批发价/零售价）
- 库存表建立
- 库存预警标记

**产出**：商品可录入、可搜索、有库存
**验收**：新增商品后列表显示"品牌 型号"；低于预警值有标记

---

### 阶段 3 · 采购闭环（2～3 天）
- 供应商 CRUD
- 采购单新建（选供应商 → 选商品 → 填数量 → 自动算金额）
- 采购单列表、详情
- 库房入库（验货，逐项确认实收数量）
- 入库后自动：库存增加 + 生成应付

**产出**：采购 → 入库闭环跑通
**验收**：入库后库存数量增加、自动生成应付记录

---

### 阶段 4 · 销售闭环（2～3 天）
- 客户 CRUD
- 销售单新建（选客户 → 选商品 → 自动带客户等级价 → 填数量）
- 销售单列表、详情
- 库房出库（拣货，逐项确认）
- 出库后自动：库存扣减 + 生成应收

**产出**：销售 → 出库闭环跑通
**验收**：出库后库存数量减少、自动生成应收记录

---

### 阶段 5 · 财务（2 天）
- 应收列表（客户欠款）
- 应付列表（欠供应商）
- 收付款登记（选择单据 → 登记金额 → 自动核销）
- 毛利统计（销售收入 − 进货成本）

**产出**：财务能对账
**验收**：登记收款后，对应销售单收款状态更新

---

### 阶段 6 · 工作台 + 报表（2 天）
- 各角色工作台待办（老板看板、采购待跟单、销售待发货、财务待收/付、库房待收/发）
- 基础报表：销售统计、库存报表、应收应付汇总

**产出**：首页有真实数据
**验收**：老板看板显示真实汇总数据

---

### 阶段 7 · 打印模板（1～2 天）
- 采购单打印页
- 销售单打印页
- 「带价格 / 不带价格」两种版式切换
- A4 打印样式（含公司抬头、单号、明细、签字栏）

**产出**：能打出 A4 单据
**验收**：切换"不带价格"后，单价/金额列整列消失

---

### 阶段 8 · 经销商端（1～2 天）
- 客户表加登录字段（login_phone、login_password、状态）
- 老板在客户管理中创建经销商账号
- 经销商登录后进入产品目录（只看产品 + 批发价）
- 权限隔离：看不到进价、库存、其他客户

**产出**：外部客户能登录看产品
**验收**：经销商看不到进价和库存数量

---

### 阶段 9 · 云同步 + 备份（2 天）
- 接入 Supabase
- 「上传到云端」按钮（手动把本机数据推到云数据库）
- 「从云端恢复」按钮（换设备拉取数据）
- 导出备份 JSON 文件
- 导入恢复 JSON 文件

**产出**：多设备同步可用
**验收**：A 设备上传后，B 设备拉取数据一致；导出再导入数据完整

---

### 阶段 10 · 验收 + 上线（2 天）
- 按 PRD.md 第 10 节 17 条验收标准逐项测试
- 手机浏览器适配（按钮不小于 44px）
- 正式发布

**产出**：系统上线
**验收**：PRD 第 10 节全部通过

---

## 四、时间总览

| 阶段 | 工期 |
|---|---|
| 阶段 0 初始化 | 1～2 天 |
| 阶段 1 数据层+登录 | 2～3 天 |
| 阶段 2 商品+库存 | 2～3 天 |
| 阶段 3 采购闭环 | 2～3 天 |
| 阶段 4 销售闭环 | 2～3 天 |
| 阶段 5 财务 | 2 天 |
| 阶段 6 工作台+报表 | 2 天 |
| 阶段 7 打印模板 | 1～2 天 |
| 阶段 8 经销商端 | 1～2 天 |
| 阶段 9 云同步+备份 | 2 天 |
| 阶段 10 验收上线 | 2 天 |
| **合计** | **约 18～25 个工作日（4～5 周）** |

---

## 五、开发顺序原则

1. **先打地基（阶段 0～2）**：数据层和商品档案必须先做对，否则后面所有单据都是空中楼阁。
2. **核心闭环先行（阶段 3～4）**：采购入库和销售出库跑通后，系统就能真正用了。
3. **锦上添花放最后（阶段 7～9）**：打印、经销商、云同步是增强项，主流程稳定后再加。
4. **验收贯穿始终**：每做完一个阶段，对照 PRD.md 第 10 节相关验收项自测，不攒到最后。

---

## 六、依赖关系说明

- 阶段 1～2 是地基，必须先做。
- 阶段 3（采购）和阶段 4（销售）都依赖阶段 2 的商品档案。
- 阶段 5（财务）依赖阶段 3、4 产生的单据数据。
- 阶段 6（工作台）依赖前面各模块都有数据。
- 阶段 7～9 相对独立，可在主流程稳定后插入。
- 阶段 10 收尾。

---

**文档结束。** 每完成一个阶段，在本文件对应阶段后打勾并记录实际完成日期。
