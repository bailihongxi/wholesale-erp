# 家电批发 ERP · 菜单功能清单

> **这份文件是干什么的**：把左侧菜单栏里**每一个菜单**的真实功能、页面上有哪些按钮/输入、做了以后起什么作用全部列出来，
> 并标注「现在到底能不能用」。
>
> 更新时间：2026-09-24（**当前产品版本 V2.1-1.4**；V2.1-1.4 手机端卡片化扩展到仓库各页：入库验货（收货单）/ 出库拣货（发货单）/ 库存调拨 / 库存盘点 / 退换货 / 库房一览 / 出入库明细的表格在手机端（<768px）全部改卡片式（沿用已验证的 `items-edit` 范式，出入库明细只读态走 `ItemCards` 组件）；新增 `docs/页面模块顺序规范.md` 写死「已选明细必须在选择商品上方」并修正调拨、盘点两页的反序；打印每页容量按纸张高度重算（A5 6→9、A4 14→22）；删除销售单 / 采购单时自动把来源报价单 / 预采询价单退回「未转」状态（双向联动，见 `src/utils/quoteLink.ts`）；上一版 V2.1-1.3 商品明细统一「**电脑端列表 / 手机端卡片**」并写死为全站规定：抽出全站唯一复用组件 `components/ui/ItemCards.vue`（左侧蓝竖条标题「商品明细（N）」+ 商品名独占一行 + 「1 套 × ¥37,950.00 =」小字 + 右侧深色加粗金额 + 虚线合计条「合计 N 件」与红色合计金额），销售单详情 / 报价单详情 / 采购单详情 / 预采询价详情的**手机端明细全部改用它**、电脑端一律保留列表（表格 / 行列表）；同时删掉被取代的旧实现（`.item-cards` / `.ic-*` / 详情态 nth-child 拆表 `.items-view`）；预采询价新建态的询价商品明细补上手机端卡片范式（`items-edit`，此前手机端会横向撑破）；新增 `docs/手机端明细卡片规范.md` 作为唯一口径；上一版 V2.1-1.2 报价单明细手机端改卡片式（与采购单「采购商品明细」同一套范式）+ 全项目版本号统一为 V2.1-1 体系 + 修复手机端明细空态被隐藏；V2.1-1.1 经销商自助单归属 salesId 写 0 + 修复经销商无法提交报价单 + 报价单详情页返回键；V2.1-1 B1 经销商门户（上）：经销商（dealer）登录后只能进「报价单」页 —— `/sales/quotes` 的 `meta.role` 加入 `dealer`、`homeRouteForRole('dealer')` 由不存在的 `/dealer/catalog` 改为 `/sales/quotes`（修登录死循环）、`navConfig` 新增 dealer 手写菜单（侧边栏仅报价单、手机端 Tab 为报价单 / 我的）；报价单页加 dealer 分支：列表按 `customerId` 隔离（只看自己的单）、新建时客户字段只读自动归属自身、价格锁定批发价、隐藏「销售单」Tab 与修改/转单按钮；`permission` 的 `canAccess` 对不在模块矩阵里的角色回退到 `navConfig` 声明的入口（否则经销商手机端只剩一个 Tab）；V2.0-29 全站按钮配色统一：打印=绿 `#16a34a` / 删除·取消=红 `#ef4444` / 返回=橘 `#f97316` / 修改=蓝 `#2f6bff`，唯一来源 `src/styles/theme.css` 的「全站按钮配色统一」段（只改 background/border-color/color，字体与圆角尺寸一律不动；出入库单「撤回」与「删除」底层同为 `revertDoc`，已合并为受 `canDeleteDoc` 闸控的「删除本单」）；V2.0-28 给单据删除加权限 —— 新增 `canDeleteDoc`，仅老板与系统管理员（hawsystem/工号 E000、admin/工号 E001）可删单据，其余角色一律无删除权；覆盖销售单 / 采购单 / 报价单 / 预采询价 / 出入库单五类可编辑单据，销售单与采购单只允许删「待出库 / 待入库」的（已动过库存的必须先撤回），出入库单的删除会先把库存原路退回再删流水；V2.0-27 修「新建 / 修改单据后回到列表页还是旧数据」：`App.vue` 的 `<keep-alive>` 让路由组件复活而非重新挂载，`onMounted` 的加载不会再跑 —— 新增 `src/composables/useReloadOnActivate.ts`（`useServerPager` 内置 + 23 个页面各自接入），顺带删掉采购单/销售单列表页首屏重复的那次 `pager.reload()`；V2.0-26 给采购单 / 预采询价 / 报价单三张单据页统一加上「修改」功能（复用销售单那套 `useEditMode` + `EditModePanel`），采购单顺带清掉旧编辑页（电脑端也被强制全屏）与 `</style>` 之外的死样式；V2.0-25 改「修改销售单」模块外观：去掉套在整块外面的 4px 橘色大边框（原来看着像弹窗、还比别的卡片窄一圈），模块内删掉「返回」按钮只留「取消 / 保存修改」，关闭统一走 `closeEdit()` —— 取消或保存成功后模块自动关闭，详情页原本那颗橘色「返回」键自动回来（编辑时它被 `v-if` 收起）。V2.0-23 把「销售单详情 / 修改销售单」的明细**电脑端也用同一套卡片** —— 明细表格整体退场，电脑端（≥768px）改为多列网格铺开（2 列，每卡含序号、「数量 × 单价 = 金额」、合计汇总卡），并修掉「点『修改』后视口停在顶部像没反应」的老毛病（自动滚到编辑区）；手机端同步保持单列卡片不变。V2.0-22 先做了手机端这两处的商品明细：详情页一行读清「数量 × 单价 = 金额」并补上手机端合计条，修改页整屏覆盖 + 每行带「数量 / 单价」标签的输入框（44px）+ 底部按钮常驻，明细不再用 `nth-child` 拆表（当时电脑端未动）。V2.0-17 的 Service Worker 缓存随版本更新：连续四轮发版只改源码、没碰过 `sw.js`，浏览器便认为 Service Worker 从未更新 —— 永远不触发 `install`/`activate`，旧缓存一直留着；加上导航请求在网络超过 1.5 秒时会回退到缓存里的旧 `index.html`（旧 HTML 又引用旧的 hash 资源），用户在手机上看到的始终是修复前的界面。现改为：缓存名由构建期注入的版本号派生（每次发版文件内容必变 → 自动清掉旧缓存）、`sw.js` 自身永不缓存、注册后主动 `update()` 绕过 HTTP 缓存、新 SW 接管后仅在用户尚未交互时自动刷新一次（避免冲掉正在录的单据）。另附手机端显示全站巡检：所有列表页走卡片布局（无表格），有表格的开单页/库房作业页逐个注入真实数据实测，合计行均无内边距/边框/背景外泄；产品版本 V2.0-25（修改销售单模块去橘色边框 + 去模块内返回按钮）；上一版 V2.0-24 为电脑端「修改销售单」明细卡片内改单行排列；V2.0-21 为手机端「选择商品」列表改卡片式；V2.0-20 为销售单页面级编辑模式；V2.0-15 启动白屏 3 秒修复；V2.0-14 手机端表格横向滚动修复）　｜　对应代码：`src/styles/theme.css`（手机端表格 + 合计行通栏，**唯一来源**）、`src/components/ProductPicker.vue`、`src/components/PageActions.vue`、`src/router/navConfig.ts`（菜单定义）、`src/views/**`（页面实现；页面里不要再写手机端 tfoot 通栏样式）
> 本轮要点：手机端商品卡片文字简化（进/批/零/库/警）；商品档案搜索加 250ms 防抖、stockMap 修正多库位汇总；员工管理表手机端改横向滚动并隐藏占宽的「可见模块」列，不再溢出。
>
> **状态图例**
> - ✅ **真实可用**：有真实数据读写，能完成业务动作
> - ⚠️ **部分可用 / 有明显问题**：能用，但缺关键环节
> - 🔁 **与其他菜单重复**：多余入口
> - ❌ **占位无功能**：只有静态文字和跳转链接

---

## 〇之前、第三轮修复：菜单点开空白 / 功能无效（2026-09-19）

老板反馈「入库验货、出库、采购、销售、客户、供应商、财务等模块内容都是错误和无用的，有的也是空白」。
排查后确认是**三个原因叠加**，均非页面本身缺失：

| # | 根因 | 表现 | 修复 |
|---|---|---|---|
| A | **菜单与路由权限不匹配** | 老板点「采购管理 / 销售管理 / 供应商 / 财务管理」被守卫弹回工作台——这 4 条路由当时只授权给 `purchaser`/`sales`/`finance` | 老板为**最高权限角色**，所有业务路由 `meta.role` 补入 `'boss'`；其他角色越权拦截不变 |
| B | **新装数据库为空** | IndexedDB 全新时各表 0 条，所有列表显示「暂无数据」，此前无示例数据或引导，被误判为「功能没开发」 | 新增 **一键示例数据**（`src/utils/demoData.ts`）：6 商品 / 3 供应商 / 3 客户（含 1 个经销商登录号）/ 3 张采购单 / 3 张销售单，并连带产生库存、流水、应收应付、操作日志。入口在**系统设置 → 示例数据**，老板工作台空数据时也会显示「开始使用」引导 |
| C | **手机端「我的」路由未注册** | 采购/销售/财务的底部 Tab「我的」指向 `/purchase/mine` 等未注册路径，命中通配规则被重定向到 `/login`，表现为**点击即退出登录** | 新增共用页 `src/views/RoleMineView.vue`，注册三条 `/xxx/mine` 路由；入口直接取自该角色菜单，避免两处维护不同步 |

**真实浏览器逐页验证结果**（老板账号实机操作）：采购管理 3 张单、销售管理 3 张单、
入库验货 2 张待收货、出库拣货 2 张待发货、客户 3 条、供应商 3 家、财务应收列表、
库存管理 6 SKU / 23 件 / ¥24,200 / 预警 5 项，全部正常。
实机跑通完整入库：进详情 → 填实收 6/10 → 确认入库 → 库存 6→12、10→20，流水 +2 条，提交后自动返回列表。

**权限约定（重要）**：老板为最高权限角色，可访问全部业务页面；其余角色仍受各自路由 `meta.role` 限制，越权访问会被弹回本角色首页。

---

## 〇、本轮改造摘要（2026-09-19 第二轮）

老板已拍板「按推荐来」，本轮完成了以下四件事：

| # | 改动 | 结果 |
|---|---|---|
| 1 | **全项目搜索框统一自带清除按钮** | 新建 `src/components/SearchInput.vue`，搜索框有内容时右侧出现「×」，点击立即清空并重新搜索，按 Esc 也能清空。已接入：商品档案、库存管理、客户管理、操作日志 |
| 2 | **库存查询增加搜索，并与「库存管理」合并** | 统一为 `/stock`「库存管理」：搜索 + 分类筛选 + 仅看低库存 + 预警区 + 库存明细 + 出入库流水 + 汇总卡（SKU/库存量/库存金额/预警数），按角色自动隐藏价格。旧路径 `/boss/stock`、`/warehouse/stock` 保留重定向 |
| 3 | **「经销商」并入「客户管理」；「毛利核算」并入「经营报表」** | 统一为 `/customers`「客户管理」（含经销商账号开关、按经销商筛选）；报表页新增日期筛选 + 快捷区间 + 6 项指标 + 近 6 月趋势图。旧路径重定向 |
| 4 | **P0 补闭环 + P1 工作台改造** | 新增采购单/销售单**详情页**（含打印）、**操作日志**（真实写入 + 查询页）、四个角色**工作台**改为真实待办看板 |

**菜单去重结果**：老板端 15 项 → **14 项**（去掉重复 3 项，新增「操作日志」1 项）；财务 5 项 → 4 项。

---

## 一、总览：每个角色看到哪些菜单

| 角色 | 电脑端左侧菜单 | 手机端底部 Tab |
|---|---|---|
| 老板 | **11 项**（第十二轮：库存作业并入库存管理） | 工作台 / 业务 / 我的 |
| 采购 | 3 项 | 工作台 / 采购单 / 我的 |
| 销售 | 3 项 | 工作台 / 销售单 / 我的 |
| 财务 | **4 项** | 工作台 / 对账 / 我的 |
| 库房 | **2 项**（第十二轮：工作台 / 库存管理） | 工作台 / 库存管理 |
| 经销商 | **仍未开发**（登录后无落地页，会被弹回登录页） | — |

---

## 二、老板端菜单逐个说明（14 项）

### 1. 工作台　`/boss/home`　✅
- **做什么**：老板每天打开的第一屏。
- **页面动作**：4 张数字卡（本月销售额 / 本月毛利 / 应收款 / 库存预警数）；待办可点击直达（待入库采购单 / 待发货销售单 / 待收款单据）；6 个快捷入口。
- **作用**：不翻模块就能看到「钱」和「今天要处理的事」。
- **数据**：实时从数据库汇总。

### 2. 商品档案　`/boss/products`　✅
- **做什么**：全系统唯一的商品主数据，所有单据和库存都从这里取数。
- **页面动作**：**搜索框（品牌/型号/分类，自带清除按钮）**；分类下拉筛选；新增 / 编辑；列表＝名称、分类、进价、批发价、零售价、库存、状态、操作；低于预警值显示红色「预警」。手机端卡片列表 + 右下角「＋」。
- **表单字段**：品牌、型号（名称＝品牌+型号实时预览）、分类、规格、单位、进价、批发价、零售价、库存预警值、状态。价格按角色权限显隐。
- **作用**：一处建档，采购、销售、库房自动带出。

### 3. 库存管理　`/stock`　✅ **（第十二轮：升级为 Hub 页，原「库存作业」已并入；第十七轮起默认打开库存作业）**
- **做什么**：全公司唯一的库存页面，**老板/财务/库房共用同一个页面**，按角色自动决定显示哪些列。
  **第十二轮起「库存作业」整页并入本页**，页内四个 Tab 切换，侧边栏不再单列「库存作业」。
- **页内四个 Tab**：`?tab=ops|detail|alert|flow`
  - **① 库存作业**（**默认**）：内嵌原来的 `/warehouse` 六大模块（入库验货 / 出库拣货 / 调拨 / 盘点 / 退换货 / 库房管理）
  - **② 库存明细**：搜索 / 分类 / 库房 / 「仅看低库存」开关；动态库房列 + 合计；每页 20 条。
    **第十七轮起 4 张汇总卡移到这里、放在搜索框上方**，与明细同屏核对
  - **③ 库存预警**：每页 20 条（全站统一） + **斑马纹表格**；列：序号 / 商品 / 分类 / 各库房 / 当前库存 / 预警值 / 缺口 / 单位 / 状态；带搜索 + 分类 + 库房 + 重置
  - **④ 出入库流水**：最近 50 条，含类型、商品、数量、关联单号、时间
- **页面动作**：
  - 4 张汇总卡（**在「库存明细」模块内、搜索框上方**）：商品 SKU 数 / 库存总量 / 库存金额（进价）/ 库存预警数；
    口径始终按**全量商品**统计，不随页内搜索变化
  - 首屏骨架屏（数据未到显示骨架，不再闪空态）
  - 手机端自动切换为卡片视图（预警卡片同样斑马纹），4 张汇总卡 2×2 排列
- **权限**：库房角色看不到任何价格列（进价/批发价/库存金额全隐藏）；**「库存金额（进价）」卡按进价汇总，
  与进价同权限 —— 销售、库房都看不到**；老板 / 采购 / 财务可见。
- **作用**：知道什么货快没了、货值多少、最近货是怎么动的，作业也在这里完成，不用再来回切菜单。
- **旧路径**：`/boss/stock`、`/warehouse/stock` 会自动跳转到本页；`/warehouse` 及其子路由**全部保留**，收藏夹不会失效。

### 4. 库存作业（已并入库存管理，`/warehouse` 路由保留）　✅
- **做什么**：把库房的日常作业收拢到一个页面，**六个模块一大屏**切换：
  **入库验货 · 出库拣货 · 库存调拨 · 库存盘点 · 退换货 · 库房管理**。
- **为什么合并**：原先侧边栏「入库验货」「出库拣货」各占一项，功能上同属库房作业，
  菜单层级冗余。合并后侧边栏只留一个「库存作业」，作业页内用 Tab 切换，路径不丢
  （`/warehouse/inbound`、`/warehouse/outbound` 等仍可直接访问，收藏夹不失效）。

#### 4.1 入库验货　`/warehouse/inbound`　✅
- **页面动作**：两个页签 —— **待收货**（待入库/部分入库采购单，可按单号·供应商·日期筛选）
  与**入库历史**（带搜索框，按时间/采购单号/供应商/商品/数量/操作人列出全部收货痕迹）。
  点进详情逐行填**实收数量**（默认带出「待收」数，可少于应到）→「确认入库 / 取消」。
- **验收备注**：详情页有**验收备注**输入框，记录到货异常、外箱破损、数量不符等有效信息，
  随入库单留存；单据明细页可再次编辑保存。
- **作用**：库存自动增加、生成流水、采购单状态转为 部分入库 / 已完成、自动生成应付；支持**分次入库**（多次收货累计够才完成）。
- **操作列**：「明细 / 撤回」两个链接按钮已**拉开 16px 间距**，不易误触。

#### 4.2 出库拣货　`/warehouse/outbound`　✅
- **页面动作**：两个页签 —— **待发货**与**出库历史**（带搜索框 + 日期区间，列出时间/销售单号/客户/商品/数量/操作人）。
  详情每行显示应发 / 已发 / 待发 / 当前库存，填**实发数量** →「确认出库 / 取消」。
- **拣货备注**：详情页有**拣货备注**输入框，记录分车、缺货说明等信息，随出库单留存。
- **作用**：库存自动扣减、生成流水、销售单状态更新、自动生成应收；支持**分次出库**。
- **超发拦截**：实发超过「待发」或超过当前库存时直接拦截并提示，不再依赖建单时校验。
- **操作列**：「明细 / 撤销」两个链接按钮已**拉开 16px 间距**，不易误触。

#### 4.3 库存调拨　`/warehouse/transfer`　✅
- **做什么**：在「总仓 / 门店」之间移动货物。
- **页面动作**：**新建调拨**（选调出库位 → 调入库位 → 从商品选择器加货 → 填调拨数量 → 提交）
  与**调拨历史**（可展开查看明细）。
- **规则**：调出与调入库位不能相同；来源库位库存不足时**整体拒绝**（不允许部分成功）。
- **库存影响**：只调整两边库位分布，**总库存不变**（总库存 = 各库位之和 的等式始终成立）；
  生成两张 `transfer` 流水（出 / 入）留痕。

#### 4.4 库存盘点　`/warehouse/count`　✅
- **做什么**：把某库位的**系统库存**与**实盘数量**对齐。
- **页面动作**：**新建盘点**（选库位 → 逐个商品填实盘数，或「加入全部商品」批量带出系统库存）
  与**盘点历史**（含盘盈 / 盘亏件数）。
- **差异呈现**：每行显示 系统库存 / 实盘数量 / 差异，盘盈绿、盘亏红，底部汇总盈、亏合计。
- **库存影响**：差异**同时**校正该库位分布与总库存（同幅增减，等式不变）；
  写一条 `adjust` 流水（备注 `盘点 PDxxxxxxxx-xxx`），可追溯。

#### 4.5 退换货　`/warehouse/returns`　✅
- **做什么**：统一的退货单据管理，覆盖**销售退货**与**采购退货**两类。
- **销售退货**（客户退回）：选原销售单 → 逐行填退货数量（不超过原单数量）→ 提交。
  库存**回流总仓**、总库存 +，生成退货单 + `sale_return` 流水，财务记一笔客户 Credit 冲减该单应收。
- **采购退货**（退回供应商）：选原采购单 → 逐行填退货数量（不超过原单数量，且**在手库存须足够**）→ 提交。
  库存**流出总仓**、总库存 −，生成退货单 + `purchase_return` 流水，财务记一笔供应商 Credit 冲减该单应付。
- **历史**：退换货历史列表同时汇总两类单据，可展开看商品 / 数量 / 单价 / 金额 / 原因，便于对账追溯。

### 6. 采购管理　`/purchase/orders`　✅
- **做什么**：采购单列表 + 新建 + **详情**。
- **页面动作**：**搜索框**（单号 / 供应商，自带清除按钮）+ 状态筛选 + **日期区间** + 一键重置，
  可查任意历史单据；「＋ 新建」（选供应商 → 添加商品带进价 → 填数量 → 自动算合计 → 提交）；
  列表「**查看**」→ 跳**该单详情页**。
- **详情页** `/purchase/orders/:id`：单头（单号 / 状态 / 付款状态 / 供应商 / 联系人 / 日期 / 备注）、
  商品明细、金额合计、**入库进度条**、**入库流水**（时间/商品/数量/操作人）、**付款记录**（含操作人）、
  **打印按钮**、底部「**返回**」。
- **打印**：先弹预览（含单价开关）→ 预览页「取消 / 打印」。

### 7. 销售管理　`/sales/orders`　✅ **（第二十轮：新增页内「报价单」Tab）**
- **做什么**：销售单列表 + 新建 + **详情**；页头 Tab「**销售单 / 报价单**」切换（报价单入口 `/sales/quotes`）。
- **页面动作**：与采购端一致 —— **搜索框**（单号 / 客户）+ 状态筛选 + **日期区间** + 重置；
  新建页只显示批发价（销售看不到进价），库存不足会拦截；
  新建页底部「**取消 / 提交销售单**」。
- **详情页** `/sales/orders/:id`：单头、明细、金额、**出库进度**、**出库流水**、**收款记录**（含操作人）、
  **打印送货单**（先预览、可切换是否含单价）、底部「**返回**」。
- **报价单** `/sales/quotes`（第二十轮）：列表 + 新建 + 详情。
  - 新建：选客户或**散客**（直接填名）→ 加商品 → 填数量与报价（默认带批发价可改）→ 有效期 / 备注；
  - 报价不占库存、不生成应收；单号 `BJ…`；
  - 详情页「**转为销售单**」：明细价格整单带过去、**散客自动建档**，报价单回填销售单号并标记「已转销售单」，不可重复转换；
  - 支持**打印报价单**与删除（已转换的不可删）。

### 8. 客户管理　`/customers`　✅ **（第十三轮：新增开票与对公账户资料）**
- **做什么**：客户档案 + 经销商账号，统一管理。
- **页面动作**：**搜索框**（客户名称 / 联系人 / 电话 / **开票抬头 / 税号**，自带清除按钮）；「仅看经销商 / 仅看普通客户」筛选；新增 / 编辑。
- **表单分组**（第十三轮重组为三组）：
  1. **基础资料** —— 名称、等级、联系人、电话、地址、账期；
  2. **开票与打款资料** —— 开票抬头、纳税人识别号、开票地址、开票电话、开户银行、对公账号
     （抬头 + 税号都填即提示「资料完整，可直接开票」）；
  3. **备注 / 经销商账号** —— 备注；勾选「设为经销商」展开登录手机号 / 密码（开通后客户可自行登录看产品目录与批发价）。
- **列表**：新增「**开票资料**」列（绿「完整」/ 灰「未填」徽标）；经销商打「经销商」徽标并显示登录号；电脑端表格，手机端卡片。
- **旧路径**：`/sales/customers`、`/boss/dealers` 自动跳转本页。

### 9. 供应商　`/purchase/suppliers`　✅ **（第十三轮：补搜索 + 备注 + 开票资料）**
- **做什么**：供应商档案。
- **页面动作**：**搜索框**（名称 / 联系人 / 电话 / 开票抬头 / 税号）；新增 / 编辑；点整行进入编辑。
- **表单字段**：名称、联系人、电话、地址、账期 + **开票与打款资料六项**（开票抬头、纳税人识别号、开票地址、开票电话、开户银行、对公账号）+ **备注**。
- **列表**：显示「开票资料完整」徽标 + 开票摘要行（抬头 · 税号 · 开户行账号）+ 地址行 + 备注行。
- **作用**：采购单的供应商来源，也是应付的往来单位；开票资料用于对账与开票。

### 10. 财务管理（财务中心）　`/finance`　✅ **（第八轮：收拢为四合一）**
- **做什么**：一个页面收拢财务全部作业，四个 Tab ——
  1. **🧾 应收应付**：对账 + 登记收付款 + 完整收付款历史（见下）
  2. **📊 经营报表**：原「经营报表」菜单已并入此处（销售/成本/毛利/趋势/预警）
  3. **✏️ 记一笔**：不走采购 / 销售单的日常收支（送货费、物流费、快递费、安装费、辅材费等）
  4. **💸 资金流水**：收付款 + 记一笔合并的资金时间线
- **页面动作（应收应付）**：三个页签 ——
  1. **应收（客户欠）**：搜索框（单号 / 客户）+ 结清状态筛选（未结清 / 已结清 / 全部）；
     每行显示单号、客户、日期、总额、已收、余额；点「登记收款」输入金额 → 确认。
  2. **应付（欠供应商）**：同上，往来单位换成供应商，登记付款。
  3. **收付款流水**：搜索框（单号 / 客户 / 供应商）+ 类型筛选（全部 / 仅收款 / 仅付款）+ 日期区间 + 重置；
     表格列出时间、类型、单据号、往来单位、金额、操作人、备注；顶部汇总收款合计与付款合计。
- **为什么之前「看不到历史」**：旧版只列出还有余额的单据，**单据一旦结清就从列表消失**，
  历史查不到。现在改为显式「未结清」筛选（默认）+ 「已结清 / 全部」可切换，
  且单笔收付款全部沉淀在「收付款流水」页签里，**永不丢失**。
- **权限与配色**：收款金额绿色、付款金额红色（中式习惯）。

### 11. 员工管理　`/boss/users`　✅ **（第八轮：新增权限配置）**
- **做什么**：给采购 / 销售 / 财务 / 库房开登录账号，**并用开关给每个角色分配可见模块**。
- **页面动作**：两个 Tab ——
  1. **👥 员工列表**：卡片统计（员工总数 / 在职 / 已停用 / 角色数）+ 表格。
     每行显示姓名、登录手机号、角色、**该角色可见模块标签**（如「🏠 工作台 📦 商品档案 +8」）、状态、操作；
     支持按姓名/手机号搜索与按角色筛选；新建 / 编辑 / 停用 / 改密。
  2. **🔐 权限配置**：选角色 → **按分组的模块开关矩阵**（工作台 / 业务 / 库存 / 财务 / 系统），
     每个模块带图标、名称与作用说明；
     支持「全选 / 按角色推荐 / 清空 / 本组全选 / 本组全不选 / 💾 保存权限 / ↺ 恢复默认」。
- **生效方式**：**开关保存后立即生效**——该角色下次登录或刷新页面，
  侧边栏与手机底部导航只显示勾中的模块；未勾中的路由会被守卫拦截并退回该角色首页。
- **规则**：
  - 权限是**角色级**的，改一次对该角色所有员工生效；
  - 老板为最高权限，**不参与配置**、恒定全部可见；
  - 至少保留一个模块（不允许清空），否则该角色登录后无页面可用；
  - 已下线的模块自动剔除，升级后不会残留脏数据；
  - 权限存在数据库里，可被备份与云同步带走。

### 12. 经营报表　`/finance?tab=reports`　✅ **（第八轮：已归入财务管理）**
- **做什么**：老板看经营汇总（原「毛利核算」并入此处）。
- **入口变更**：侧边栏不再单独列「经营报表」，改为在「🧾 财务管理」页内第二个 Tab；
  旧路径 `/boss/reports` 仍保留可访问（收藏夹不失效）。
- **页面动作**：
  - **日期筛选**（起止日期）+ 快捷区间（本月 / 近3月 / 近半年 / 全部数据）
  - 6 项指标：销售收入、销售成本、毛利、毛利率、应收未收、应付未付
  - **近 6 个月销售/毛利趋势柱状图**
  - 库存预警商品清单
- **作用**：既能看整体盘子，也能按任意时间段核对；趋势图看淡旺季。
- **旧路径**：`/finance/gross-profit` 自动跳转本页。

### 13. 操作日志　`/boss/audit-logs`　✅ **（本轮新增）**
- **做什么**：追溯「谁在什么时候做了什么」。
- **已记录的动作**：登录、新增/修改商品、创建采购单、采购入库、创建销售单、销售出库、登记收款、登记付款、新增客户、新增供应商、新增员工、导出备份、导入恢复。
- **页面动作**：**搜索框**（动作/详情关键字，自带清除按钮）；按动作类型下拉筛选；列表显示时间、操作人、动作、详情；最新在前。

### 14. 系统设置　`/boss/settings`　✅
- **做什么**：公司信息 + 品牌与图标 + 数据备份恢复 + GitHub 云同步 + 示例数据 + 价格规则 + 打印设置 + 库房入口。
- **（第十三轮）各模块可折叠收缩**：
  - 新增 `ui/CollapseCard.vue`，下面 **8 个模块全部收成可折叠卡片**：
    公司信息 / 品牌与图标 / 数据备份与恢复 / 示例数据 / 价格规则（加价比例）/ 打印设置 / 库房管理 / ☁️ 云同步（GitHub）
  - **默认只展开「公司信息」**，其余显示为一行标题（点标题回弹展开）
  - 页头右侧有「**全部展开 / 全部收起**」一键操作（品牌面板的折叠也由本页统一托管，能一起动）
  - 展开状态记在 `localStorage['erp_settings_panels']`，下次进入保持原样
  - **收起不销毁内容**（DOM 保留），填了一半的表单不会丢；「退出登录」保持为独立区块不折叠
  - 效果：整个设置页高度 **4118px（全展开）→ 810px（默认）**
- **页面动作**：
  - 公司名称 / 地址 / 电话保存；「导出备份」下载 JSON；「导入恢复」选 JSON 恢复；「退出登录」
  - 云同步：填 GitHub 用户名 / 仓库名 / Token / 同步口令，可「同步到云端」「从云端恢复」（AES-GCM + gzip + 指纹去重）
  - **品牌与图标**（`BrandSettingsPanel.vue`）：
    - 登录页标志、登录主标题 / 副标题、侧边栏标志
    - **6 个角色头像**（老板 / 采购 / 销售 / 财务 / 库房 / 经销商）
    - **各模块快捷图标**（按路由改，改完侧边栏、手机 Tab、老板业务页立即生效）
    - 每个图标可选 **emoji（或 1～2 个字）** 或 **上传图片**（自动压成 96px 方图）
    - 支持「恢复默认」；配置存 `localStorage['erp_brand_config']`，随备份/云同步一起走
    - 缺省自动回落到 `navConfig` 的默认 emoji，升级不会把图标变空

### 附：老板手机端两个 Tab 页　✅ **（本轮改造）**
- `/boss/business`「业务」：**入口改为由老板侧边栏菜单自动生成**（排除底部 Tab 自身），新增/调整菜单会自动同步，不会漏项。
- `/boss/mine`「我的」：新增当前登录人卡片（姓名 + 角色），入口＝财务管理 / 报表中心（含毛利）/ 人事权限 / **操作日志** / 系统设置。

---

## 三、其他角色菜单

### 采购（3 项）
| 菜单 | 路由 | 做什么 | 现状 |
|---|---|---|---|
| 工作台 | `/purchase/home` | **待入库单数量、本月采购额、供应商数、未付款金额**；待入库单据列表（点击直达验货页）；快捷操作 | ✅ 真实数据 |
| 采购单 | `/purchase/orders` | 列表 + 新建 + **详情页**（含打印） | ✅ |
| 供应商 | `/purchase/suppliers` | 档案增删改 | ✅ |

### 销售（3 项）
| 菜单 | 路由 | 做什么 | 现状 |
|---|---|---|---|
| 工作台 | `/sales/home` | **待出库单数量、本月销售额、客户数、未收款金额**；待出库单据列表（点击直达拣货页）；快捷操作 | ✅ 真实数据 |
| 销售单 | `/sales/orders` | 列表 + 新建 + **详情页**（含送货单打印）；页头 Tab「销售单 / 报价单」 | ✅ |
| 报价单 | `/sales/quotes` | 询价临时报价（BJ），可**一键转销售单**、散客自动建档 | ✅ 第二十轮 |
| 客户管理 | `/customers` | 客户 + 经销商统一管理，带搜索与清除按钮 | ✅ |

### 财务（4 项）
| 菜单 | 路由 | 做什么 | 现状 |
|---|---|---|---|
| 工作台 | `/finance/home` | **应收未收、应付未付、本月已收、本月已付**；待收款 / 待付款单据列表（点击直达对账页） | ✅ 真实数据 |
| 财务管理 | `/finance` | 财务中心：应收应付 + **经营报表** + **记一笔** + 资金流水（四 Tab） | ✅ |
| 库存管理 | `/stock` | 与老板端同一个页面 | ✅ |

### 库房（3 项）
| 菜单 | 路由 | 做什么 | 现状 |
|---|---|---|---|
| 工作台 | `/warehouse/home` | **待收货单数、待发货单数、今日入库量、今日出库量**；待收货 / 待发货单据列表（点击直达验货/拣货页） | ✅ 真实数据 |
| 库存管理 | `/stock` | 与老板端同一个页面（**库房自动隐藏所有价格**，含「库存金额（进价）」卡）；打开即落在**库存作业**（入库验货 / 出库拣货 / 调拨 / 盘点 / 退换货 / 库房管理） | ✅ |

> **第十二轮起**：侧边栏不再单列「库存作业」，库房默认权限为 `[/warehouse/home, /stock]`，
> 手机端底部 Tab 为「工作台 / 库存管理」。`ALL_MODULES` 共 **11 项**（老板侧边栏 11 项）。

---

## 四、还没做的功能（按优先级）

| # | 缺失功能 | 说明 | 影响 |
|---|---|---|---|
| 1 | **经销商端产品目录页** | 经销商登录后看商品 + 批发价 | 逻辑（`stores/dealer.ts`）已写好，**页面没做**；经销商现在登录会被弹回登录页 |
| 2 | ~~**简版盘点单**~~ | 实盘数量 vs 账面数量，生成差异调整 | ✅ **已完成**（第五轮，`/warehouse/count`，同步校正库位与总库存） |
| 2.1 | ~~**退换货模块**~~ | 销售退货 / 采购退货 的统一单据管理与财务红冲 | ✅ **已完成**（第五轮，`/warehouse/returns`） |
| 2.2 | ~~**库位调拨**~~ | 总仓 ↔ 门店 之间移动货物，总库存不变 | ✅ **已完成**（第五轮，`/warehouse/transfer`） |
| 3 | **员工账号停用 / 改密 / 编辑** | — | 离职员工无法停用，有安全隐患 |
| 4 | **客户 / 供应商停用** | — | 停用客户仍会出现在开单下拉里 |
| 5 | ~~手机端「我的」Tab 路由未注册~~ | `/purchase/mine`、`/sales/mine`、`/finance/mine` | ✅ **已修复**（第三轮） |
| 6 | ~~**真实云同步**~~ | 上传 / 拉取 | ✅ **已完成**（第七轮，GitHub 仓库 + AES-GCM 加密，见第九节） |
| 6.1 | ~~**库房自行设定 / 多库房分布**~~ | 库房增删改、出入库选库房、库存明细看分布 | ✅ **已完成**（第七轮，见第九节） |
| 6.2 | ~~**部署到 GitHub Pages**~~ | 免服务器、多端访问 | ✅ **已完成**（第七轮，`npm run deploy` + `docs/GITHUB_PAGES.md`） |
| 7 | **客户等级 → 批发价档位** | 按等级自动带不同批发价 | 现在只有一档批发价，等级没起作用 |
| 8 | ~~出库「实发 > 库存」前端拦截~~ | 实发 > 待发 / 实发 > 库存 | ✅ **已修复**（第四轮） |
| 9 | **员工 / 客户操作接审计日志** | 目前只有部分动作写日志 | 离职停用等动作无法追溯 |

---

## 五、本轮测试情况

| 测试文件 | 覆盖内容 | 用例数 |
|---|---|---|
| `tests/search-and-merge.test.ts` | 统一搜索组件（清除按钮行为）、库存管理搜索/筛选/权限、客户管理搜索/筛选、报表日期筛选与趋势 | 22 |
| `tests/p0-order-detail.test.ts` | 列表「操作」跳详情页、采购/销售详情页、单据打印（含/不含单价）、操作日志写入与查询 | 11 |
| `tests/p1-workbench.test.ts` | 四个角色工作台的待办数量 / 金额统计 / 跳转 / 完成后消失 | 12 |
| `tests/warehouse-menu.test.ts` | 菜单去重后的路由、旧路径重定向、角色守卫 | 12 |
| `tests/nav-permission.test.ts` | **第三轮新增**：遍历每个角色的每个菜单项，断言路由已注册且登录后能真正停留（不被弹回首页、不跳登录页）；老板全业务可进；越权拦截 | 14 |
| `tests/demo-data.test.ts` | **第三轮新增**：示例数据生成后各表有数、采购/销售/入库/出库/客户/供应商/库存/财务各页面不再是空白、库存与流水一致、经销商账号可登录、幂等保护、清空后账号保留、首屏引导显隐 | 12 |
| `tests/page-actions.test.ts` | **第四轮新增**：`PageActions` 组件（返回/取消/主操作/loading 禁用）、7 个二级页面按钮存在性与跳转、取消不产生数据副作用 | 10 |
| `tests/history-search.test.ts` | **第四轮新增**：采购/销售搜索与日期区间、入库历史、出库历史、财务收付款流水、已结清历史查看、收付款类型筛选 | 10 |
| `tests/print-preview.test.ts` | **第四轮新增**：打印预览弹窗渲染、取消/打印按钮、打印调用与回退、含单价开关、无权限隐藏开关 | 8 |
| 其余既有测试 | 阶段 1~6、历史 phase 测试、业务流程、逐菜单渲染 | 172 |

| `tests/product-batch.test.ts` | **第五轮新增**：CSV 导入去重/覆盖、同名商品合并删除、批量改分类、商品选择器搜索+库存+分页+已选高亮、批发/零售切换、加价比例与应用、出入库改量与超限拒绝 | 20 |

**合计 325 用例全部通过**（31 个测试文件），`npm run build`（含 `vue-tsc` 类型检查）通过。

> `tests/nav-permission.test.ts` 在修复前**失败 8 项**，正是本次「点开菜单被弹回」事故的回归防线。

---

## 六、查询与历史留痕约定（第四轮，2026-09-19）

> 需求原文：采购、入库、销售、出库、财务都应添加查询输入框并能查历史；
> 每个二级页面按业务要有返回或取消；打印要重排且预览页要有打印和取消；
> 财务看不到历史信息；每个模块都要有明细历史，信息流转后必须留痕。

### 6.1 查询输入框一览

| 模块 | 查询范围 | 附加筛选 | 清除按钮 |
|---|---|---|---|
| 采购管理 | 单号 / 供应商 | 状态、日期区间、重置 | ✅ |
| 销售管理 | 单号 / 客户 | 状态、日期区间、重置 | ✅ |
| 入库 · 入库历史 | 单号 / 供应商 / 商品 | 日期区间、重置 | ✅ |
| 出库 · 出库历史 | 单号 / 客户 / 商品 | 日期区间、重置 | ✅ |
| 财务 · 应收/应付 | 单号 / 往来单位 | 结清状态（未结清/已结清/全部） | ✅ |
| 财务 · 收付款流水 | 单号 / 客户 / 供应商 | 收付款类型、日期区间、重置 | ✅ |
| 客户管理 | 客户名称 / 联系人 / 电话 / **开票抬头 / 税号**（第十三轮扩展） | 经销商 / 普通客户 | ✅ |
| 供应商 | 名称 / 联系人 / 电话 / **开票抬头 / 税号**（第十三轮新增） | — | ✅ |

### 6.2 各模块历史明细

| 模块 | 能看到的历史 |
|---|---|
| 采购管理 | 全部采购单（任意状态、任意日期可查）；单详情内可见**入库流水**与**付款记录** |
| 入库 | 待收货单 + **入库历史**（时间/采购单号/供应商/商品/数量/操作人）；单详情内可见本单入库记录 |
| 销售管理 | 全部销售单；单详情内可见**出库流水**与**收款记录** |
| 出库 | 待发货单 + **出库历史**（时间/销售单号/客户/商品/数量/操作人）；单详情内可见本单出库记录 |
| 财务 | 应收 / 应付（可含已结清）/ **收付款流水**（时间/类型/单据号/往来单位/金额/操作人/备注） |
| 库存管理 | 库存明细 + 出入库流水 |
| 系统 | 操作日志（建单/出入库/收付款/建档/登录） |

### 6.3 二级页面操作按钮

| 页面 | 类型 | 按钮 |
|---|---|---|
| 采购单详情 | 详情 | 返回 |
| 销售单详情 | 详情 | 返回 |
| 入库验货 | 作业 | 取消 + 确认入库 |
| 出库拣货 | 作业 | 取消 + 确认出库 |
| 新建采购单 | 表单 | 取消 + 提交采购单 |
| 新建销售单 | 表单 | 取消 + 提交销售单 |
| 新增 / 编辑商品 | 表单 | 取消 + 保存 |

### 6.4 打印（第五轮重做）

点「打印」→ 弹出预览弹窗（iframe 按**真实纸张尺寸**呈现）→ 底部「取消」「打印」→
可勾选「打印含单价」（库房等无价格权限不显示该开关）。

**纸张**（弹窗右上角可切换）

| 选项 | 尺寸 | 用途 |
|---|---|---|
| **A5 横版**（默认） | 210 × 148 mm = A4 纸的一半 | 日常送货单 / 出库单 |
| **A4 竖版** | 210 × 297 mm | 明细较多、需要完整归档 |

**分页**：明细超过一页容量（A5 6 行 / A4 14 行，可手动指定）自动分页，
每页重复表头并带「单号 · 日期 · 制单 · 第 X / Y 页」，**合计与签章只在最后一页**。

**表头与字段可自行编辑**（弹窗内「⚙ 表头设置」，另在系统设置 →「打印设置」也能改）：

- 表头：公司抬头 / 副标题 / 公司地址 / 公司电话 / 页脚文字 / 是否显示签章栏 / 每页行数
- **明细字段**：8 个字段（序号、商品名称、**类别**、型号、单位、数量、单价、金额）可
  **勾选显隐、改列名、上下调顺序**；不打单价时「单价 / 金额」自动剔除

排版：公司抬头 → 单据名 → 单号·日期·制单·页码 → 往来单位信息 → 明细表 → 合计 → 备注 → 签章栏 → 页脚。
明细中的「类别」（洗衣机 / 冰箱 / 空调…）让客户一眼看懂是什么商品。
合计直接落在表格底部合计行（独立的「数量/金额/大写」方框已按要求移除）。

---

## 七、商品与单据能力增强（第五轮，2026-09-19）

> 需求原文要点：①商品档案能导入导出、多选批量编辑、同名合并/删除、导入自动去重；
> ②采购/销售/出入库/财务票据按库房与财务常识重排；③商品列表加搜索框、销售单显示库存、
> 商品档案 100 条分页、其余 20 条分页、全部分页斑马纹；④系统设置一键设定批发/零售加价比例，
> 新建商品只填成本自动算售价；⑤销售单批发/零售切换，金额合计放在明细表最下面；
> ⑥出入库历史单可查看明细、打印、部分出入库拆单关联、修改、撤回。

### 7.1 商品档案 `/boss/products`

| 能力 | 说明 |
|---|---|
| 导入 | 「⇩ 导入」选 CSV；导入时按 **品牌+型号** 与现有商品比对，弹出「跳过 / 覆盖」策略选择，返回新增/更新/跳过/重复数量 |
| 导出 | 「⇧ 导出」把当前筛选结果导出为 CSV（含成本/批发/零售/库存/分类） |
| 多选批量编辑 | 勾选多行 →「批量编辑」→ 可统改分类、品牌、单位、状态、加价后重算售价 |
| 重复检测 | 「⧉ 重复检测」列出同名（品牌+型号）商品，支持**合并**（库存与价格合并到主记录）或**删除** |
| 分页 / 斑马纹 | **100 条/页**，偶数行浅底（`#f6f8fb`），带分页器 |

### 7.2 商品选择器（采购单 / 销售单共用）`src/components/ProductPicker.vue`

- 顶部搜索框（商品名 / 型号 / 品牌）+ 分类筛选 + 「只看有货」
- 列表显示**可用库存**（偏低 / 无货提示），销售单下**无库存商品不可销售**
- 分页 **20 条/页** + 斑马纹
- 已加入明细的商品：**整行蓝色高亮**（`#e8f0fe`），「＋ 添加」按钮变**橘色「已选」**（可重复添加时显示次数）

### 7.3 采购单 / 销售单明细

- 表头：序号 / 商品名称 / 类别 / 单位 / 库存 / 数量 / 单价（可改） / 金额 / 操作
- **合计行在明细表最下方**（`tfoot`）：合计数量 + 合计金额，不再单独放在列表外
- 销售单顶部「本单价格类型」：**批发价（经销商拿货）/ 零售价（散客零售）** 一键切换，
  切换后单价与合计同步刷新（例：海尔 KFR-35GW 批发 ¥2,200 → 零售 ¥2,699）

### 7.4 加价比例（系统设置 `/boss/settings`）

- 公式：批发价 = 成本 ×（1 + 批发加价率），零售价 = 成本 ×（1 + 零售加价率）
- 可设取整方式：整元（推荐）/ 整十元 / 保留两位小数
- 「⚡ 一键应用到全部商品」按各自成本重算全库批发价与零售价（成本与库存不受影响）
- 新建/编辑商品**只填成本**，两个售价自动带出（页面实时试算：成本 ¥2,000 → 批发 ¥2,300、零售 ¥2,600）

### 7.5 出入库单据（拆单 / 修改 / 撤回）

| 页面 | 能力 |
|---|---|
| `/warehouse/inbound`、`/warehouse/outbound` | 双 Tab：**待收货 / 待发货** + **入库历史 / 出库历史**（单号、来源单、往来单位、商品、数量、金额、时间、经手人，分页 20 条 + 斑马纹），历史行有「明细」 |
| `/warehouse/inbound/doc/:batchNo`、`/warehouse/outbound/doc/:batchNo` | 单据明细：抬头信息 + 明细表（含类别、上限）+ **合计行** + 拆单提示 + **同源单据的其他批次**（可点击互跳）+ 🖨 打印 / ✎ 修改单据 / ↩ 撤回本单 |
| 部分入库 / 部分出库 | 验货/拣货页「本次实收」按实际到货改量，**一次收货生成一张入库单**；同来源单的多个批次通过来源单号关联，详情页显式提示「本单为其中之一」 |
| 修改 | 明细页进入编辑态，逐行改数量（上限为来源单订购量，超限提示），保存后**按新数量重算库存与来源单状态**，库存与流水同步回滚/追加 |
| 撤回 | 二次确认后整单撤回：库存原路退回、来源单恢复为待入库/待出库、流水留痕，随后返回列表 |

**实机验证（老板账号，示例数据）**：入库单 RK…555 改量 6→4 保存后合计 ¥13,400→¥10,200；
部分入库生成新批次 RK…210（+13），详情页显示「来源采购单共有 2 个批次」并可互跳；
撤回 RK…210 后收货进度 27/32 → 14/32、库存 7 台 → 4 台，回滚正确。

---

## 八、库存作业合并与调拨 / 盘点 / 退换货（第六轮，2026-09-19）

> 需求原文要点：①验收单增加备注框，记录有效信息；②入库验货「本单收货」操作列的
> 「明细 / 撤回」按钮太近易误触；③出库拣货「本单发货」操作列的「明细 / 撤销」同样太近；
> ④入库验货、出库拣货统一归入「库存作业」，并在其中新增 **调拨** 与 **盘点** 模块，
> 即库存作业页内共 **入库验货 / 出库拣货 / 调拨 / 盘点** 四大模块；
> ⑤项目应补充**退换货**模块，覆盖销售退货与采购退货，并统一管理单据。

### 8.1 备注框

| 页面 | 能力 |
|---|---|
| 入库验货详情 | 新增**验收备注**输入框（到货异常 / 外箱破损 / 数量不符），随入库单留存 |
| 出库拣货详情 | 新增**拣货备注**输入框（分车 / 缺货说明），随出库单留存 |
| 出入库单明细 `/warehouse/{inbound,outbound}/doc/:batchNo` | 备注可**再次编辑并保存**（写审计日志），打印时一并带出 |

### 8.2 操作列间距

「明细」与「撤回 / 撤销」按钮包在 `.op-cell` 内（`display:inline-flex; gap:16px`），
实机测得计算样式 `gap=16px`，点错概率显著下降。

### 8.3 菜单合并

老板端与库房端侧边栏均由「入库验货 + 出库拣货」两项合并为一项「**🧱 库存作业**」（`/warehouse`），
老板端侧边栏 **14 → 13 项**；原 `/warehouse/inbound`、`/warehouse/outbound` 路由保留，直达不失效。

### 8.4 数据模型新增

| 表 | 用途 |
|---|---|
| `locations` | 库位（默认 总仓 id=1 / 门店 id=2） |
| `locationStock` | 库位库存分布（productId + locationId） |
| `transferOrders` / `transferItems` | 调拨单 / 明细 |
| `stocktakes` / `stocktakeItems` | 盘点单 / 明细 |
| `returnOrders` / `returnItems` | 退（换）货单 / 明细（`kind: 'sale' \| 'purchase'`） |

`StockRecord.type` 扩展 `transfer` / `sale_return` / `purchase_return`；
`Payment.type` 扩展 `refund`（客户 Credit）/ `supplier_credit`（供应商 Credit）。
数据库版本 `dexie v4 → v5`。

### 8.5 库存一致性不变量

> **总库存（`stock`）= Σ 各库位（`locationStock`）**

| 动作 | 总库存 | 库位分布 |
|---|---|---|
| 采购入库 / 销售出库 | ± N | 总仓 ± N（同步） |
| 调拨 | **不变** | 调出 − N、调入 + N |
| 盘点 | ± 差异 | 该库位 ± 同一差异 |
| 销售退货 | + N | 总仓 + N |
| 采购退货 | − N | 总仓 − N |

### 8.6 退货与财务红冲

- 销售退货 → 写 `refund`（客户 Credit），`listReceivables` 的余额 = 原单金额 − 已收 − 退货金额。
- 采购退货 → 写 `supplier_credit`，`listPayables` 的余额 = 原单金额 − 已付 − 退货金额。
- 两道校验：退货数量不得超过原单数量；采购退货还要求**在手库存足够**（实物退得出）。

### 8.7 测试与实机验证

- 新增 `tests/returns.test.ts`（5 项）与 `tests/warehouse-ops.test.ts`（7 项）。
- 全套 **337 个测试通过（33 个文件）**，`vue-tsc -b` 构建 **0 错误**。
- 实机（老板账号 + 示例数据）：库存作业页 5 个 Tab 正常切换；入库验货备注框可见可输入、
  确认入库后生成批次且「明细 / 撤回」`gap=16px`；出库拣货「明细 / 撤销」`gap=16px`；
  调拨页（总仓 → 门店）与盘点页（含「加入全部商品」）正常渲染；退换货页三子页正常。

---

## 九、多库房与云同步（第七轮，2026-09-19）

本轮解决两件事：**库房由自己设定、一物可分布多库**；**数据加密存进 GitHub，实现免服务器多端同步**。

### 9.1 库房自行设定

「库存作业」页新增第 6 个 Tab **🏬 库房管理**（系统设置里也有入口「🏬 去管理库房」）。

| 能力 | 说明 |
|---|---|
| 新增 | 填名称（≤20 字，重名 / 空名拦截）+ 备注（地址 / 用途） |
| 改名 | 库房 id 不变，**库存分布与历史单据自动跟着新名字显示** |
| 删除 | 有库存时拦截并提示「还有 N 件库存，请先调拨或出库清空」；且至少保留一个库房 |
| 库房一览 | 显示每个库房的库存件数、商品种数；「总仓」标 `默认` 徽标 |

默认库房仍是 **总仓 / 门店**；`DEFAULT_LOCATIONS` 之外可随意新增（如 北京仓 / 退货仓）。
示例数据播种时会额外生成 **1 张调拨单**，让库存天然分布在不同库房，便于演示。

### 9.2 出入库增加库房选择（单据级）

| 页面 | 选库房的位置 | 效果 |
|---|---|---|
| 入库验货详情 | 顶部「**入库库房**」下拉 | 本次收货全部进所选库房；**下次收货可换库房**，各记各的数量与流水 |
| 出库拣货详情 | 顶部「**出库库房**」下拉 | 本次发货全部从所选库房出；「可用库存」列显示的就是**该库房**的现有量 |

- 出库校验改为**按所选库房**：该库房不够就整单拒绝（避免「总库存够但本库房没货」的错发）。
- 连续收 / 发同一单时，库房默认沿用**上一批**的库房，不用每次重选。
- `purchase.inbound(..., locationId)` / `sales.outbound(..., locationId)` 新增参数；
  `stockRecords.locationId` 落库；出入库单明细列表新增**库房**列。

### 9.3 库存明细体现多库房分布

`/stock` 桌面端表格按库房**动态生成列**：`商品 | 分类 | 总仓 | 门店 | 北京仓 | … | 合计库存 | 单位 | …`。
库房增删改后表头自动变化，一格一库一眼看清分布；手机端卡片显示「总仓 2 / 门店 1 / 北京仓 0」+ 合计。
顶部另有「全部库房」筛选，可按库房过滤。

### 9.4 云同步（GitHub）

**入口**：系统设置 → ☁️ 云同步（GitHub）。

| 区域 | 内容 |
|---|---|
| 数据概览 | 数据空间（总条数）、上次同步时间、连接状态（已配置 / 未配置） |
| 主按钮 | ☁️ 同步到云端、🔄 从云端恢复、⚙️ 同步设置 |
| 六项配置 | GitHub 用户名、仓库名、分支、快照路径、Access Token、同步口令 |
| 辅助 | 保存同步设置 / 测试连接 / 清除本机配置；Pages 地址快捷链接；安全说明 |

**实现要点**

- 存储走 **GitHub Contents API**（GET 读 / PUT 写），快照默认 `data/erp-snapshot.json`。
- **先 gzip 再 AES-GCM 加密**：口令经 PBKDF2-SHA256（20 万次迭代）派生密钥；
  仓库里只有密文，没口令谁都解不开，仓库设 Public 也不怕。口令不上传。
- **指纹去重**：上传前比对明文 SHA-256，没变化直接提示「云端已是最新」，不产生多余 commit。
- **覆盖式**：每次覆盖同一个文件，仓库不会越用越臃肿。
- **恢复前二次确认**；写入失败会**回滚**到恢复前的数据。
- 错误提示全部中文化（401 / 403 / 404 / 409 / 422 / 网络不通 / 口令不对）。
- 页面跑在 `*.github.io` 上时，会**从网址反推用户名与仓库名**并预填。
- 导出的表由 `db.tables` 动态生成，**新增表自动纳入备份**（库房 / 调拨 / 盘点 / 退换货都包含）。

### 9.5 GitHub Pages 部署

| 文件 | 作用 |
|---|---|
| `scripts/deploy-gh-pages.sh`（`npm run deploy <仓库地址>`） | 构建并把 `dist` 推到 `gh-pages` 孤儿分支 |
| `public/.nojekyll` | 禁用 Jekyll，避免下划线文件被丢弃 |
| `public/404.html` | 兜底跳转（hash 路由下一般用不到） |
| `docs/GITHUB_PAGES.md` | 建仓库 → 生成 Token → 部署 → 配置同步 → 排错，全流程说明 |
| `vite.config.ts` | `base: './'` 相对路径，天然适配仓库子目录 |

### 9.6 数据模型与版本

- `locations` 增 `remark`、`createdAt`（无需新索引，schema 仍是 v5）。
- 同步备份结构 `version: 2`，`tables` 改为 `Record<string, unknown[]>`（动态表名）。
- 导入用 `bulkPut` 而非 `bulkAdd`，带主键的备份行不会因自增 id 冲突整批失败。

### 9.7 测试与实机验证

- 新增 `tests/cloud-sync.test.ts`（12 项）、`tests/warehouse-locations.test.ts`（7 项）。
- 修正过时用例：`phase9` 备份版本号 1 → 2、「上传即成功」改为「未配置时提示先配置」。
- 全套 **356 个测试通过（35 个文件）**，`npm run build`（`vue-tsc -b`）**0 错误**。
- 实机（老板账号 + 示例数据）：库存作业 6 个 Tab 正常；库房管理新增「北京仓」成功；
  调拨页调出 / 调入下拉**动态**出现北京仓；库存明细表头为 `总仓 | 门店 | 北京仓 | 合计库存`，
  行内分布与合计一致（如 海尔 KFR-35GW = 2/2/0/4）；入库页「入库库房」、出库页「出库库房」下拉正常；
  云同步区显示「数据空间 3 条 / 从未同步 / 未配置」，展开后 6 项配置齐全，
  Token 与口令为密码框；未配置就点同步会提示「请先填写并保存同步设置」。

---

## 十、第八轮：角色权限开关 + 记一笔 + 界面精细化

### 10.1 需求对照

| # | 需求 | 实现 | 状态 |
|---|---|---|---|
| 1 | 员工管理里用开关给每个角色分配模块，分配后各角色登录界面不同 | `stores/permission.ts` + 员工管理「🔐 权限配置」Tab；菜单与路由守卫双过滤 | ✅ |
| 2 | 经营报表归入财务管理，并在财务管理增加「记一笔」 | 财务中心 `/finance` 四 Tab；`ledgerEntries` 表 + `addLedger/ledgerSummary` | ✅ |
| 3 | 全系统排版与卡片精细化 | `styles/theme.css` 设计系统 + `components/ui/*` 通用组件 | ✅ |

### 10.2 角色权限模型

```
角色（role）─── 角色级权限（rolePerms.modules[]）─── 模块清单（ALL_MODULES）
                        │
                        ├─ 未配置 → 回退 DEFAULT_ROLE_PERMS
                        ├─ 老板   → 恒定全部可见，不接受配置
                        └─ 已配置 → 只保留合法路由，至少保留 1 个
```

- **生效点有三处，必须同时过滤**：侧边栏 `SideBar`、手机底部导航 `MobileTabBar`、路由守卫 `beforeEach`。
  只改菜单不改守卫会出现「菜单没了但手工输网址还能进」的漏洞。
- **权限存数据库**（`rolePerms` 表），不存 localStorage —— 这样能被备份与云同步带走。
- **手机端「我的」入口**也取自同一份清单，不会出现两端菜单不一致。

### 10.3 「记一笔」数据模型

| 字段 | 说明 |
|---|---|
| `direction` | `in` 收入 / `out` 支出 |
| `category` | 送货 / 物流 / 快递 / 安装 / 辅材 / 维修 / 工资 / 房租 / 办公 / 招待 / 其他费用（支出）；服务 / 返利 / 利息 / 押金退回 / 其他收入（收入） |
| `amount` | 金额（> 0，两位小数） |
| `entryDate` | 发生日期（默认今天，可改） |
| `counterparty` | 往来单位（选填） |
| `remark` | 备注（选填） |

**与采购 / 销售结算完全隔离**：记一笔写 `ledgerEntries`，**不写 `payments`**，
因此不会产生应收应付，也不会污染毛利核算；只在「记一笔汇总 / 资金流水」里体现。

### 10.4 界面设计系统

- **令牌层**：品牌色、状态色、中性色、圆角（`--r-xs`~`--r-pill`）、阴影（`--sh-xs`~`--sh-lg`）、间距（`--sp-1`~`--sp-6`）。
- **组件层**（`src/components/ui/`）：`PageHeader`、`SectionCard`、`StatCard`、`EmptyState`、`ToggleSwitch`、`SegmentedTabs`。
- **原生控件基线**用 `:where()` 包裹 → 特异性为 0，Vant 等组件库自带样式**仍然优先**，不会互相打架。
- **遗留类名收敛**：`.block` / `.sec-title` / `.tabs` 等早期类名统一到设计系统，
  17 个页面删掉各自重复定义，全站观感一致。

### 10.5 验收结果

- 新增 `tests/permission.test.ts`（8 项）、`tests/ledger.test.ts`（8 项）。
- 修正过时用例：`stage1` 1.7/1.9、`stage3` 3.3/3.5、`warehouse-menu` 财务菜单断言；
  `history-search` / `stage6` 的 `.tabs button` 选择器改为 `.ui-seg button`。
- 全套 **372 个测试通过（37 个文件）**，`npm run build`（`vue-tsc -b`）**0 错误**。
- 实机：权限矩阵 17 个开关按 5 组展示，关掉「库房工作台」后保存提示「已保存，该角色共 2 个模块」；
  用新建的库房账号登录，**侧边栏只剩「库存作业 / 库存管理」两项**（权限真实生效）；
  记一笔填示例 → 保存成功生成 `JY20260919-612`，汇总显示「本期支出 ¥120.00 / 净额 ¥-120.00」，
  流水行显示「🚚 送货费用 · 顺丰同城 · -¥120.00」；财务中心经营报表 Tab 正常渲染 6 项指标与趋势图。

---

## 十一、第九轮：设计系统正式采用（2026-09-19）

用户看过第八轮的界面截图后确认「采用这套风格」，自本轮起视觉规范转为**硬性基线**。

### 11.1 现状盘点

| 状态 | 数量 | 页面 |
|---|---|---|
| 已套用设计系统 | 18 | 老板工作台 / 商品档案 / 员工管理 / 系统设置 / 经营报表 / 操作日志 / 库存明细 / 财务三页 / 采购三页 / 销售三页 / 库房工作台 / 库存作业 |
| 本轮补齐 | 4 | 新增·编辑商品、新建采购单、新建销售单、入库·出库单明细 |
| 无需处理 | 15 | 库存作业的 6 个子页（嵌在 Tab 内，不需要独立页头）、手机端「我的」、业务中心、登录页等（已在第八轮单独精细化） |

### 11.2 本轮两处改动

1. **嵌套页头自动降级**
   财务中心、库存作业这类 Hub 页会把子页整个嵌进 Tab 面板，子页自带的页头会和父页头叠成两层大标题。
   已加全局规则 `.tab-pane .ui-page-head`：面板内页头自动降为紧凑小节标题
   （标题 19px → 15px，副标题 12px，底部虚线分隔）。
   实机实测财务中心 `heads=2`：父「财务管理」19px、子「经营报表」15px，层级清楚不重复。

2. **`PageHeader` 新增状态徽标**
   增加 `badge` / `badge-tone`（`success / warning / danger / info / muted`）两个属性，
   单据详情页不必再自写 `<span class="tag">`。
   例：入库单明细页头显示「入库单 · RK20260919-101 」＋ 绿色「收货入库」徽标。

### 11.3 给后续开发的约束

- 新页面**必须**用 `ui-page` + `PageHeader` + `SectionCard` + `StatCard` + `EmptyState`；Tab 用 `SegmentedTabs`。
- **不要写裸 `input {}` / `select {}` 覆盖样式**——原生控件基线包在 `:where()` 里（特异性为 0），
  改成裸选择器会污染 Vant 组件。
- **页头副标题避开业务敏感词**：「进价」「库存金额」等会被权限测试断言命中
  （销售不得见进价、库房不得见任何价格），改用中性说法（「价格」「库存分布」）。

### 11.4 验证

- 全量 **372 个测试通过（37 个文件）**，`npm run build`（`vue-tsc -b`）**0 错误**。
- 实机确认：财务中心嵌套页头降级、新增商品 / 新建销售单页头、入库单明细状态徽标均正常。
- 截图归档：`docs/screenshots/round9-*.png`（4 张）。
- 已清理替换后残留的 `.pe-title` / `.ct` 局部样式。

---

## 十二、第十一轮：库存作业界面重做与手机端布局修复（2026-09-19）

### 12.1 用户反馈

> 库存作业页面排版太老旧古板，与整体系统不协调，而且页面排布头部与显示部分错位了。

### 12.2 三条根因

| # | 现象 | 根因 |
|---|---|---|
| 1 | 页内子 Tab 像「灰框老式胶囊」 | 手写 `<div class="ui-seg"><button>` 时**漏了 `.ui-seg-item` 类**，退回浏览器默认按钮外观 |
| 2 | 头部 / Tab / 表格左边缘逐层缩进 | `InboundView` 私有容器 `max-width: 1100px` 与父级 1240px 不一致，宽屏下各自居中 |
| 3 | **手机端顶栏变左侧竖条、内容被挤窄** | `.app-layout` 是 `flex` 且默认 `row`，手机端顶栏 / 内容 / 底部 Tab 被横排 |

第 3 条影响**所有手机端页面**，是本次最有价值的修复。

### 12.3 改动清单

| 范围 | 文件 | 改动 |
|---|---|---|
| 设计系统 | `src/styles/theme.css` | 分段控件改「浅色凹槽 + 选中项白色浮起卡片」；新增 `.ui-seg.md` / `.ui-seg.sm` 两档尺寸与 `.ui-seg-badge` 待办角标；手机端横向滑动；`.tab-pane > div.ui-page` 宽度兜底 |
| 通用组件 | `src/components/ui/SegmentedTabs.vue` | 支持 `options[{key,label,icon,badge}]` + `size` + `v-model` |
| 库存作业 Hub | `WarehouseOpsView.vue` | 容器改 `ui-page`；模块导航用 `.ui-seg.md` + 图标 + **实时待办角标** |
| 5 个子页 | `InboundView` / `OutboundView` / `TransferView` / `CountView` / `ReturnsView` | 页内 Tab 统一换 `SegmentedTabs`（`size="sm"`，带计数）；`InboundView` 去掉 1100px 私有宽度 |
| 财务中心 | `ReconcileView.vue` | 对账页三个 Tab 同样换组件 |
| 布局缺陷 | `AppLayout.vue` | **新增 `.app-layout.is-mobile { flex-direction: column }`** |
| 下拉控件 | 入库详情 / 出库详情 / 调拨 / 盘点 / 退换货 | 挂 `.ui-select`，私有样式只留宽度 —— 恢复边框、圆角与下拉箭头 |

### 12.4 验收结果

| 项目 | 结果 |
|---|---|
| 1440 / 1280 视口 | ✅ 标题、模块 Tab、页内 Tab、表格 全部 `x=244` |
| 手机端 390 × 844 | ✅ 顶栏 `w=390` 通栏，子页 `x=12 w=366` |
| 库房下拉 | ✅ `ui-select loc-sel`，`h=40 / 1px / r=8 / 有箭头` |
| 全量测试 | ✅ **372 通过 / 37 文件 / 0 error** |
| `npm run build` | ✅ **0 错误** |

截图归档：`docs/screenshots/round11-*.png`（7 张）。

### 12.5 后续开发约束

1. 页内 Tab 一律用 `SegmentedTabs` 组件，别手写裸 `<button>`。
2. Hub 页内嵌子页不要写 `max-width` 或自己 `margin: 0 auto`。
3. `.app-layout.is-mobile { flex-direction: column }` 不可删。
4. 下拉控件挂 `.ui-select`，私有样式只写宽度 / 外边距。

---

## 十三、第十三轮：开票资料与设置页折叠（2026-09-20）

### 13.1 用户反馈

| # | 原话要点 |
|---|---|
| 1 | 「客户和供应商的信息管理需要添加**开票信息等公户信息**，地址，备注信息等信息」——现有编辑**无法保存**其开票信息等公户信息 |
| 2 | 「系统设置页面中的各个小模块设置成可以进行**回弹收缩**，以保持页面的清洁识别性」 |

### 13.2 客户 / 供应商：新增六个开票与对公字段

| 字段 | 键名 | 说明 |
|---|---|---|
| 开票抬头 | `invoiceTitle` | 发票上的单位全称，可与客户简称不同 |
| 纳税人识别号 | `taxNo` | 统一社会信用代码 / 税号 |
| 开票地址 | `invoiceAddress` | 发票地址栏 |
| 开票电话 | `invoicePhone` | 发票电话栏 |
| 开户银行 | `bankName` | 对公账户开户行 |
| 对公账号 | `bankAccount` | 对公银行账号 |

- 判定口径（`src/utils/invoice.ts`）：**抬头 + 税号都填 = 资料完整（可直接开票）**；只填抬头算「已填未完整」。
- 客户与供应商**共用** `components/InvoiceFieldset.vue`（三列网格六个输入框）。
- 列表可见性：客户列表加「开票资料」列（绿完整 / 灰未填）；供应商列表显示「开票资料完整」徽标 + 摘要行。
- 搜索：两边的搜索框都支持**按开票抬头 / 税号命中**（开票场景常用税号反查）。
- 示例数据的 3 个客户与 3 个供应商都已补齐全套开票资料。

### 13.3 修复：编辑保存会丢备注

原 `CustomersView` 保存时把 `remark` **写死为空串**，且 `openEdit()` 没有回填 `address` 与备注，
导致「编辑一次备注就没了」。本轮改为 **完整回填 + 完整回写**，
并用 `tests/invoice-info.test.ts` 的「改税号 + 改备注 → 保存 → 重读断言」锁死不再回归。

### 13.4 系统设置页可折叠收缩

- 新增 `src/components/ui/CollapseCard.vue`（受控折叠卡片）：
  - 点标题回弹展开 / 收起，**用 CSS `max-height` 折叠而非 `v-if`** → 内容 DOM 保留，**填了一半的表单不丢**
  - 箭头旋转用 `cubic-bezier(.34,1.56,.64,1)` 做出回弹感
  - `role="button"` + `tabindex="0"` + `aria-expanded`，支持键盘 Enter / 空格
  - 尊重 `prefers-reduced-motion`
- 设置页 **8 个模块**全部折叠化，**默认只展开「公司信息」**；
  页头提供「**全部展开 / 全部收起**」；状态存 `localStorage['erp_settings_panels']`。
- 「退出登录」保持独立区块，不折叠。
- 实测页面高度：**4118px（全展开）→ 810px（默认）→ 588px（全收起）**。

### 13.5 验收结果

| 项目 | 结果 |
|---|---|
| 全量测试 | ✅ **419 通过 / 40 文件 / 0 error** |
| `npm run build` | ✅ **0 错误**（2.68s） |
| 新增测试 | `tests/invoice-info.test.ts`（11 例）、`tests/settings-collapse.test.ts`（12 例） |
| 实机验证 | ✅ 客户/供应商开票资料落库并可搜；设置页折叠与一键操作正常 |

### 13.6 后续开发约束

1. 让用户填的表单，保存时必须**完整回写全部字段**，编辑时必须**完整回填**。
2. 表单字段超过 6 个应**分组**，不要平铺一大片。
3. 开票相关判定统一走 `utils/invoice.ts`。
4. 可折叠模块一律用 `ui/CollapseCard.vue`，**CSS 折叠不用 `v-if`**。
5. 页面级折叠状态由页面统一持有并配「全部展开 / 全部收起」，子组件用 `v-model:open` 接入。

---

## 十四、第十五轮：全站列表统一 20 条/页 + 斑马纹（2026-09-20 · V1.0-5）

### 14.1 一句话结论

**系统里所有列表现在都是每页 20 条、都能翻页、都有斑马纹**，不再有「有的 100 条、有的 50 条、有的干脆不分页」。

### 14.2 逐菜单变化

| 菜单 | 路径 | 变化 |
|---|---|---|
| 商品档案 | `/boss/products` | 每页 100 → **20**；表格斑马纹沿用 `#eef2f9` |
| 采购管理 → 采购单 | `/purchase/orders` | **新增分页**（20 条/页）+ 表格斑马纹；手机端卡片同步分页并补斑马纹 |
| 销售管理 → 销售单 | `/sales/orders` | 同上 |
| 客户管理 | `/sales/customers` | **新增分页**（20 条/页）+ 卡片/表格斑马纹 |
| 供应商 | `/purchase/suppliers` | **新增分页**（20 条/页）；卡片列表补斑马纹 |
| 财务管理 → 应收应付 | `/finance/reconcile` | **新增分页**（20 条/页）；应收、应付各记各的页码；余额合计仍按全部数据 |
| 财务管理 → 收付款流水 | `/finance/reconcile` | **新增分页**（20 条/页） |
| 财务管理 → 资金流水 | `/finance` | **新增分页**（20 条/页）；**合计行仍汇总全部**，不受翻页影响 |
| 财务管理 → 记一笔 | `/finance/ledger` | **新增分页**（20 条/页） |
| 库存管理 → 库存作业 / 库房管理 | `/warehouse` | 库房列表 **新增分页**（序号连续）；各作业列表手机端卡片补斑马纹 |
| 库存管理 → 出入库流水 | `/stock` | **新增分页**（20 条/页）；取数上限放宽，长历史也能翻 |
| 库存管理 → 库存预警 | `/stock` | 每页 50 → **20** |
| 操作日志 | `/boss/audit-logs` | **新增分页**（20 条/页）+ 斑马纹；手机端卡片补斑马纹 |
| 员工管理 | `/boss/users` | **新增分页**（20 条/页） |

### 14.3 验收（实机实测）

| 页面 | 实测 |
|---|---|
| 商品档案 | 每页 20 行 · 共 51 条 第 1/3 页 · 斑马纹可辨 |
| 采购单 / 销售单 | 每页 20 行 · 共 29 条 第 1/2 页 |
| 客户 / 供应商 | 每页 20 条 · 共 29 条 第 1/2 页 |
| 操作日志 / 员工 | 每页 20 行 · 共 27 条 第 1/2 页 |
| 资金流水 / 记一笔 / 收付款 | 每页 20 行 · 共 56 / 26 / 30 条 |
| 出入库流水 / 库存预警 | 每页 20 行 · 共 42 / 50 条 |
| 手机端卡片（390 视口） | 20 张卡片 · 斑马纹可辨 |

---

## 十五、第十六轮：应用图标自定义 + 发送到桌面（2026-09-20 · V1.0-5）

### 15.1 入口

**系统设置**（`/boss/settings`）→ 第 3 个折叠模块 **「应用图标与桌面快捷方式」**
（在「品牌与图标」之后；页头「全部展开」可一次展开）。

### 15.2 这个模块能做什么

| 区块 | 内容 |
|---|---|
| 当前应用图标（预览） | 88px 大图预览 + 模拟的浏览器标签页效果；显示当前来源与底色 |
| 图标来源 | **内置图标** / **Emoji · 文字**（可输入 1~6 个字，或点常用 emoji：📦 🛒 🏪 💼 🧾 👑 🚚 🏬）/ **上传图片** |
| 底色 | 品牌蓝渐变 / 深海蓝 / 亮蓝 / 墨黑 / 橘红 / 翡翠绿 / 纯白（上传图片时不生效，用图片本身画面） |
| 操作 | 「✅ 应用到系统」「↺ 恢复默认图标」 |
| 发送到桌面 | 状态徽标（已安装 / 可一键安装 / 需手动添加）+「📲 一键发送到桌面」+「看手动步骤」（按平台给文字步骤） |

### 15.3 改一处，四处生效

| 生效对象 | 说明 |
|---|---|
| 浏览器标签页 / 收藏夹图标 | 立即替换（`link[rel=icon]` 32×32 与 192×192） |
| 苹果「添加到主屏幕」图标 | 立即替换（`apple-touch-icon` 180×180） |
| 安卓 / 电脑「发送到桌面」图标 | manifest 同步重建，安装后桌面图标就是新图 |
| 刷新后保持 | 启动即套用已保存设置，不会掉回默认图 |

### 15.4 「发送到桌面」分平台说明

| 平台 | 用户看到什么 |
|---|---|
| 电脑 Chrome / Edge | 状态显示「可一键安装」→ 点按钮弹**系统原生安装窗**；装完桌面 / 开始菜单出现快捷方式 |
| 安卓 Chrome | 同上（一键安装） |
| 苹果 Safari | 状态显示「需手动添加」→ 点「看手动步骤」依次列出：分享 ⬆️ → 添加到主屏幕 → 添加 |
| 微信内打开 | 明确提示「微信内置浏览器不支持安装」，并指引「在浏览器打开」 |

### 15.5 其它变化

- 系统设置页折叠模块由 **8 个增至 9 个**（页头「全部展开 / 全部收起」自动带上新模块）。
- 装到桌面后**断网也能打开**（Service Worker 缓存应用外壳；数据本就在本机 IndexedDB）。
- 电脑/手机浏览器标签页标题、主题色、可添加到主屏幕声明均已补齐。

### 15.6 本轮测试

| 项 | 结果 |
|---|---|
| 全量测试 | ✅ **462 通过 / 42 文件 / 0 error** |
| `npm run build` | ✅ 0 错误 |
| 新增测试 | `tests/app-icon-install.test.ts`（33 例：分页覆盖矩阵 + 图标工具 + 安装判定 + PWA 资源 + 面板渲染） |
| 实机验证 | ✅ 图标改动即时生效（favicon / apple-touch-icon → dataURL、manifest → blob）；恢复默认可回退；SW 注册且控制页面；拦截全部请求后仍能打开登录页 |

---

## 十六、第十七轮：库存概况卡片下移 + 默认打开库存作业（2026-09-20 · V1.0-6）

### 16.1 改了什么

| # | 位置 | 变化 |
|---|---|---|
| 1 | 库存管理（`/stock`） | 页头的 **商品 SKU / 库存总量 / 库存金额（进价）/ 库存预警** 四张卡，移到「库存明细」模块内、**搜索框上方** |
| 2 | 库存管理（`/stock`） | **默认打开「库存作业」**（原来是库存明细）；`?tab=detail|alert|flow` 仍可直达各模块 |

### 16.2 怎么用

- 点侧边栏「**库存管理**」→ 直接进**库存作业**（入库验货 / 出库拣货 / 调拨 / 盘点 / 退换货 / 库房管理），
  日常收发货不用再多点一下。
- 需要看货值 / 各库房分布时，切到「**库存明细**」：四张概览卡就在搜索框上方，
  搜完筛完一抬眼就能对照「全库多少 SKU、多少件、值多少钱、几项要补货」。
- 概览卡的数字**不随搜索变化**（始终是全库口径），所以筛"只看某库房"时也不会被带偏。

### 16.3 权限与细节

| 项 | 说明 |
|---|---|
| 进价权限 | 「库存金额（进价）」是按**进价**汇总的，与明细表「进价」列同权限 → **销售、库房看不到**（页面不会有「进价 / 库存金额 / ¥」字样） |
| 手机端 | 四张卡 2×2 排列，同样在搜索框上方 |
| 二次进入 | 已在 `/stock?tab=detail` 时再点侧边栏「库存管理」（URL 无 tab）→ Tab 回到**库存作业** |

### 16.4 本轮测试

| 项 | 结果 |
|---|---|
| 全量测试 | ✅ **473 通过 / 43 文件 / 0 error** |
| `npm run build` | ✅ 0 错误 |
| 新增/校准用例 | `tests/stock-cards-v17.test.ts`（8 例：卡片位置 + 默认 Tab + 版本一致性）；
`stock-hub` 新增「不带参数默认库存作业」「统计卡在搜索框上方」「金额卡销售/库房看不到」；
另有 5 组原用例显式带上 `?tab=detail`（默认 Tab 变更导致的断言过期） |
| 实机验证 | ✅ 电脑端：直接进 `/stock` 落在库存作业；`?tab=detail` 四张卡在搜索框上方、口径与明细一致；手机端（390 视口）卡片 2×2 |

---

**文档结束。** 若需要对上表第四节「还没做的功能」继续开发，按表格顺序推进即可。
