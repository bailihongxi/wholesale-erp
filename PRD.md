# 家电批发进销存 ERP 产品需求文档（PRD）

> 文档版本：V2.6　|　**产品版本：V2.0-2（云数据库迁移版）**
> 日期：2026-09-20
> 用途：本文件为后续开发唯一依据，开发过程中如需变更，须经确认后修改本文档。
> 代码基线：`src/version.ts` → `APP_VERSION = 'V2.0-2'`，`package.json` → `version: "2.0.0-1"`。
>
> **修订记录**
> - **V1.0**（2026-09-19）：第一版锁定稿。
> - **V1.1**（2026-09-19）：新增第 2.4 节「各角色电脑端菜单与功能定义」、第 11 节「功能现状盘点与待开发清单」；
>   修正 2.2 手机端 Tab 表使其与已实现结构一致；引用对照文件 `MENU_SPEC.md`（逐菜单功能明细）。
>   另修正交互约定：**侧边栏折叠/展开按钮位于左侧菜单栏头部**（「家电批发ERP」文字之后），
>   功能页面左上角不放置任何返回或折叠按钮。
> - **V1.2**（2026-09-19）：老板拍板「按推荐来」，第 11 节更新为改造后的现状。
>   执行内容：① 合并 3 组重复菜单（库存 / 客户-经销商 / 毛利-报表），老板菜单 15→14 项、财务 5→4 项；
>   ② 四个角色工作台与老板手机端两个 Tab 由占位改为真实数据；
>   ③ 新增采购单/销售单详情页与打印；④ 新增操作日志（真实写入 + 查询页）；
>   ⑤ 新增统一搜索组件 `SearchInput`，全项目搜索框自带清除按钮。详见 11.2～11.6。
> - **V1.3**（2026-09-19）：修复「点开菜单页面空白/功能无效」事故。新增第 11.7 节：
>   ① 老板为最高权限角色，所有业务路由补入 `'boss'`，修复点菜单被弹回工作台；
>   ② 新增一键示例数据 `utils/demoData.ts`（系统设置页 + 工作台首屏引导），
>   解决新装数据库为空导致各页面空白被误判为「功能未开发」；
>   ③ 新增 `RoleMineView` 并注册手机端三个 `/xxx/mine` 路由，修复点击即登出。
>   另：验收状态更新为 255 用例 / 26 文件全部通过，并补充真实浏览器逐页验证结果。
> - **V1.4**（2026-09-19）：新增第 11.8 节「查询 / 历史留痕 / 二级页面按钮 / 打印规范」，
>   从需求层面固化四条规则：① 采购、入库、销售、出库、财务必须提供查询输入框并支持历史查询；
>   ② 每个二级页面按业务提供「返回」（详情页）或「取消」（表单页）；
>   ③ 打印必须先预览，预览页固定提供「打印」「取消」，排版按 A4 单据规范；
>   ④ 任何信息流转都必须留下明细痕迹（出入库流水、收付款流水），
>   各部门在自己的模块内即可查到本部门的历史。验收状态更新为 283 用例 / 29 文件全部通过。
> - **V1.5**（2026-09-19）：新增**第 12 节「打印规范」**并从需求层面固化：
>   ① 纸张为 **A5 横版（A4 的一半，默认）** 与 **A4 竖版** 两种，**取消 A3**；
>   ② 明细超页自动分页，每页重复表头与页码，合计与签章只在末页；
>   ③ **表头与明细字段可由用户自行编辑**（勾选显隐、改列名、调顺序）；
>   ④ 明细必须显示**产品类别**（洗衣机 / 冰箱 / 空调…），便于客户识别；
>   ⑤ 表格与签章/页脚之间不留大片空白，合计只保留在表格底部（移除重复的金额方框）。
>   验收状态更新为 305 用例 / 30 文件全部通过。
> - **V1.6**（2026-09-19）：**产品版本定为 V1.0-1 并发布基线**。涵盖第 13～17 节：
>   ① 第 13 节 —— 库存作业合并（入库验货 / 出库拣货 / 调拨 / 盘点 / 退换货 / 库房管理）与退换货模块；
>   ② 第 14 节 —— 多库房（库房自定义、出入库选库房、库存明细多库房列）与 GitHub 云同步；
>   ③ 第 15 节 —— 角色权限开关矩阵、财务管理「记一笔」、界面精细化（设计系统）；
>   ④ 第 16 节 —— 设计系统经用户确认采用，收口页头 / 徽标 / 嵌套层级；
>   ⑤ 第 17 节 —— **V1.0-1 发布基线**：功能总览、交付清单、Git 仓库与 GitHub Pages 地址。
>   验收状态更新为 **372 用例 / 37 文件全部通过**。
> - **V1.7**（2026-09-19）：**产品版本升为 V1.0-2**。新增**第 19 节「第十二轮：库存合并 / 性能 / 电脑端筛选条 / 品牌自定义」**：
>   ① **库存作业并入库存管理** —— `/stock` 改为 Hub 页（库存明细 / 库存预警 / 出入库流水 / 库存作业四个页内 Tab），
>   老板菜单 12→11 项，侧栏不再单列「库存作业」，原 `/warehouse` 及其子路由全部保留；
>   ② **库存预警独立成模块** —— 每页固定 **50 条**、表格**斑马纹**（沿用全站 `.data-table` 奇偶行底色），
>   带搜索 / 分类 / 库房筛选与分页；
>   ③ **加载体验** —— 首屏骨架屏 `LoadingBlock` 取代"先闪空态再出数据"、
>   批量查询消除 N+1（`getLowStockProducts` / `reconcileProducts`）、路由分包空闲预取、`index.html` 品牌占位消除白屏；
>   ④ **电脑端筛选条紧凑化** —— 仅 `@media (min-width:768px)` 生效：搜索框放大到 `flex:1 1 320px`，
>   下拉 / 按钮 / 日期收缩到内容宽度（`max-width:230px`，日期 158px），控件高度统一 40px，
>   排布收进 1～2 行；**手机端完全不变**；
>   ⑤ **品牌与图标可自定义**（第 19.5 节）—— 系统设置新增「品牌与图标」：登录页标志与标题、
>   侧边栏标志、各角色头像、各模块快捷图标，支持 emoji 或上传图片（自动压 96px 方图），存 `localStorage` 随备份走。
>   验收状态更新为 **392 用例 / 38 文件全部通过**。
> - **V1.8**（2026-09-20）：**产品版本升为 V1.0-3**。新增**第 20 节「第十三轮：开票与对公账户信息 / 系统设置折叠」**：
>   ① **客户 / 供应商开票与对公账户资料**（第 20.1 节）—— 新增开票抬头、纳税人识别号、开票地址、
>   开票电话、开户银行、对公账号六个字段，客户表单重组为「基础资料 / 开票与打款资料 / 备注」三组，
>   供应商页补齐备注与开票资料并支持按抬头 / 税号搜索；**修复了原编辑保存会丢备注的问题**；
>   ② **系统设置各模块可折叠收缩**（第 20.2 节）—— 新增 `ui/CollapseCard.vue`，
>   设置页 8 个模块全部改为可折叠卡片，默认只展开「公司信息」，并支持「全部展开 / 全部收起」，
>   展开状态记入 `localStorage`。
>   验收状态更新为 **419 用例 / 40 文件全部通过**。
> - **V1.9**（2026-09-19）：**产品版本升为 V1.0-4**。第十四轮 UI / 体验修复（5 项）：
>   ① **搜索框放大镜遮挡**：输入后放大镜自动淡出，并修正全局 input 基线特异性，scoped 左内边距不再被压；
>   ② **经营报表左右对齐 + 库存预警 50 条分页斑马纹**：`reports-page` 去 `max-width:1100` 改 `width:100%`，
>   BossReportsView 内嵌低库存预警分页（序号 / 缺口列 + 斑马纹防差行）；
>   ③ **财务「登记收款」行按钮挤字**：金额框固定 120px（双类选择器），确认 / 取消按钮 `white-space:nowrap` 不竖排；
>   ④ **库存管理「库存作业」Tab 移到最前**；
>   ⑤ **全站「返回 / 取消」按钮统一橘红实底白字**：新增 `--c-amber` 令牌与 `.ui-btn-cancel` 类，
>   覆盖 PageActions、财务 / 客户 / 供应商 / 打印 / 库房位置 / 员工 / 商品弹窗等所有取消返回按钮。
>   验收状态更新为 **428 用例 / 41 文件全部通过**。

> - **V2.0**（2026-09-20）：**产品版本升为 V1.0-5**。本轮含两次交付，见本文第 21、22 节：
>   ① **第十五轮 · 全站列表统一 20 条/页 + 斑马纹**：`PAGE_SIZE_LIST / PAGE_SIZE_PRODUCT / PAGE_SIZE_ALERT` 三者统一为 **20**
>   （商品档案由 100 改 20、库存预警由 50 改 20，全站不再有例外）；商品档案、采购单、销售单、应收应付与收付款、
>   资金流水、记一笔、操作日志、员工、客户、供应商、库房、出入库流水 **12 类列表全部接上分页条**，
>   表格走 `data-table`、卡片列表走 `zebra-list`，斑马纹统一为看得出的 `#eef2f9`。
>   ② **第十六轮 · 应用图标自定义 + 发送到桌面**：系统设置新增「应用图标与桌面快捷方式」面板，
>   可把 emoji / 文字 / 上传图片渲染成 PNG，实时替换浏览器标签页、收藏夹与苹果主屏图标，
>   并重建 manifest（安卓「发送到桌面」用的就是改过的这张图）；同时加入 PWA manifest 与 Service Worker，
>   **装到桌面后断网也能打开**。详见第 22 节。
>
> - **V2.1**（2026-09-20）：**产品版本升为 V1.0-6**。第十七轮，见本文第 23 节：
> - **V2.4**（2026-09-20）：**产品版本升为 V1.0-9**。第二十轮：采购 / 销售建单支持**赠品行**（金额计 0、照常出入库、打印标记「赠品」）；销售管理页内新增**报价单**（客户询价临时报价 BJ 单号，订单确定后一键转销售单、散客自动建档、双向可查），见本文第 26 节。
> - **V2.3**（2026-09-20）：**产品版本升为 V1.0-8**。第十九轮：打印模板头部两行改一行（公司抬头居左、单据标题居中）；「记一笔」支持关联单据（入库/出库/退换货/采购/销售/调拨/盘点单）；全站弹窗改为阻塞式（点遮罩不关闭，按 ESC 关闭），见本文第 25 节。
> - **V2.2**（2026-09-20）：**产品版本升为 V1.0-7**。第十八轮：登录系统重新设计（用户名/手机号双通道、密码加盐哈希、连续失败锁定、7 天会话过期、员工档案扩展、自助改密与老板重置密码），见本文第 24 节：
>   ① **库存概况卡片下移** —— 商品 SKU / 库存总量 / 库存金额（进价）/ 库存预警 四张卡从「库存管理」页头
>   移到**库存明细模块内、搜索框上方**，随明细一起看，页头只留标题与 Tab；
>   ② **默认打开「库存作业」** —— `/stock` 不带参数进入时默认落在库存作业（入库 / 出库 / 调拨 / 盘点…），
>   并补上 `route.query.tab` 监听，使从别处再进「库存管理」也能回到 URL 指定的模块。

---

## 1. 产品目标和使用场景

### 1.1 产品目标
为家电批发贸易公司提供一套浏览器可用的进销存管理系统，实现「进货—库存—销售—对账」全流程线上化，数据实时联动，老板随时掌握经营情况。后续架构预留扩展能力，可平滑增加鞋服、家纺、家具等行业。

### 1.2 产品形态
- **网页应用**，部署在 GitHub Pages 上，电脑和手机浏览器均可访问（不做 App、不做小程序）。
- 数据优先保存在本机浏览器（IndexedDB），刷新、关闭、重启不丢失；通过免费云数据库（如 Supabase）实现**手动云同步**（上传/拉取）。
- GitHub Pages 只负责托管网页，不存储业务数据。

### 1.3 使用场景与角色
| 角色 | 使用场景 |
|---|---|
| 老板 | 看经营数据、管商品档案、管员工账号、审批付款、看报表 |
| 采购 | 向供应商下采购单、跟单、管理供应商 |
| 销售 | 向客户下销售单、管理客户、打印销售单/送货单 |
| 库房 | 收货验货入库、拣货发货出库、盘点、看库存 |
| 财务 | 应收应付对账、收付款登记、核算毛利、出报表 |
| 经销商（外部客户） | 老板分配账号后，登录查看产品信息和批发价 |

### 1.4 权限铁律
- 销售看不到进货价（进价）。
- 采购看不到销售毛利。
- 库房看不到任何价格（只能看到商品、数量、位置）。
- 经销商只能看自己被授权的产品和批发价，看不到进价、库存数量和其他客户。

### 1.5 角色模块权限（第八轮新增）
- 老板可在「员工管理 → 权限配置」里，**用开关给采购 / 销售 / 财务 / 库房每个角色分配可见模块**。
- 分配后该角色登录只看到勾中的模块，未勾中的路由被守卫拦截并退回该角色首页。
- 权限为**角色级**，一次修改对该角色所有员工生效；**老板恒定全部可见**，不参与配置。
- 每个角色至少保留一个模块；已下线模块自动剔除；权限存数据库，可被备份与云同步带走。

---

## 2. 整体页面框架和导航结构

### 2.1 登录页
手机号 + 密码登录，系统根据账号自动识别角色，进入对应工作台。

### 2.2 各角色底部 Tab（每个角色 3 个）
> 说明：下表为**当前已实现**的结构。各 Tab 内应展示的「真实待办数据」见第 11 节待开发清单。

| 角色 | Tab 1 | Tab 2 | Tab 3 |
|---|---|---|---|
| 老板 | 工作台（经营看板+待办） | 业务（商品中心/采购/销售/库房 入口） | 我的（财务/人事/报表/设置 入口） |
| 采购 | 工作台（应显示待跟单、待入库） | 采购单（列表+新建） | 我的 |
| 销售 | 工作台（应显示待发货、今日销售） | 销售单（列表+新建+打印） | 我的 |
| 财务 | 工作台（应显示待收款、待付款） | 对账（应收应付） | 我的 |
| 库房 | 工作台（应显示待收货、待发货） | 入库（验货） | 出库（拣货发货） |

> ⚠️ 已知问题：采购 / 销售 / 财务 三个角色的「我的」Tab 路由（`/purchase/mine`、`/sales/mine`、`/finance/mine`）
> 尚未注册，点击会跳到登录页，需修复。

### 2.3 经销商端（外部）
登录后进入「产品目录」：商品列表 + 搜索 + 产品详情（含批发价）。
> 当前状态：**页面尚未开发**。经销商账号可以创建和登录，但登录后没有落地页。

### 2.4 各角色电脑端菜单与功能定义
> 每个菜单的**完整功能、页面动作、作用、当前是否可用**见配套文件 `MENU_SPEC.md`；此处为菜单总表。

**老板端（15 项）**

| # | 菜单 | 路由 | 功能定位 | 当前状态 |
|---|---|---|---|---|
| 1 | 工作台 | `/boss/home` | 经营看板（销售额/毛利/应收/预警）+ 待办 + 快捷入口 | ✅ |
| 2 | 商品档案 | `/boss/products` | 商品主数据：新增/编辑、多级价格、库存预警值 | ✅ |
| 3 | 库存管理 | `/boss/stock` | 实时库存 + 低库存预警 + 出入库流水 | ✅ |
| 4 | 入库验货 | `/warehouse/inbound` | 按采购单验货入库（支持分批），库存增加、生成应付 | ✅ |
| 5 | 出库拣货 | `/warehouse/outbound` | 按销售单拣货出库（支持分批），库存扣减、生成应收 | ✅ |
| 6 | 库存查询 | `/warehouse/stock` | 只读库存列表（库房角色看不到价格） | 🔁 与 3 重复 |
| 7 | 采购管理 | `/purchase/orders` | 采购单列表 + 新建 | ⚠️ 缺详情页 |
| 8 | 销售管理 | `/sales/orders` | 销售单列表 + 新建 | ⚠️ 缺详情页 |
| 9 | 客户管理 | `/sales/customers` | 客户档案 + 经销商账号开关 | ✅ |
| 10 | 供应商 | `/purchase/suppliers` | 供应商档案 | ✅ |
| 11 | 财务管理 | `/finance/reconcile` | 应收应付对账 + 收付款登记 | ✅ |
| 12 | 员工管理 | `/boss/users` | 员工账号与角色权限 | ⚠️ 仅能新增 |
| 13 | 经销商 | `/boss/dealers` | 开通经销商登录账号 | 🔁 与 9 重复 |
| 14 | 经营报表 | `/boss/reports` | 经营汇总 + 低库存清单 | ⚠️ 待重做 |
| 15 | 系统设置 | `/boss/settings` | 公司信息 + 备份恢复 + 云同步 | ⚠️ 云同步为假实现 |

**其他角色**

| 角色 | 菜单 | 状态说明 |
|---|---|---|
| 采购 | 工作台 `/purchase/home` | ❌ 占位，应显示待跟单/待入库 |
| | 采购单 `/purchase/orders` | ⚠️ 缺详情页 |
| | 供应商 `/purchase/suppliers` | ✅ |
| 销售 | 工作台 `/sales/home` | ❌ 占位，应显示今日销售/待发货 |
| | 销售单 `/sales/orders` | ⚠️ 缺详情页 |
| | 客户管理 `/sales/customers` | ✅ |
| 财务 | 工作台 `/finance/home` | ❌ 占位，应显示待收/待付金额 |
| | 应收应付 `/finance/reconcile` | ✅ |
| | 毛利核算 `/finance/gross-profit` | 🔁 与 14 重复，且无筛选、无明细 |
| | 库存查询 `/warehouse/stock` | 🔁 重复 |
| | 经营报表 `/boss/reports` | ⚠️ |
| 库房 | 工作台 `/warehouse/home` | ❌ 占位，应显示待收货/待发货单数 |
| | 入库验货 `/warehouse/inbound` | ✅ |
| | 出库拣货 `/warehouse/outbound` | ✅ |
| | 库存查询 `/warehouse/stock` | 🔁 重复 |

**登录页**：手机号 + 密码，系统自动识别角色并进入对应工作台；经销商账号从客户档案中识别。

---

## 3. 页面的内容结构

### 3.1 老板工作台（首页）
- 经营看板：今日销售额、今日毛利、今日采购额、库存预警数。
- 待办列表：待审采购单、待收款、待付款。
- 快捷入口：商品档案、员工账号、报表。

### 3.2 商品档案页
- 列表：商品名称（品牌+型号自动组合）、分类、规格、库存数量、预警标记。**100 条/页**、斑马纹。
- 搜索：按品牌/型号/商品名搜索（自带清除按钮）+ 分类 / 状态筛选。
- **导入 / 导出 CSV**：导入时按「品牌+型号」自动与现有商品去重，可选「跳过 / 覆盖」策略；导出为 CSV。
- **多选批量编辑**：勾选多行后统改分类、品牌、单位、状态等字段。
- **重复检测**：列出同名商品，支持合并（库存与价格并入主记录）或删除。
- 新增/编辑商品表单（字段见第 4 节）：**只填成本价**，批发价 / 零售价按系统设置的加价比例自动带出。
- 权限：老板全权增删改；采购/财务只读（可见进价）；销售只读（不可见进价）；库房不可见价格。

### 3.3 采购单页
- 列表：采购单号、供应商、日期、状态（待入库/部分入库/已完成）、金额。
- 新建采购单：选择供应商 → **商品选择器**（顶部搜索框、显示当前库存、20 条/页、已选行蓝色高亮、「已选」橘色按钮）→ 填数量 → 自动算金额 → 提交。
- 商品明细表：**合计行在表格最下方**（合计数量 + 合计金额）。
- 详情：商品明细、状态、打印按钮。

### 3.4 销售单页
- 列表：销售单号、客户、日期、状态（待出库/部分出库/已完成）、金额。
- 新建销售单：选择客户 → **批发价 / 零售价 一键切换**（本单价格类型）→ 商品选择器（显示可用库存，无库存不可销售）→ 填数量 → 自动算金额 → 提交。
- 商品明细表：**合计行在表格最下方**（合计数量 + 合计金额）。
- 详情：商品明细、状态、打印按钮。
- 打印预览：可选「带价格 / 不带价格」两种版式 → 打印或存 PDF。

### 3.5 库房入库页
- **待收货**列表：采购单号、供应商、日期、状态、金额。
- 验货入库：逐项确认实收数量（可填实收≠应到，「本次实收」默认带出待收数量）→ 确认入库 → 库存自动增加、应付自动生成；支持**分批次部分入库**，一次收货生成一张入库单。
- **入库历史**：入库单号、来源采购单、供应商、商品、数量、金额、收货时间、经手人 → 「明细」。
- 入库单明细页：抬头 + 明细（含类别）+ 合计 + **同源单据其他批次（拆单关联，可互跳）** + 🖨 打印 / ✎ 修改（改量，按新数量重算库存与来源单状态）/ ↩ 撤回（库存原路退回）。

### 3.6 库房出库页
- **待发货**列表：销售单号、客户、商品明细。
- 拣货出库：逐项确认 → 确认出库 → 库存自动扣减、应收自动生成；支持**分批次部分出库**。
- **出库历史**：出库单号、来源销售单、客户、商品、数量、金额、发货时间、经手人 → 「明细」。
- 出库单明细页：同入库单明细，支持打印 / 修改 / 撤回（库存原路退回，来源销售单恢复待出库）。

### 3.7 财务对账页
- 应收列表（客户欠款）、应付列表（欠供应商）。
- 收付款登记：选择单据 → 登记金额 → 自动核销。
- 对账单导出（一期可先简化）。

### 3.8 报表页
- 销售统计：按日/周/月销售额、毛利。
- 库存报表：库存数量、库存金额、预警商品。
- 应收应付汇总。

### 3.9 经销商产品目录页
- 商品列表：品牌+型号、规格、批发价。
- 搜索、产品详情。
- 无任何价格以外的财务/库存信息。

---

## 4. 每个模块的用途

| 模块 | 用途 |
|---|---|
| 登录与账号 | 识别角色，按角色进入不同界面，保证数据隔离 |
| 工作台 | 各角色每天打开后第一眼看到的待办和关键数据 |
| 商品档案 | 全系统唯一产品主数据，所有单据和库存从这里取数 |
| 采购管理 | 采购员向供应商下单，跟踪到货，产生应付 |
| 销售管理 | 销售向客户下单，跟踪发货，产生应收 |
| 库房管理 | 实际执行入库、出库、盘点，维护库存准确 |
| 财务管理 | 应收应付对账、收付款、核算真实利润 |
| 报表 | 老板看经营数据，辅助决策 |
| 打印 | 采购单给供应商、销售单当送货单给客户签字 |
| 经销商端 | 经销商自助查看产品和批发价，减少业务员沟通成本 |
| 系统设置 | 商品档案管理、员工账号管理、数据同步 |

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 商品档案
**必要数据**：品牌、型号（名称=品牌+型号自动组合）、分类、规格、单位、进价（成本）、批发价、零售价、库存预警值、状态。
**必要操作**：新增、编辑、搜索、查看、**CSV 导入（自动去重）/ 导出**、**多选批量编辑**、**同名商品合并或删除**。
**定价**：系统设置维护批发加价率与零售加价率；新建/编辑只填成本，两个售价自动推算；「一键应用到全部商品」按各自成本重算全库售价。

### 5.2 采购管理
**必要数据**：采购单号（自动生成，如 CG20260919-001）、供应商、日期、商品明细（商品、类别、数量、进价、小计）、**明细表底部合计（数量 + 金额）**、总金额、状态、付款状态。
**必要操作**：新建采购单（商品选择器带搜索框与库存显示）、查看列表、查看详情、打印采购单。

### 5.3 销售管理
**必要数据**：销售单号（自动生成，如 XS20260919-001）、客户、日期、**价格类型（批发 / 零售）**、商品明细（商品、类别、数量、单价、小计）、**明细表底部合计（数量 + 金额）**、总金额、状态、收款状态。
**必要操作**：新建销售单（批发/零售切换）、查看列表、查看详情、打印销售单（带/不带价格）。

### 5.4 库房管理（库存作业）
**统一入口**：侧边栏一项「库存作业」`/warehouse`，页内六个模块 Tab 切换 ——
**入库验货、出库拣货、库存调拨、库存盘点、退换货、库房管理**。

**必要数据**：待收货单、待发货单、实收/实发数量、库存数量、出入库流水、**入库单 / 出库单（按批次）**、
**库房（可自行设定）与库房库存分布**、**调拨单**、**盘点单**、**退（换）货单**、
各类单据的**备注**（验收备注 / 拣货备注 / 退货原因）。

**必要操作**：
- 验货入库、拣货出库（均支持部分收/发，一次生成一张单据）
- 库存查询、**历史单据查看明细与打印**、**修改单据（改量并重算库存）**、**撤回单据（库存原路退回）**
- **填写 / 修改验收备注与拣货备注**（详情页与单据明细页均可编辑，打印时带出）
- **库房自行设定**：新增 / 改名 / 删除库房（名称与备注），有库存的库房须先清空才能删除，
  且至少保留一个；改名后库存分布与历史单据自动跟着新名字显示
- **入库、出库选择库房**（单据级）：整单货物进 / 出所选库房，下次作业可换库房，各记各的数量与流水；
  出库按**所选库房**的库存校验，该库不足则整单拒绝
- **库存分布可视**：库存明细按库房**动态生成列**，一行看清「某商品在各库房各多少 + 合计」，
  手机端显示「总仓 2 / 门店 1」这样的分布文本，并支持按库房筛选
- **新增盘点单**（逐项或整批带出系统库存 → 填实盘数 → 差异同时校正库房与总库存，区分盘盈 / 盘亏）
- **新增调拨单**（任意两个库房之间移动货物，来源库不足整体拒绝，总库存不变）
- **销售退货**（客户退回 → 库存回流所选库房、应收冲减）与**采购退货**（退回供应商 → 库存流出、应付冲减），
  统一在「退换货」页管理与回查

**库存一致性不变量**：**总库存 = Σ 各库房库存**（入库/出库同步改所选库房，调拨只挪库房，盘点按差异同幅校正）。

### 5.5 财务管理（财务中心 `/finance`）
**必要数据**：应收单（客户欠）、应付单（欠供应商）、收付款记录（金额、日期、经办人）、
**经营报表指标与趋势**、**「记一笔」其他收支流水**（金额、方向、分类、日期、往来单位、备注）。

**必要操作**：
- 登记收款、登记付款、查看应收应付汇总、查看毛利 —— **应收应付** Tab
- 按日期区间看销售 / 成本 / 毛利 / 毛利率 / 应收未收 / 应付未付 + 近 6 月趋势 + 库存预警 —— **经营报表** Tab
  （原独立菜单「经营报表」已归入此处）
- **记一笔**：记录**无法用采购单 / 销售单结算**的日常收支 —— **记一笔** Tab
  支出分类：送货费用、物流费用、快递费用、安装费用、辅材费用、维修费用、工资薪酬、房租水电、办公用品、招待差旅、其他费用；
  收入分类：服务收入、返利收入、利息收入、押金退回、其他收入。
- 收付款 + 记一笔合并的资金时间线 —— **资金流水** Tab

**隔离规则**：记一笔**只写 `ledgerEntries`，不写 `payments`**，
因此不产生应收应付、不参与毛利核算，只在记一笔汇总与资金流水里体现。

### 5.6 经销商端
**必要数据**：商品名称（品牌+型号）、规格、批发价。
**必要操作**：登录、搜索产品、查看产品详情。

---

## 6. 首页和每个模块之间的关系

```
登录页（识别角色）
   ↓
各角色工作台（首页）
   ├── 老板：经营看板 + 待办 → 进入报表 / 商品档案 / 员工账号
   ├── 采购：待跟单 → 进入采购单 / 供应商
   ├── 销售：待发货 → 进入销售单 / 客户
   ├── 财务：待收/付 → 进入对账收付款
   ├── 库房：待收/待发 → 进入入库 / 出库
   └── 经销商：产品目录（直接登录即达）
```

**核心业务闭环**：
1. 采购在工作台看到待跟单 → 建采购单 → 库房入库 → 自动生成应付 → 财务付款。
2. 销售在工作台看到待发货 → 建销售单 → 库房出库 → 自动生成应收 → 财务收款。
3. 老板工作台实时看到这条闭环产生的销售额、毛利、库存变化。

---

## 7. 本地数据文件、备份和恢复要求

### 7.1 本地存储
- 所有业务数据保存在浏览器 **IndexedDB** 中。
- 要求：刷新页面、关闭浏览器、重启电脑后，数据不丢失。
- 同一台电脑的同一浏览器打开，数据一直在。

### 7.2 云同步（手动，第七轮落地为 GitHub 方案）
- **存储位置**：GitHub 仓库中的一个快照文件（默认 `data/erp-snapshot.json`），
  通过 GitHub Contents API 读写，不需要自建服务器，也无需付费云数据库。
- **加密**：先 gzip 压缩，再用口令派生的密钥做 AES-GCM 加密（PBKDF2-SHA256，20 万次迭代）。
  仓库里只有密文，**没有口令无法还原**，即使仓库是 Public 也安全；口令本身不上传。
- **手动上传**：系统设置 → 云同步 → 「☁️ 同步到云端」。
  上传前比对内容指纹，与云端一致时跳过，不产生多余提交；同一文件覆盖写，不留历史版本。
- **手动拉取**：换设备后填相同配置与口令 → 「🔄 从云端恢复」。
  覆盖式恢复，操作前二次确认；写入失败会回滚到恢复前的数据。
- **配置项**：GitHub 用户名 / 仓库名 / 分支 / 快照路径 / Access Token / 同步口令。
  Token 与口令只存本机 `localStorage`，不进备份、不上传；换设备需重新填写。
- 同步为手动触发，不是实时自动同步；以最后一次上传的云端数据为准。

### 7.3 备份与恢复
- **导出备份**：支持把全部数据导出为一个 JSON 文件下载到本地（手动）。
- **导入恢复**：支持从备份 JSON 文件恢复数据。
- 备份文件命名建议：`erp-backup-YYYYMMDD.json`。
- 导出由 `db.tables` 动态生成，**新增表自动纳入**（库房 / 库位库存 / 调拨 / 盘点 / 退换货都在内）；
  导入用 `bulkPut`，带主键的备份行不会因自增 id 冲突整批失败。
- Pages 只托管网页、不提供数据库；业务数据在浏览器 IndexedDB，
  「云同步」是唯一的云端副本通道，快照也是加密后存在仓库里。

---

## 8. 第一版必须完成的功能

1. **登录与账号**：老板创建员工账号（采购/销售/财务/库房），各角色登录进入各自工作台。
2. **分角色工作台**：每个角色看到自己的待办和关键数据，不是同一张列表换名字。
3. **商品档案**：新增/编辑商品，名称=品牌+型号自动组合，多级价格，库存预警。
4. **采购管理**：采购单新建、列表、详情、打印（带/不带价格可选）。
5. **销售管理**：销售单新建、列表、详情、打印（带/不带价格可选）。
6. **库房管理**：验货入库、拣货出库、库存查询、简版盘点。
7. **库存自动联动**：入库加库存、出库扣库存，出入库自动留流水。
8. **财务管理**：应收应付自动生成、收付款登记、毛利统计。
9. **基础报表**：销售统计、库存报表、应收应付汇总。
10. **经销商端**：老板在客户管理中创建经销商账号，经销商登录后查看产品和批发价。
11. **打印模板**：采购单、销售单支持 A4 打印，带/不带价格两种版式切换。
12. **本地存储**：IndexedDB 保存，刷新/关闭/重启不丢。
13. **手动云同步**：上传/拉取按钮 + 导出/导入备份文件。
14. **部署**：GitHub Pages 部署，手机和电脑浏览器自适应访问。

---

## 9. 第一版暂时不做的功能

| 暂不做 | 原因 |
|---|---|
| 经销商在线下单、查订单、查账单 | 一期只做"看产品+批发价"，下单放二期 |
| 鞋服/家纺/家具行业扩展 | 架构已预留，一期只做家电 |
| SN 序列号（一物一码）、保修管理 | 家电高级功能，二期 |
| 多仓库、库位、批次管理 | 一期单仓，库存只到"商品+数量" |
| 多规格 SKU（颜色/尺码） | 鞋服才需要，二期 |
| Excel 完整导入导出 | 一期只做 JSON 备份导入导出 |
| 消息推送、审批流 | 一期只做待办提醒 |
| 手机安装为 App / PWA | 一期浏览器直接访问 |
| 在线支付 | 不做，线下转账后系统记账 |

---

## 10. 验收标准（可实验检查）

### 10.1 数据持久化
- [ ] 录入一条商品后，刷新页面，数据还在。
- [ ] 关闭浏览器再打开，数据还在。
- [ ] 重启电脑后打开，数据还在。

### 10.2 权限隔离
- [ ] 销售账号登录，新建销售单选商品时，看不到进价字段。
- [ ] 库房账号登录，入库/出库/库存页面，任何位置都不显示单价和金额。
- [ ] 经销商账号登录，只能看到产品和批发价，看不到进价、库存数量、其他客户信息。
- [ ] 老板账号能看到全部数据。

### 10.3 业务闭环
- [ ] 新建一张采购单 → 库房执行入库 → 库存数量自动增加 → 自动生成一条应付记录。
- [ ] 新建一张销售单 → 库房执行出库 → 库存数量自动减少 → 自动生成一条应收记录。
- [ ] 财务登记收款后，对应销售单收款状态更新。
- [ ] 采购单入库后，修改商品档案的进价，历史采购单的金额不变（价格为下单时快照）。

### 10.4 打印
- [ ] 打开销售单详情，点打印，可选择「带价格」或「不带价格」。
- [ ] 不带价格版式下，单价、金额、合计金额整列不显示。
- [ ] 打印版式为 A4，内容包含公司抬头、单号、客户/供应商、商品明细、签字栏。

### 10.5 数据同步与备份
- [ ] 在本机录入数据，点「上传到云端」，用另一台设备登录后点「从云端恢复」，数据一致。
- [ ] 点「导出备份」能下载一个 JSON 文件。
- [ ] 点「导入恢复」选择该 JSON 文件，数据完整恢复。

### 10.6 多端访问
- [ ] 电脑浏览器打开正常显示。
- [ ] 手机浏览器打开，布局自适应，关键操作按钮不小于 44px。

### 10.7 商品档案
- [ ] 新增商品时只填品牌和型号，商品名称自动显示为「品牌 型号」。
- [ ] 修改商品品牌或型号，列表和单据中的显示自动跟随（不另存名称字段）。

---

## 11. 功能现状盘点与待开发清单（2026-09-19 盘点，第二轮改造后更新）

> 逐菜单的明细（定位 / 页面动作 / 作用 / 现状）见 `MENU_SPEC.md`；本节为 PRD 层面的结论。
> **老板已拍板「按推荐来」，11.2 / 11.3 的建议已全部执行完毕。**

### 11.1 已真实可用
老板工作台、商品档案、**库存管理（统一页，含搜索）**、入库验货、出库拣货、采购单（含详情页）、销售单（含详情页）、**客户管理（含经销商）**、供应商、应收应付对账、**经营报表（含毛利，支持日期筛选与趋势图）**、**操作日志**、系统设置（公司信息 / 导出备份 / 导入恢复 / 退出登录）、**四个角色工作台（真实待办看板）**。

### 11.2 菜单功能重复 —— ✅ 已合并（2026-09-19 完成）
| 重复组 | 处理方式 | 结果 |
|---|---|---|
| `/warehouse/stock` ↔ `/boss/stock` | 合并为统一的**库存管理** `/stock` | 保留搜索 + 分类筛选 + 仅看低库存 + 预警 + 明细 + 流水 + 汇总卡；按角色自动隐藏价格。旧路径 302 重定向 |
| `/boss/dealers` ↔ `/sales/customers` | 合并为统一的**客户管理** `/customers` | 经销商只是客户的一个开关；支持搜索与「仅看经销商」筛选。旧路径 302 重定向 |
| `/finance/gross-profit` ↔ `/boss/reports` | **毛利并入经营报表** `/boss/reports` | 报表页新增日期筛选 + 快捷区间 + 6 项指标（含毛利率、应收/应付）+ 近 6 月趋势图。旧路径 302 重定向 |

菜单数量变化：老板端 15 → **14 项**（去掉 3 个重复项，新增「操作日志」）；财务端 5 → **4 项**。

### 11.3 占位无功能 —— ✅ 已改为真实数据（2026-09-19 完成）
- 采购 / 销售 / 财务 / 库房 四个角色「工作台」已改为真实待办看板：
  - 采购：待入库单数、本月采购额、供应商数、未付款金额 + 待入库单据列表（直达验货页）
  - 销售：待出库单数、本月销售额、客户数、未收款金额 + 待出库单据列表（直达拣货页）
  - 财务：应收未收、应付未付、本月已收、本月已付 + 待收/待付款单据列表（直达对账页）
  - 库房：待收货单数、待发货单数、今日入库量、今日出库量 + 待收/待发货单据列表（直达验货/拣货页）
- 老板手机端「业务」Tab：入口改为**由老板侧边栏菜单自动生成**（新增菜单自动同步，不会漏项）。
- 老板手机端「我的」Tab：新增当前登录人卡片与「操作日志」入口。

### 11.4 PRD 要求但尚未开发
| # | 功能 | 影响 |
|---|---|---|
| 1 | 经销商端产品目录页 | 经销商账号登录后无落地页，会被弹回登录页 |
| 2 | 简版盘点单 | 第 5.4 节要求，未开发 |
| 3 | 员工账号编辑 / 停用 / 改密 | 离职账号无法停用 |
| 4 | 客户、供应商停用 | 停用客户仍会出现在开单下拉 |
| 5 | ~~手机端「我的」Tab 路由（采购/销售/财务）~~ | ✅ 已修复（见 11.7） |
| 6 | 真实云同步（Supabase） | 现为假实现，仅写时间戳 |
| 7 | 客户等级 → 批发价档位 | 现只有一档批发价，等级未起作用 |
| 8 | 出库「实发 > 库存」前端拦截 | 拣货页超发未被拦截 |

### 11.5 已完成的增强项（第二轮）
- **全项目搜索框统一自带清除按钮**：新增 `src/components/SearchInput.vue`，有内容时显示「×」，点击立即清空并重新搜索，Esc 也可清空。已接入商品档案、库存管理、客户管理、操作日志。
- **采购单 / 销售单详情页**：`/purchase/orders/:id`、`/sales/orders/:id`，含单头、明细、金额、**入库/出库进度**、收付款记录、**打印**（可切换是否含单价）。
- **列表「操作」按钮修复**：此前写「新建」且跳到新建页（语义错误），已改为「查看」并跳该单详情。
- **操作日志**：新增 `src/utils/audit.ts`，关键业务动作（建单、出入库、收付款、建档等）真实写入 `auditLogs`，并新增查询页 `/boss/audit-logs`（支持搜索与按动作筛选）。
- **打印**：接上此前闲置的 `utils/printTemplate.ts`，新增 `src/utils/print.ts` 执行 A4 打印。

### 11.6 验收状态（第二轮结束时）
- 全量测试 **255 用例 / 26 个测试文件 全部通过**（无跳过、无降级断言）。
- `npm run build`（含 `vue-tsc` 类型检查）通过；`npm run dev` 启动后 HTTP 200 可访问。
- 已用真实浏览器（agent-browser）登录老板账号逐页实机验证（见 11.7）。

### 11.7 「点开菜单页面空白/功能无效」事故修复（2026-09-19）
**现象**：老板账号点开采购管理 / 销售管理 / 供应商 / 财务管理，页面闪一下跳回工作台；
入库验货、出库拣货、客户、库存、财务等页面显示空白。

**根因 A —— 菜单与路由权限不匹配**：老板侧边栏 4 项指向仅授权其他角色的路由
（`/purchase/orders`、`/sales/orders`、`/purchase/suppliers`、`/finance/reconcile`），
被路由守卫按 `meta.role` 弹回工作台。
→ **修复**：老板为最高权限角色，所有业务路由 `meta.role` 均补入 `'boss'`，
其他角色的越权拦截保持不变。

**根因 B —— 新装数据库为空**：IndexedDB 全新时各业务表为 0 条，
所有列表均显示「暂无数据」，此前无任何示例数据或引导，易被判断为「功能没开发」。
→ **修复**：新增 `src/utils/demoData.ts` 一键示例数据（6 商品 / 3 供应商 / 3 客户含 1 个经销商账号 /
3 张采购单 / 3 张销售单，并连带产生库存、流水、应收应付、操作日志），
在**系统设置 → 示例数据**提供载入与清空；老板工作台在空数据时显示「开始使用」引导卡片。

**根因 C —— 手机端「我的」路由未注册**：`/purchase/mine`、`/sales/mine`、`/finance/mine`
命中通配规则被重定向到 `/login`，表现为点击即退出登录。
→ **修复**：新增共用页 `src/views/RoleMineView.vue` 并注册三条路由，
入口直接取自该角色菜单，避免两处维护不同步。

**回归防线**：新增 `tests/nav-permission.test.ts`（14 用例，修复前失败 8 项）——
遍历每个角色的每个菜单项，断言路由已注册且登录后能真正停留；
新增 `tests/demo-data.test.ts`（12 用例）——断言生成示例数据后
各页面均不再是空白、库存与流水一致、经销商账号可登录、清空后账号保留。

### 11.8 查询 / 历史留痕 / 二级页面按钮 / 打印规范（2026-09-19 第三轮）

本节为**需求层面的硬性规则**，后续新增任何模块都必须遵守。

**规则 1 · 查询输入框**
采购、入库、销售、出库、财务管理五个模块的列表页**必须**提供查询输入框，
且一律使用统一组件 `SearchInput`（自带清除按钮 ×，按 Esc 也可清空）。
查询范围与附加筛选见下表：

| 模块 | 查询范围 | 附加筛选 |
|---|---|---|
| 采购管理 | 单号 / 供应商 | 单据状态、日期区间、一键重置 |
| 销售管理 | 单号 / 客户 | 单据状态、日期区间、一键重置 |
| 入库 | 单号 / 供应商 / 商品 | 日期区间、一键重置（在「入库历史」页签） |
| 出库 | 单号 / 客户 / 商品 | 日期区间、一键重置（在「出库历史」页签） |
| 财务 | 单号 / 往来单位 | 结清状态（未结清 / 已结清 / 全部）、收付款类型、日期区间 |

**规则 2 · 历史留痕（本轮核心）**
任何一次信息流转都必须在数据库留下不可省略的明细痕迹，
各部门在自己模块内即可查到本部门的历史，不依赖其他部门页面：

| 痕迹 | 产生时机 | 可见位置 |
|---|---|---|
| 入库流水（purchase_in） | 库房确认入库 | 入库页「入库历史」、入库验货页「本单入库记录」、采购单详情「入库流水」、库存管理流水 |
| 出库流水（sale_out） | 库房确认出库 | 出库页「出库历史」、出库拣货页「本单出库记录」、销售单详情「出库流水」、库存管理流水 |
| 收付款流水（receive / pay） | 财务登记收付款 | 财务「收付款流水」页签、采购/销售单详情收付款记录 |
| 操作日志（auditLogs） | 建单 / 出入库 / 收付款 / 建档 / 登录 | 操作日志页 |

**每一条痕迹都必须可追溯到**：时间、关联单据号、往来单位、商品、数量、金额、操作人。
财务的收付款流水与应收应付**必须保留已结清记录**（默认视图看未结清，
可切换到「已结清 / 全部」查历史），不得因为结清就从系统中消失。

**规则 3 · 二级页面按钮**
每个二级页面按其业务性质提供操作按钮，统一使用 `PageActions` 组件：

- **详情类页面**（采购单详情、销售单详情、入库验货、出库拣货）→ 提供「返回」；
- **表单类页面**（新建采购单、新建销售单、新增/编辑商品）→ 提供「取消」；
- 有提交动作的页面同时提供主操作按钮；提交中须禁用并显示「处理中…」防重复提交；
- 点「取消」不得产生任何数据副作用（不建单、不写流水）。

**规则 4 · 打印**
- 打印必须**先预览、后打印**：点「打印」弹出预览弹窗，弹窗内完整呈现单据排版；
- 预览弹窗底部必须同时提供「打印」与「取消」两个按钮；
- 排版规范：公司抬头 → 单据名称 → 单号/日期/制单 → 往来单位信息 → 明细表
  （序号/商品名称/型号/单位/数量/单价/金额）→ 合计 → 金额大写框 → 备注 → 签章栏；
- 纸张按 A4，明细行不得跨页被切断，表头跨页重复；
- 提供「打印含单价」开关；**无价格查看权限的角色（库房）不得看到该开关**；
- 金额统一为千分位 + 两位小数（如 `27,600.00`）。

**新增回归防线**：`tests/page-actions.test.ts`（10）、
`tests/history-search.test.ts`（10）、`tests/print-preview.test.ts`（8）。

### 11.9 验收状态（第三轮）

- 全量测试 **283 用例 / 29 个测试文件 全部通过**（无跳过、无降级断言）。
- `npm run build`（含 `vue-tsc` 类型检查）通过；`npm run dev` 启动后 HTTP 200 可访问。
- 真实浏览器验证：入库历史 4 条、出库历史 4 条、财务收付款流水 4 笔均可正常展示；
  采购单搜索过滤生效；打印预览弹窗排版正常且含「取消 / 打印」按钮。

---

## 附录：数据库表结构详细设计（20 张表）

> 说明：以下为逻辑设计，实际实现时可根据所用数据库（IndexedDB / Supabase）调整字段类型，但字段含义和关系必须保持。

### A1. users（员工账号表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| name | 姓名 |
| phone | 手机号（登录名） |
| password | 密码（加密存储） |
| role | 角色：boss / purchaser / sales / finance / warehouse |
| status | 启用 / 停用 |
| created_at | 创建时间 |

### A2. customers（客户/经销商表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| name | 客户名称 |
| contact | 联系人 |
| phone | 联系电话 |
| address | 地址 |
| level | 客户等级（决定批发价档位） |
| credit_limit | 授信额度 |
| payment_term | 账期 |
| login_phone | 经销商登录手机号（空=无登录权限） |
| login_password | 经销商登录密码（加密） |
| status | 启用 / 停用 |
| remark | 备注 |

### A3. suppliers（供应商表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| name | 供应商名称 |
| contact | 联系人 |
| phone | 联系电话 |
| address | 地址 |
| payment_term | 账期 |
| remark | 备注 |

### A4. products（商品档案表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| brand | 品牌 |
| model | 型号 |
| category | 分类（空调/冰箱/洗衣机/电视…） |
| spec | 规格参数（选填） |
| unit | 单位（台/件/套） |
| purchase_price | 进价 |
| wholesale_price | 批发价 |
| retail_price | 零售价 |
| warn_stock | 库存预警值 |
| status | 在售 / 停售 |
| remark | 备注 |
| extra | 扩展属性（JSON，预留以后行业） |

> 商品名称 = brand + model 实时组合，不单独存字段。

### A5. stock（库存表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| product_id | 商品 id |
| quantity | 当前数量 |
| updated_at | 更新时间 |

### A6. purchase_orders（采购单主表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| order_no | 单号（CG+日期+序号） |
| supplier_id | 供应商 id |
| purchaser_id | 采购员 id |
| order_date | 日期 |
| status | 待入库 / 部分入库 / 已完成 |
| total_amount | 总金额 |
| pay_status | 未付 / 部分付 / 已付 |
| remark | 备注 |

### A7. purchase_order_items（采购单明细表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| purchase_order_id | 采购单 id |
| product_id | 商品 id |
| quantity | 数量 |
| price | 下单时进价快照 |
| subtotal | 小计 |

### A8. sale_orders（销售单主表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| order_no | 单号（XS+日期+序号） |
| customer_id | 客户 id |
| sales_id | 销售员 id |
| order_date | 日期 |
| status | 待出库 / 部分出库 / 已完成 |
| total_amount | 总金额 |
| receive_status | 未收 / 部分收 / 已收 |
| remark | 备注 |

### A9. sale_order_items（销售单明细表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| sale_order_id | 销售单 id |
| product_id | 商品 id |
| quantity | 数量 |
| price | 下单时批发价快照 |
| subtotal | 小计 |

### A10. stock_records（出入库流水表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| type | 类型：purchase_in / sale_out / adjust（盘点校正）/ transfer（调拨）/ sale_return（销售退货）/ purchase_return（采购退货） |
| ref_order_id | 关联单据 id（出入库单 / 调拨单 / 盘点单 / 退货单） |
| product_id | 商品 id |
| quantity | 数量（入库正数，出库负数） |
| operator_id | 操作人 id |
| created_at | 时间 |
| location_id | 发生库位 id（默认 1=总仓） |
| batch_no | 所属单据号（RK / CK / DB / PD / TH 开头） |
| remark | 备注（验收备注 / 拣货备注 / 退货原因 / 盘点单号） |

### A11. payments（收付款记录表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| type | 类型：receive（收）/ pay（付）/ refund（销售退货红冲）/ supplier_credit（采购退货红冲） |
| ref_order_id | 关联销售单或采购单 id |
| counterparty_id | 往来单位 id（客户 / 供应商） |
| amount | 金额 |
| pay_date | 日期 |
| operator_id | 经办人 id |
| remark | 备注（如「销售退货 TH20260919-123」） |

### A12. audit_logs（操作日志表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| operator_id | 操作人 id |
| action | 动作（新增/修改/删除/入库/出库…） |
| detail | 内容 |
| created_at | 时间 |

### A13. locations（库位表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识（1=总仓，2=门店，初始化时写入） |
| name | 库位名称 |

### A14. location_stock（库位库存分布表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| product_id | 商品 id |
| location_id | 库位 id |
| quantity | 该库位数量 |

> 一致性约束：`Σ location_stock.quantity（按商品）= stock.quantity`。

### A15. transfer_orders（调拨单主表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| order_no | 调拨单号（DB20260919-123） |
| from_loc / to_loc | 调出 / 调入库位 id |
| date | 调拨时间 |
| operator_id | 操作人 id |
| status | 状态（done） |

### A16. transfer_items（调拨单明细表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| transfer_order_id | 调拨单 id |
| product_id | 商品 id |
| quantity | 调拨数量 |

### A17. stocktakes（盘点单主表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| order_no | 盘点单号（PD20260919-123） |
| location_id | 盘点库位 id |
| date | 盘点时间 |
| operator_id | 操作人 id |
| status | 状态（done） |

### A18. stocktake_items（盘点单明细表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| stocktake_id | 盘点单 id |
| product_id | 商品 id |
| system_qty | 盘点时该库位的系统库存 |
| actual_qty | 实盘数量（差异 = actual − system，同步校正库位与总库存） |

### A19. return_orders（退换货单主表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| order_no | 退货单号（TH20260919-123） |
| kind | 类型：sale（销售退货）/ purchase（采购退货） |
| ref_order_id / ref_order_no | 来源销售单 / 采购单 id 与单号 |
| party_id | 往来单位 id（客户 / 供应商） |
| date | 退货时间 |
| operator_id | 操作人 id |
| status | 状态（done） |
| total_amount | 退货金额（财务红冲依据） |
| remark | 退货说明 |

### A20. return_items（退换货单明细表）
| 字段 | 说明 |
|---|---|
| id | 唯一标识 |
| return_order_id | 退货单 id |
| product_id | 商品 id |
| quantity | 退货数量（不得超过原单数量） |
| price | 退货单价（默认取原单成交价） |
| amount | 金额 = 数量 × 单价 |
| reason | 退货原因（质量问题 / 尺码不符 / 发错货…） |

---

## 12. 打印规范（第五轮定稿）

### 12.1 纸张
| 选项 | 尺寸 | 用途 |
|---|---|---|
| **A5 横版**（默认） | 210 × 148 mm = A4 纸的一半 | 日常送货单 / 出库单 |
| **A4 竖版** | 210 × 297 mm | 明细较多、需要完整归档 |

（A3 不在范围内。）

### 12.2 分页规则
- 明细行数超过一页容量时自动分页；容量默认 A5 = 6 行 / A4 = 14 行，可由用户覆盖；
- **每一页都重复表头**与「单号 · 日期 · 制单 · 第 X / Y 页」；
- **合计与签章只出现在最后一页**；行数守恒，不丢行、不重复；
- 首页预留往来单位区、末页预留合计/签章区，余两页时均分，避免只 1 行的尾页。

### 12.3 表头与字段（用户可自行编辑）
- 表头：公司抬头、副标题、公司地址、公司电话、页脚文字、签章栏开关、每页行数；
- 明细字段：序号、商品名称、**类别**、型号、单位、数量、单价、金额 ——
  可勾选显隐、修改列名、上下调整顺序；「合计」行自动对齐数值列；
- 明细必须显示**产品类别**（洗衣机 / 冰箱 / 空调…），便于客户识别商品；
- 不打单价时自动剔除「单价 / 金额」两列；库房角色不可见价格开关；
- 入口：单据详情页「打印 → ⚙ 表头设置」，以及 系统设置 →「打印设置」；
- 设置持久化于本机，损坏或缺少字段时自动回退/补齐。

### 12.4 版式
公司抬头 → 单据名 → 单号·日期·制单·页码 → 往来单位信息 → 明细表 → 合计 → 备注 → 签章栏 → 页脚。
合计直接落在明细表底部合计行（不再另起「数量 / 金额 / 大写」方框）。
内容自上而下紧凑排列，**不在表格与签章/页脚之间留大片空白**。

---

## 13. 库存作业合并与调拨 / 盘点 / 退换货（第六轮，2026-09-19）

### 13.1 需求与实现对照

| # | 需求 | 实现 |
|---|---|---|
| 1 | 验收单增加备注框，记录有效信息 | 入库验货详情页新增**验收备注**输入框；出库拣货详情页新增**拣货备注**；备注随单据留存，单据明细页可再次编辑保存，打印时带出 |
| 2 | 入库验货「本单收货」操作列「明细 / 撤回」太近易误触 | 两按钮包入 `.op-cell`（`inline-flex; gap:16px`），实机测得计算样式 `gap=16px` |
| 3 | 出库拣货「本单发货」操作列「明细 / 撤销」太近易误触 | 同上，且按钮文案按需求定为「**撤销**」 |
| 4 | 入库验货、出库拣货统一归入「库存作业」，并新增调拨、盘点 | 侧边栏两项合并为一项「库存作业」`/warehouse`；页内 Tab：**入库验货 / 出库拣货 / 库存调拨 / 库存盘点**（后续扩展为 5 个 Tab，见 13.3） |
| 5 | 补充退换货模块，覆盖销售退货与采购退货，统一管理单据 | 新增「**退换货**」Tab `/warehouse/returns`：销售退货 / 采购退货 / 退换货历史 |

### 13.2 库存一致性规则

> **总库存（stock）= Σ 各库位（locationStock）**

| 动作 | 总库存 | 库位分布 | 财务影响 |
|---|---|---|---|
| 采购入库 | + N | 总仓 + N | 生成应付 |
| 销售出库 | − N | 总仓 − N | 生成应收 |
| 调拨 | 不变 | 调出 − N、调入 + N | — |
| 盘点 | ± 差异 | 该库位 ± 同一差异 | — |
| 销售退货 | + N | 总仓 + N | 应收冲减（refund） |
| 采购退货 | − N | 总仓 − N | 应付冲减（supplier_credit） |

### 13.3 退换货业务规则

- **销售退货**：选原销售单 → 每行退货数量**不得超过原单数量**；库存回流总仓；生成退货单与 `sale_return` 流水；
  财务记 `refund`，应收余额 = 原单金额 − 已收 − 退货金额。
- **采购退货**：选原采购单 → 退货数量不得超过原单数量，且**在手库存必须足够**（实物要退得出）；
  库存流出总仓；生成退货单与 `purchase_return` 流水；财务记 `supplier_credit`，
  应付余额 = 原单金额 − 已付 − 退货金额。
- **单据管理**：退货单号 `TH` 开头；历史列表汇总两类单据，可展开查看商品 / 数量 / 单价 / 金额 / 原因。

### 13.4 验收标准与验证结果

| 检查项 | 结果 |
|---|---|
| `npx vitest run` 全量测试 | ✅ **337 通过 / 33 个文件**（新增 `returns.test.ts` 5 项、`warehouse-ops.test.ts` 7 项） |
| `npm run build`（含 `vue-tsc -b`） | ✅ **0 错误** |
| 库存作业页 5 个 Tab 切换 | ✅ 实机（老板账号 + 示例数据） |
| 验收备注框可见、可输入、随单留存、可再编辑 | ✅ 实机 + 单测 |
| 操作列按钮间距 | ✅ 实机计算样式 `gap=16px`（入库「明细 / 撤回」、出库「明细 / 撤销」） |
| 调拨页（总仓 → 门店，含可用库存与低库存标记） | ✅ 实机 |
| 盘点页（含「加入全部商品」批量带出） | ✅ 实机 |
| 退换货页（销售退货 / 采购退货 / 历史） | ✅ 实机 + 单测 |
| 数据库版本迁移 | ✅ `dexie v4 → v5`（新增 8 张表，老库自动升级、默认库位自动补齐） |

---

## 14. 多库房与云同步（第七轮，2026-09-19）

### 14.1 需求对照

| # | 需求 | 落地情况 |
|---|---|---|
| 1 | 参照参考图中的云同步设置，部署到 GitHub Pages | ✅ 六项配置界面 + GitHub Contents API 加密同步 + `npm run deploy` |
| 2 | 库房可自行设定 | ✅ 库存作业 → 🏬 库房管理：新增 / 改名 / 删除（有货拦截） |
| 3 | 入库、出库要有库房选择 | ✅ 两个详情页顶部「入库库房 / 出库库房」下拉 |
| 4 | 库存明细要体现商品存放在哪个库 | ✅ 表格按库房动态生成列 + 合计列；手机端显示分布文本 |
| 5 | 一条库存数量有多个，可分存不同库房 | ✅ `locationStock` 按 (商品, 库房) 独立记数，一键「加入全部商品」可拆量 |
| 6 | 进而可进行不同库房的调拨 | ✅ 调拨的调出 / 调入下拉动态读取库房表，新增库房立即可选 |

### 14.2 库房模型

```
locations        库房（id / name / remark / createdAt），默认 总仓(1) / 门店(2)
locationStock    库房库存分布（productId + locationId → quantity）
stock            总库存（每商品一行）
不变量：         Σ locationStock[商品] === stock[商品]
```

| 动作 | 总库存 | 库房分布 |
|---|---|---|
| 采购入库 | + N | 所选库房 + N |
| 销售出库 | − N | 所选库房 − N（不足则拒单） |
| 调拨 A → B | 不变 | A − N、B + N |
| 盘点某库 | ± 差异 | 该库房 ± 同一差异 |
| 销售退货 | + N | 所选库房 + N |
| 采购退货 | − N | 所选库房 − N（不足以退则拒单） |

**老数据自愈**：只写过总库存、没有分布的历史数据，在出入库 / 查看分布时
`reconcileProduct` 会把差额补到默认库房，保证不变量成立（出库校验才有意义）。

### 14.3 云同步与部署

| 项目 | 方案 |
|---|---|
| 存储 | GitHub 仓库单文件快照（默认 `data/erp-snapshot.json`），Contents API 读写 |
| 加密 | PBKDF2-SHA256(200k) 派生密钥 → AES-GCM-256；先 gzip 后加密 |
| 去重 | 比对明文 SHA-256 指纹，无变化则跳过上传 |
| 覆盖策略 | 每次覆盖同一文件，不堆积历史版本 |
| 恢复 | 覆盖式；操作前二次确认；失败回滚 |
| 部署 | `npm run deploy <仓库地址> [分支]`：构建 → 推 `dist` 到 `gh-pages` 孤儿分支 |
| 适配 | `vite base: './'`、`public/.nojekyll`、`public/404.html` |
| 指导文档 | `docs/GITHUB_PAGES.md`（建仓库 / Token / Pages / 同步 / 排错） |

### 14.4 验收标准与验证结果

| 检查项 | 结果 |
|---|---|
| `npx vitest run` 全量测试 | ✅ **356 通过 / 35 个文件**（新增 `cloud-sync.test.ts` 12 项、`warehouse-locations.test.ts` 7 项） |
| `npm run build`（含 `vue-tsc -b`） | ✅ **0 错误** |
| 库存作业 6 个 Tab（含库房管理） | ✅ 实机 |
| 新增库房「北京仓」 | ✅ 实机：列表即时出现，库存件数 / 商品种数为 0 |
| 调拨下拉动态包含新库房 | ✅ 实机：调出 / 调入均出现「总仓 / 门店 / 北京仓」 |
| 库存明细多库房列 | ✅ 实机：表头 `总仓 | 门店 | 北京仓 | 合计库存`；海尔 KFR-35GW = 2 / 2 / 0 / 4 |
| 入库页「入库库房」下拉 | ✅ 实机 `总仓 / 门店 / 北京仓` |
| 出库页「出库库房」下拉 + 可用库存按库房显示 | ✅ 实机 `总仓 / 门店 / 北京仓`，明细列标题随所选库房变化 |
| 云同步界面（数据空间 / 上次同步 / 连接状态 + 六项配置） | ✅ 实机：`3 条 / 从未同步 / 未配置`；Token 与口令为密码框 |
| 未配置就点上传的提示 | ✅ 实机吐司「请先填写并保存同步设置」 |
| 加密往返 / 错口令 / 指纹去重 / 配置持久化 | ✅ 单测覆盖 |

---

## 15. 第八轮：角色权限开关、记一笔与界面精细化

### 15.1 需求与实现对照

| # | 需求 | 实现 | 状态 |
|---|---|---|---|
| 1 | 员工管理里用开关给每个角色分配模块，各角色登录界面不同 | `stores/permission.ts`（`rolePerms` 表，Dexie v6）+ 员工管理「🔐 权限配置」Tab；侧边栏 / 手机导航 / 路由守卫三处同时过滤 | ✅ |
| 2 | 经营报表归入财务管理，并新增「记一笔」 | 财务中心 `/finance` 四 Tab；`ledgerEntries` 表 + `addLedger / listLedger / deleteLedger / ledgerSummary` | ✅ |
| 3 | 全系统排版与卡片精细化 | `src/styles/theme.css` 设计系统 + `src/components/ui/*` 六大通用组件；17 个页面收敛重复样式 | ✅ |

### 15.2 角色权限模型

```
角色 role ──► rolePerms.modules[] ──► ALL_MODULES（17 个模块，5 组）
                  │
                  ├─ 未配置  → 回退 DEFAULT_ROLE_PERMS
                  ├─ boss    → 恒定全部可见，拒绝配置
                  └─ 已配置  → 过滤非法路由，至少保留 1 个
```

生效点必须**三处同时过滤**，否则会出现「菜单没了但手工输网址还能进」：
`SideBar`（电脑端侧边栏）、`MobileTabBar`（手机底部导航）、`router.beforeEach`（路由守卫）。

### 15.3 「记一笔」与其他收支

这类费用**没有商品、不走库存、不生成应收应付**，无法用采购单 / 销售单结算：

| 方向 | 分类 |
|---|---|
| 支出 | 送货费用 · 物流费用 · 快递费用 · 安装费用 · 辅材费用 · 维修费用 · 工资薪酬 · 房租水电 · 办公用品 · 招待差旅 · 其他费用 |
| 收入 | 服务收入 · 返利收入 · 利息收入 · 押金退回 · 其他收入 |

数据落在 `ledgerEntries`，与 `payments` **完全隔离**，不污染应收应付与毛利。

### 15.4 界面设计系统

- **令牌层**：品牌色 / 状态色 / 中性色 / 圆角 / 阴影 / 间距统一为 CSS 变量。
- **组件层**：`PageHeader`（页头）、`SectionCard`（卡片）、`StatCard`（统计卡）、
  `EmptyState`（空状态）、`ToggleSwitch`（开关）、`SegmentedTabs`（分段控件）。
- **原生控件基线**用 `:where()` 包裹，**特异性为 0**，Vant 等组件库自带样式仍然优先，不会互相打架。
- **遗留类名收敛**：`.block` / `.sec-title` / `.tabs` 等早期类名统一到设计系统。
- **✅ 已由用户验收确认采用**，自第九轮起作为全站强制基线，新页面必须套用（详见第 16 节）。

### 15.5 验收结果

| 项目 | 结果 |
|---|---|
| 全量测试 | ✅ **372 通过 / 37 文件**（新增 `permission.test.ts` 8 项、`ledger.test.ts` 8 项） |
| `npm run build` | ✅ **0 错误**（`vue-tsc -b`） |
| 权限矩阵 | ✅ 17 个开关按 5 组展示，含全选 / 按角色推荐 / 清空 / 保存 / 恢复默认 |
| 权限生效 | ✅ 关掉「库房工作台」保存后提示「该角色共 2 个模块」；库房账号登录**侧边栏只剩 2 项** |
| 记一笔 | ✅ 生成 `JY20260919-612`，汇总「本期支出 ¥120.00 / 净额 ¥-120.00」，流水行显示分类与往来单位 |
| 财务中心 | ✅ 四 Tab（应收应付 / 经营报表 / 记一笔 / 资金流水），经营报表 6 项指标 + 趋势图正常 |

---

## 16. 第九轮：设计系统正式采用（规范基线）

用户在上面 15.4 的截图验收后回复「采用这套风格」，自此这套视觉规范从「试行」转为**项目硬性基线**。

### 16.1 新页面必须遵守

| 事项 | 要求 |
| --- | --- |
| 页面容器 | 根元素加 `ui-page`（宽度 1240px 居中），不要自写 `max-width` |
| 页头 | 一律用 `<PageHeader title sub />`；带状态标签时用 `badge` + `badge-tone` |
| 卡片 | 用 `<SectionCard>`；历史遗留的 `.block` 已全局收敛，效果等同 |
| 数字概览 | 用 `<StatCard>`，`tone` 取 `success / danger / warning / neutral` |
| 空数据 | 用 `<EmptyState icon text hint>`，不要自写「暂无数据」 |
| Tab 切换 | 用 `<SegmentedTabs>`（旧 `class="tabs"` 已全部替换） |
| 表单控件 | 直接用原生 `input / select / textarea`，全局基线已接管；**禁止写裸 `input {}` 覆盖** |
| 表格 | 统一 `class="data-table"`（斑马纹 / 悬停 / 合计行已内置） |

### 16.2 两条容易踩的约束

1. **原生控件基线必须留在 `:where()` 里**。`:where()` 特异性为 0，Vant 自带样式仍然优先；
   一旦改成裸选择器（`input { … }`）就会污染 Vant 组件，弹窗与表单会串样。
2. **页头副标题要避开业务敏感词**。「进价」「库存金额」等词会被权限测试断言命中
   （销售不得见进价、库房不得见任何价格），文案要写中性说法（如「价格」「库存分布」）。

### 16.3 本轮补充

- **嵌套页头自动降级**：财务中心 / 库存作业把子页整个嵌进 Tab 面板，
  子页自带 PageHeader 会与父页头叠成两层大标题。
  已加全局规则 `.tab-pane .ui-page-head`，面板内页头自动降为紧凑小节标题（19px → 15px ＋ 虚线分隔）。
- **`PageHeader` 支持状态徽标**：新增 `badge` / `badge-tone` 属性，
  单据详情页（如「入库单 · RK20260919-101 收货入库」）不再需要自写 `<span class="tag">`。
- **补齐 4 个未套用页头的基本页**：新增商品 / 编辑商品、新建采购单、新建销售单、入库单（出库单）明细。
- 清理了这一批替换后残留的 `.pe-title` / `.ct` 局部样式。

### 16.4 验收结果

| 项目 | 结果 |
|---|---|
| 全量测试 | ✅ **372 通过 / 37 文件** |
| `npm run build` | ✅ **0 错误**（`vue-tsc -b`） |
| 嵌套页头 | ✅ 财务中心实测 `heads=2`：父「财务管理」19px、子「经营报表」15px ＋ 虚线分隔 |
| 基本页页头 | ✅ 新增商品 / 新建销售单 / 入库单明细均正确渲染，状态徽标为 `ui-badge badge-success` |

---

## 17. V1.0-1 发布基线（2026-09-19）

### 17.1 版本定义

| 项 | 值 |
|---|---|
| **产品版本** | **V1.0-1** |
| 版本来源 | `src/version.ts` → `APP_VERSION = 'V1.0-1'`；`package.json` → `"version": "1.0.0-1"` |
| 版本含义 | V<主版本>.<次版本>-<修订号>；V1.0-1 即 1.0 的第 1 个修订版 |
| 发布日期 | 2026-09-19 |
| 版本展示 | 系统设置页头徽标（灰色 `V1.0-1`），用户可随时核对当前版本 |
| 发版步骤 | 改 `src/version.ts` + `package.json` 两处 → 更新 PRD 头部「产品版本」行 → 提交并打 tag |

### 17.2 功能总览（按模块）

| 模块 | 路由 | 状态 | 说明 |
|---|---|---|---|
| 工作台 | `/boss/home` | ✅ | 本月销售 / 毛利 / 应收 / 预警 + 待办 + 快捷操作 |
| 商品档案 | `/boss/products` | ✅ | 搜索、分类、批量导入导出、去重、条码 |
| 采购管理 | `/purchase/*` | ✅ | 开单、审核、分次入库、供应商、应付 |
| 销售管理 | `/sales/*` | ✅ | 开单、审核、分次出库、客户、应收 |
| **库存作业** | `/warehouse` | ✅ | 六合一：入库验货 / 出库拣货 / 调拨 / 盘点 / 退换货 / 库房管理 |
| **多库房** | — | ✅ | 库房自定义、出入库选库房、库存明细按库分列、库房间调拨 |
| **财务中心** | `/finance` | ✅ | 应收应付 / 经营报表 / **记一笔** / 资金流水 |
| 库存明细 | `/stock` | ✅ | 动态库房列 + 合计列 + 库房筛选 |
| 员工与权限 | `/boss/users` | ✅ | 员工 CRUD + 6 角色 × 17 模块开关矩阵 |
| 系统设置 | `/boss/settings` | ✅ | 公司信息、打印设置、备份恢复、**GitHub 云同步** |
| 操作日志 | `/boss/audit-logs` | ✅ | 全站关键操作留痕 |

### 17.3 交付清单

**代码**
- 20 张 Dexie 表（Dexie v6），老库自动升级
- 11 个 Pinia store：`user / product / purchase / sales / finance / inventory / returns / permission / stockDoc / menuOrder / sync / dashboard`
- 设计系统 `src/styles/theme.css` + 6 个通用组件 `components/ui/`
- 云同步 `src/utils/cloudSync.ts`（AES-GCM + gzip + 指纹去重）
- 打印 `src/utils/print*`（A5 横版 / A4 竖版、自动分页、字段自定义）

**文档**
- `PRD.md`（本文档，V1.6 修订）、`MENU_SPEC.md`（逐菜单明细）、`DEV_PLAN_V2.md`（执行计划）
- `docs/GITHUB_PAGES.md`（部署与云同步配置全流程）

**测试**
- 37 个测试文件 / **372 条用例**，覆盖：权限矩阵、记一笔、多库房一致性、云同步加解密、
  打印分页、出入库批次、退换货、菜单结构等

### 17.4 仓库与线上地址

| 项 | 内容 |
|---|---|
| 源码仓库 | `bailihongxi/wholesale-erp`（主分支 `main`，公开） |
| 在线访问 | `https://bailihongxi.github.io/wholesale-erp/` |
| 部署方式 | `npm run deploy` → 构建 `dist` 并推送到 `gh-pages` 分支 |
| 数据存放 | 浏览器 IndexedDB，不上传服务器；云同步由用户自备 GitHub 仓库与 Token |

### 17.5 验收结果

| 项目 | 结果 |
|---|---|
| 全量测试 | ✅ **372 通过 / 37 文件 / 0 error**（`npx vitest run`） |
| `npm run build` | ✅ **0 错误**（`vue-tsc -b` + vite build） |
| 构建产物 | ✅ `dist/` 含 `.nojekyll`、`404.html`，`base: './'` 适配子目录 |
| 线上访问 | ✅ `https://bailihongxi.github.io/wholesale-erp/` HTTP 200，登录与模块跳转正常 |
| 版本展示 | ✅ 线上系统设置页头徽标显示 **V1.0-1** |
| 权限生效 | ✅ 库房账号登录后侧边栏仅剩已授权模块 |
| 记一笔 | ✅ 生成 `JY…` 流水，只写 `ledgerEntries`，不污染应收应付 |
| 多库房 | ✅ 库存明细按库房动态分列，调拨下拉动态读取 |
| 云同步 | ✅ 加密信封往返、错误口令、指纹去重均通过单测 |

**Git 基线**

| 项 | 内容 |
|---|---|
| 首次提交 | `324420c feat: 家电批发进销存 ERP V1.0-1 发布基线`（162 个文件） |
| 版本标签 | `v1.0-1` |
| 主分支 | `main`（源码） |
| 页面分支 | `gh-pages`（仅构建产物，孤儿分支，每次覆盖推送） |

### 17.6 已知边界（非缺陷）

1. **无服务端**：数据保存在本机浏览器，换设备需在系统设置手动「同步到云端 / 从云端恢复」。
2. **云同步为覆盖式**：以最后一次上传为准，不做多端冲突合并。
3. **云同步需自行配置**：用户名 / 仓库名 / Token / 同步口令，详见 `docs/GITHUB_PAGES.md`。
4. **权限只管模块可见性**：字段级权限（销售不见进价、库房不见任何价格）按既有铁律固定，不随开关变化。

---

## 18. 第十一轮：库存作业界面重做与手机端布局修复（2026-09-19）

### 18.1 需求（用户截图反馈）

> 库存作业页面排版太老旧古板，与整体系统的类型不协调，而且页面排布头部与显示部分错位了，需要立即更改。

### 18.2 根因（三条，均为实际缺陷）

| # | 现象 | 根因 |
|---|---|---|
| 1 | 页内子 Tab 呈「灰框老式胶囊」，与财务中心等页不一致 | 子页写成 `<div class="ui-seg"><button :class="{active}">`，按钮**漏了 `.ui-seg-item` 类**，命中不到设计系统样式，退回浏览器默认按钮外观（2px 灰边框 + 灰底） |
| 2 | 「头部 → Tab → 表格」左边缘逐层缩进 | `InboundView` 私有容器写了 `max-width: 1100px; margin: 0 auto`，父级是 1240px，宽屏下子页比父级窄一档并各自居中 |
| 3 | **手机端顶栏被压成左侧窄竖条，内容挤到右边只剩一小块** | `.app-layout` 是 `display: flex` 且未设 `flex-direction`（默认 `row`）。电脑端 `SideBar + 主列` 横排正确；手机端 `顶栏 + 内容 + 底部 Tab` 也被横排，顶栏只剩自身内容宽度（实测 134px），底部 Tab 因 `position: fixed` 逃出流式布局。**所有手机端页面都受影响** |

### 18.3 改动

**A. 分段控件升级（`src/styles/theme.css`，全站生效）**

- 视觉从「深蓝实心块一排按钮」改为**现代分段控件**：容器退为浅色凹槽（`--c-surface-alt` + inset 阴影），**选中项浮为白色卡片**（白底 + 主色文字 + 细描边 + 轻投影），整体更像导航而非按钮阵列。
- 新增 `.ui-seg.md`（页面级，高 36px）与 `.ui-seg.sm`（Hub 页内嵌子页级，高 30px）两档尺寸，主次分明。
- 新增 `.ui-seg-badge`（待办角标）：红底白字圆形计数，用于「入库验货 ②」「出库拣货 ②」。
- 手机端 `.ui-seg` 横向滑动（`overflow-x: auto` + 隐藏滚动条），模块多时不挤成两行。

**B. 统一组件（`src/components/ui/SegmentedTabs.vue`）**

`options` = `{ key, label, icon?, badge? }`，`size` = `'md' | 'sm'`，`v-model` 绑定选中 key。
全站 6 处手写 Tab 统一改用它：库存作业的 5 个子页（入库验货 / 出库拣货 / 库存调拨 / 库存盘点 / 退换货）+ 财务中心对账页。
`.ui-seg > button:not(.ui-seg-item)` 兜底规则保留，即使将来漏写类名也不会崩样式。

**C. 库存作业 Hub 页（`WarehouseOpsView.vue`）**

- 容器改为 `ui-page`，与全站版式（1240px 居中）一致。
- 顶部模块导航升级为 `.ui-seg.md` + 图标 + 待办角标（实时统计待收货 / 待发货单数）。
- 子页在 `.tab-pane` 内渲染，页头自动降级（沿用第九轮规则）。

**D. 布局缺陷修复（`AppLayout.vue`）**

```css
.app-layout.is-mobile { flex-direction: column; }   /* 顶栏 / 内容 / 底部 Tab 纵向排列 */
```

**E. 对齐兜底（`theme.css`）**

```css
.tab-pane > div.ui-page, .tab-pane > div.page { max-width: none; margin: 0; }
```
用 `div` 提高特异性，确保能盖过子页 scoped 样式里的 `max-width`，任何子页都不会再比父级窄一档。

**F. 下拉控件统一**

入库详情 / 出库详情 / 调拨 / 盘点 / 退换货的库房与单据下拉原来自带私有外观（`height:44px + 自绘边框`），**覆盖掉了全局 `.ui-select` 的箭头**，看起来像纯文本。
现改为挂 `.ui-select` 类，私有样式只保留宽度约束；实测计算样式统一为 `h=40px / border=1px / radius=8px / 自带下拉箭头`。

**G. 空态观感**

21 处 `.empty` 依赖测试断言引用，不做结构改动，改为用统一 CSS 规则提升观感（居中 + 图标 + 留白）。

### 18.4 验收结果

| 项目 | 结果 |
|---|---|
| 对齐（1440 视口） | ✅ 标题 / 模块 Tab / 页内 Tab / 表格 全部 `x=244`（同一左边缘） |
| 对齐（1280 视口） | ✅ 全部 `x=244` |
| 手机端（390 × 844） | ✅ 顶栏 `x=0 w=390` 通栏；内容 `x=0 w=390`；子页 `x=12 w=366` |
| 子 Tab 观感 | ✅ 与财务中心、员工管理一致（浅槽 + 白卡片浮起） |
| 库房下拉 | ✅ `class="ui-select loc-sel"`，`h=40 / 1px / r=8 / 有箭头` |
| 全量测试 | ✅ **372 通过 / 37 文件 / 0 error** |
| `npm run build` | ✅ **0 错误** |

截图归档：`docs/screenshots/round11-*.png`（7 张）。

### 18.5 后续开发约束（新增）

1. **页面内的 Tab 一律用 `SegmentedTabs` 组件**，不要手写 `<div class="ui-seg"><button>` —— 漏写 `.ui-seg-item` 会退回浏览器默认按钮外观。
2. **Hub 页（库存作业 / 财务中心）内嵌的子页不要写 `max-width`** 或自己 `margin: 0 auto`；宽度由父级统一控制。
3. **`.app-layout` 的 `is-mobile` 纵向排列不可删** —— 删掉后手机端所有页面顶栏都会变成左侧竖条。
4. 新增下拉控件请挂 `.ui-select`，私有样式只写宽度 / 外边距，不要覆盖边框与背景（会丢掉下拉箭头）。

---

## 19. 第十二轮：库存合并 / 加载性能 / 电脑端筛选条 / 品牌自定义（2026-09-19）

> 本节对应产品版本 **V1.0-2**。第 17 节为 V1.0-1 历史基线，版本号以本文头部为准。

### 19.1 需求（用户反馈 4 条）

| # | 原话要点 | 归类 |
|---|---|---|
| 1 | 库存作业菜单并入库存管理；库存预警每页显示 50 条且用斑马纹 | 信息架构 + 列表规范 |
| 2 | 很多页面会「闪一下」空白/空态再出数据，加载也偏慢 | 性能与加载体验 |
| 3 | 电脑端很多页面的搜索栏与关联下拉、按钮排布太散，要紧凑：搜索栏可放大，其余控件收缩，收进 1～2 行；**只改电脑版，不动手机版** | 电脑端排版 |
| 4 | 网页版快捷图标、登录页头像、各角色头像都可以自己改 | 品牌可配置 |
| 5 | 库房管理「新增库房」那一行同样要紧凑（截图红框） | 电脑端排版（补） |

### 19.2 库存作业并入库存管理

- `/stock` 升级为 **Hub 页**：`PageHeader` + `SegmentedTabs`（库存明细 / 库存预警 / 出入库流水 / 库存作业）+ `.tab-pane`。
- 侧边栏菜单**不再单列「库存作业」**：老板菜单 12 → **11 项**；库房角色默认权限改为 `[/warehouse/home, /stock]`；手机端 Tab 改为「工作台 / 库存管理」。
- **`/warehouse` 及其全部子路由保留**（收藏夹、外部链接不失效），仅从菜单入口收起；`WarehouseOpsView` 新增 `embedded` 属性，内嵌时不重复渲染页头、Tab 收小一档。
- 数据在 Hub 页**统一加载一次后传给子模块**，切 Tab 不再各自查库（顺带消除切 Tab 的闪动）。

### 19.3 库存预警独立成模块

- 固定 `PAGE_SIZE_ALERT = 50`（新增常量，与商品列表的 20 条区分），带分页条。
- 表格沿用全站 `.data-table`（自带**斑马纹**：奇偶行 `#ffffff` / `#fbfcfe`），加 `.alert-table` 与 `.is-warn` 左侧红条。
- 工具栏：搜索（品牌 / 型号 / 分类）+ 分类下拉 + 库房下拉 + 重置；支持「缺口」列（预警值 − 当前库存，至少 1）。
- 手机端沿用卡片列表，同样带斑马纹。

### 19.4 加载性能

| 问题 | 根因 | 处理 |
|---|---|---|
| 先闪空态再出数据 | 数据未到就渲染了空态 | 新增 `components/ui/LoadingBlock.vue` 骨架屏，7 个列表首页接入，`onMounted` 完成后关闭 |
| 首屏白屏 | 路由 chunk 现用现拉 | `main.ts` 加 `prefetchRoutes()`：`load` 后用 `requestIdleCallback` 空闲预取懒加载分包 |
| 启动白屏 | `#app` 挂载前无内容 | `index.html` 加 `#app-boot` 品牌占位（logo + 呼吸条） |
| 商品上千时列表慢 | `getLowStockProducts` / `reconcileProducts` 逐个商品查库（N+1） | 改为 `Promise.all` 一次取全表 + `Map` 内存比对，只对不一致的记录落库 |

### 19.5 电脑端筛选条紧凑化（仅电脑端）

```css
@media (min-width: 768px) {
  :is(.toolbar, .data-toolbar, .ui-toolbar) { display:flex; flex-wrap:wrap; gap:10px; }
  :is(.toolbar, .data-toolbar, .ui-toolbar) > :is(input, select, button, label)
    { flex:0 0 auto; width:auto; min-width:0; max-width:230px; }
  :is(.toolbar, .data-toolbar, .ui-toolbar) > :is(.search-wrap, .grow)
    { flex:1 1 320px; width:auto; min-width:220px; max-width:none; }   /* 搜索框放大 */
  :is(.toolbar, .data-toolbar, .ui-toolbar) > input[type='date'] { max-width:158px; }
  .ui-card-extra { flex:1 1 auto; justify-content:flex-end; }
}
```

- **根因**：`theme.css` 里 `:where(input, select)` 基线给了 `width:100%`，flex 子项 basis 被拉到整行宽 → 每个控件各占一条。
- 控件高度统一 **40px**、圆角 `--r-sm(8px)`、描边 `--c-border-strong`；日期框收到 158px。
- 已落地页面：采购单、销售单（两个 toolbar 合并为一个，日期区间并入）、商品档案、对账、操作日志、客户、入库、出库、商品选择器、**库房管理「新增库房」行**。
- **手机端（< 768px）完全不变**，已用 390 视口实测复核（`navW=390 / contentX=0 / contentW=390`）。

### 19.6 品牌与图标可自定义

- 新增 `src/utils/brand.ts`（配置模型 + `useBrand()`）与 `src/components/BrandSettingsPanel.vue`（挂在系统设置页）。
- 可改项：登录页标志 / 登录标题 / 登录副标题 / 侧边栏标志 / **6 个角色头像** / **各模块快捷图标**。
- 图标支持 **emoji（或 1～2 个字）** 与 **上传图片**（`utils/image.ts` 自动压成 96px 方图 dataURL）。
- 存 `localStorage['erp_brand_config']`，随整库备份与云同步一起走；**任一字段缺省都回落到 navConfig 默认 emoji**，升级不会把图标变空；配置损坏时静默回落，不让页面打不开。
- 接入点：`SideBar`、`MobileTabBar`、`AppLayout` 顶栏头像、`LoginView`、`BossBusinessView`、`RoleMineView`、`SettingsView`。

### 19.7 验收结果

| 项目 | 结果 |
|---|---|
| 全量测试 | ✅ **392 通过 / 38 文件 / 0 error** |
| `npm run build` | ✅ **0 错误**（`vue-tsc -b` + vite build，2.94s） |
| 新增测试文件 | ✅ `tests/stock-hub.test.ts`（19 例：预警 50 条 / 斑马纹 / Hub 四 Tab / 骨架 / 筛选条媒体查询 / 品牌配置） |
| 库存预警实机 | ✅ 载入示例数据后：5 行预警、斑马纹交替、分页「共5条 · ‹ 1 ›」 |
| 电脑端筛选条实机 | ✅ 采购单 / 销售单 / 客户 / 审计 / 库存明细 / 商品档案 / 财务 均收进 1～2 行 |
| 手机端复核 | ✅ 390 视口布局未变 |

### 19.8 后续开发约束（新增）

1. **新增列表页一律用 `.data-table` + `TablePager`**，需要特殊页容量时用 `PAGE_SIZE_ALERT` 这类常量，不要在组件里写死数字。
2. **首屏有异步数据的页面必须挂 `LoadingBlock`**，不要直接渲染空态（会闪）。
3. **筛选条不要给子项写死宽度**，交给 `@media (min-width:768px)` 的紧凑规则统一收口；只有搜索框可以用 `.grow` / `.search-wrap` 放大。
4. **涉及菜单或路由增删必须跑 `tests/nav-permission.test.ts`**，并同步 `MENU_SPEC.md`。
5. 新增可配置项请走 `utils/brand.ts` 的「缺省回落」模式，**不允许把图标 emoji 硬编码进组件模板**。
6. **可折叠模块一律用 `ui/CollapseCard.vue`**：用 CSS 折叠（DOM 保留），**不要用 `v-if`**——`v-if` 会让已有测试拿不到节点，也会把用户填了一半的表单清掉。

---

## 20. 第十三轮：开票与对公账户信息 / 系统设置折叠（2026-09-20）

> 本节对应产品版本 **V1.0-3**。第 19 节为 V1.0-2 历史基线，版本号以本文头部为准。

### 20.1 需求（用户反馈 2 条）

| # | 原话要点 | 归类 |
|---|---|---|
| 1 | 客户资料及供应商信息维护时，需要增加**开票信息等公户信息**，现有编辑**无法保存**其开票信息等公户信息，还要地址、备注 | 基础资料字段扩展 + 缺陷修复 |
| 2 | 系统设置页面中的各个小模块设置成可以进行**回弹收缩**，以保持页面的清洁识别性 | 界面信息密度 |

### 20.2 客户 / 供应商：开票与对公账户资料

**新增字段（客户与供应商共用同一套模型）** —— 见 `src/utils/invoice.ts` 的 `INVOICE_FIELDS`：

| 字段 | 键名 | 说明 |
|---|---|---|
| 开票抬头 | `invoiceTitle` | 发票上的单位全称，可与客户简称不同 |
| 纳税人识别号 | `taxNo` | 统一社会信用代码 / 税号 |
| 开票地址 | `invoiceAddress` | 发票地址栏 |
| 开票电话 | `invoicePhone` | 发票电话栏 |
| 开户银行 | `bankName` | 对公账户开户行 |
| 对公账号 | `bankAccount` | 对公银行账号 |

- **数据模型**：`types/index.ts` 的 `Customer` / `Supplier` 新增 6 个可选字段（`?`），**老数据不迁移也能读**——
  缺省一律按空串回落，不会因为没这六个字段而报错或让页面打不开。
- **工具方法**（`utils/invoice.ts`）：`emptyInvoice()` / `hasInvoice()` / `invoiceComplete()` /
  `invoiceOf()` / `invoiceSummary()`。判定口径：**抬头 + 税号都填了才算「完整」（可直接开票）**，
  只填了抬头算「已填但未完整」。
- **公共表单组件**：`components/InvoiceFieldset.vue`（三列网格，六个输入框），客户与供应商共用。
- **客户表单**重组为三组：**基础资料 → 开票与打款资料 → 备注 / 经销商账号**。
- **供应商页**除开票资料外，**补齐了「备注」字段**，并新增**搜索框**（按名称 / 联系人 / 电话 / 抬头 / 税号）。
- **列表可见性**：
  - 客户列表新增「开票资料」列，显示绿「完整」/ 灰「未填」徽标；
  - 供应商列表显示「开票资料完整」徽标 + 开票摘要行（抬头 · 税号 · 开户行账号）+ 地址行 + 备注行。
- **搜索**：客户与供应商的搜索框均支持**按开票抬头或税号命中**（报销 / 开票场景常用税号反查）。
- **示例数据**：`utils/demoData.ts` 的 3 个客户与 3 个供应商均补齐完整的开票与对公资料，便于直接看效果。

> **缺陷修复（用户明确反馈）**：原 `CustomersView` 保存时把 `remark` 写死为空串、`address` 等字段未参与回填，
> 导致「编辑一次就丢备注」。本轮改为 **`openEdit()` 完整回填 + `save()` 完整回写**，
> 并由 `tests/invoice-info.test.ts` 用「改税号 + 改备注 → 保存 → 重读断言」锁死。

### 20.3 系统设置：各模块可折叠收缩

- 新增 `src/components/ui/CollapseCard.vue`：

| 行为 | 实现 |
|---|---|
| 点标题展开 / 收起 | `v-model` 受控组件，标题栏 `click` + `keyup.enter` / `keyup.space` |
| 回弹动效 | 箭头 `transform` 用 `cubic-bezier(.34,1.56,.64,1)`，本体 `max-height + opacity` |
| **收起不销毁** | 用 CSS `max-height:0` 折叠而非 `v-if`，**DOM 保留**，表单填了一半的内容不丢 |
| 无障碍 | `role="button"` + `tabindex="0"` + `aria-expanded` + `:focus-visible` 描边 |
| 减少动效 | `@media (prefers-reduced-motion: reduce)` 关闭过渡 |

- 设置页 **8 个模块**全部改为折叠卡片：公司信息 / 品牌与图标 / 数据备份与恢复 / 示例数据 /
  价格规则（加价比例）/ 打印设置 / 库房管理 / ☁️ 云同步（GitHub）。「退出登录」保持为独立区块不折叠。
- **默认只展开「公司信息」**，其余收成一行标题。
- 页头右侧新增 **「全部展开 / 全部收起」** 一键操作；`BrandSettingsPanel` 的折叠状态由设置页统一托管
  （`v-model:open`），所以一键操作能一起动。
- 展开状态存 `localStorage['erp_settings_panels']`，**下次进入保持原样**；存储损坏 / 隐私模式写不进去时静默回落默认值。

### 20.4 验收结果

| 项目 | 结果 |
|---|---|
| 全量测试 | ✅ **419 通过 / 40 文件 / 0 error** |
| `npm run build` | ✅ **0 错误**（`vue-tsc -b` + vite build，2.68s） |
| 新增测试文件 | ✅ `tests/invoice-info.test.ts`（11 例）、`tests/settings-collapse.test.ts`（12 例） |
| 开票资料实机 | ✅ 客户列表显示「完整」徽标；编辑表单三组布局、六字段回填正确；按税号可搜到 |
| 设置页实机 | ✅ 默认 8 个模块仅「公司信息」展开；模块高度 **4118px（全展开）→ 810px（默认）→ 588px（全收起）** |
| 布局回归 | ✅ 手机端未受影响 |

### 20.5 本轮暴露并修掉的组件缺陷（重要）

`CollapseCard.vue` 初版模板里引用的是 `open`，而 prop 名是 `modelValue`，
Vue 不报错（模板里未定义属性静默取 `undefined`），结果是**所有模块永远渲染成收起**、点不开。
由 `tests/settings-collapse.test.ts` 第 1 例抓出，已改为 `computed(() => props.modelValue)` + 独立 `toggle()`。

> **教训**：模板里引用到不存在的属性时 Vue 只是 `[Vue warn]`，不会中断构建。
> 这类「看起来能跑但行为全错」的组件，**必须靠挂载测试断言真实渲染结果**才能发现。

### 20.6 后续开发约束（新增）

1. **让用户填的表单，保存时必须完整回写全部字段**（含备注），编辑时必须完整回填；改一个字段不许把别的字段清空。
2. 表单字段超过 6 个时应**分组**（如「基础资料 / 开票与打款资料 / 备注」），不要平铺一大片。
3. **开票相关判定统一走 `utils/invoice.ts`**，不要在页面里各写一套「是否填过」的判断。
4. **可折叠模块一律用 `ui/CollapseCard.vue`**，用 CSS 折叠不用 `v-if`（见 19.8 第 6 条）。
5. 页面级折叠状态（多个同类模块）由**页面统一持有**并配「全部展开 / 全部收起」，子组件用 `v-model:open` 接入。

---

---

## 21. 第十五轮：全站列表统一 20 条/页 + 斑马纹（V1.0-5，2026-09-20）

### 21.1 需求

用户原话：「将系统中所有列表的页面都按照 20 条每页来进行分页显示，并且进行斑马纹区别。
目前所能看到的系统中商品档案中的列表，采购管理中的采购单列表，销售管理中的销售单列表，
财务管理中的首付款列表，操作日志列表，员工管理，和客户管理，供应商列表，都按照 20 条每页分页 斑马纹处理。」

确认结论（用户选择）：**全站统一 20 条/页，含此前单独指定的商品档案（100）与库存预警（50）；斑马纹要「看得出」。**

### 21.2 验收标准（可实验检查）

| # | 检查项 | 合格标准 |
|---|---|---|
| 1 | 分页粒度 | 任意列表每页最多 20 行；`PAGE_SIZE_LIST = PAGE_SIZE_PRODUCT = PAGE_SIZE_ALERT = 20` |
| 2 | 分页条 | 每张列表底部有「共 N 条 · 第 X / Y 页」并可翻页、可跳页 |
| 3 | 表格斑马纹 | 奇偶行底色不同：奇数 `#ffffff` / 偶数 `#eef2f9`（肉眼可辨） |
| 4 | 卡片斑马纹 | 手机端卡片列表同样错行变色（同 `#eef2f9`） |
| 5 | 汇总不受影响 | 资金流水 / 应收应付等的「合计、余额」按**全部数据**汇总，不随翻页变化 |
| 6 | 边界 | 翻到末页行数正确；序号连续（第 2 页从 21 开始）；「默认库房」徽标不会被翻页后的首行误标 |

### 21.3 覆盖清单（12 类列表）

商品档案、采购单、销售单、客户管理、供应商、财务（应收应付 / 收付款流水）、资金流水、记一笔、
操作日志、员工管理、库房列表、库存出入库流水。

### 21.4 实现要点与踩坑

1. **粒度常量统一**：`composables/usePagination.ts` 三个常量全部 20；旧断言（100 / 50）按新规则**校准**而非删除。
2. **私有样式会盖掉斑马纹**：`AuditLogView`、`LocationsView` 原本写了 `.data-table { border-collapse: collapse }`，
   会把设计系统的圆角/底纹挤掉 —— 表格外观一律交回全局 `.data-table`，页面只留字号。
3. **卡片列表补 `zebra-list`**：单据、应收应付、收付款、库房作业的手机端卡片原先没有斑马纹；
   且 `zebra-list` 原用 `--c-surface-alt(#fafbfd)` 太淡，**改为与表格同档 `#eef2f9`**。
4. **序号与「默认」标记**：库房列表第 2 页首行曾被 `i === 0` 误标为「默认」，改为 `i === 0 && page === 1`；
   流水/库房的序号改用 `pager.startIndex + i` 保持连续。
5. **取数上限**：出入库流水原 `limit(50)` 会让分页只有 3 页，改为 `FLOW_FETCH_LIMIT`，分页才有意义。

---

## 22. 第十六轮：应用图标自定义 + 发送到桌面（V1.0-5，2026-09-20）

### 22.1 需求

用户原话：「给系统增加可以更改 icon 和发送到桌面的快捷方式图标的功能。」

确认结论（用户选择）：**完整方案 —— 自定义图标 + PWA 安装到桌面 + 离线可用。**

### 22.2 能做什么

| 能力 | 说明 |
|---|---|
| 改应用图标 | 支持 **emoji / 1~6 个字 / 上传图片**三种来源，6 种底色（品牌蓝渐变 / 深蓝 / 亮蓝 / 墨黑 / 橘红 / 翡翠绿 / 纯白） |
| 立即生效 | 保存即替换浏览器**标签页图标、收藏夹图标、苹果主屏图标**，并重建 manifest，无需刷新 |
| 发送到桌面 | 系统设置里一键调起浏览器原生安装；不支持时按平台给出**文字步骤**（苹果 / 安卓 / 电脑 / 微信内） |
| 桌面图标一致 | 安装后桌面 / 主屏幕快捷方式用的就是改过的那张图（manifest 图标同步替换） |
| 断网可用 | 装过后没网也能打开（Service Worker 缓存应用外壳；数据本来就在本机 IndexedDB） |
| 可回退 | 「恢复默认图标」一键回到内置品牌图 |

### 22.3 验收标准（可实验检查）

| # | 检查项 | 合格标准 |
|---|---|---|
| 1 | 标签页图标 | 改成 🏪 后 `<link rel="icon">` 的 href 变为 `data:image/png;base64,…` |
| 2 | 苹果主屏图标 | `<link rel="apple-touch-icon">` 同步替换 |
| 3 | manifest | `<link rel="manifest">` 变为 blob，图标换成新画的 PNG（另保留一张 maskable 兜底） |
| 4 | 恢复默认 | 三个 link 全部回到 `public/icons/*.png` |
| 5 | 安装状态 | 支持时显示「可一键安装」并弹出原生安装窗；否则显示「需手动添加」+ 分平台步骤 |
| 6 | 断网 | 断网后刷新仍能打开登录页（Service Worker 缓存命中） |
| 7 | 重启保持 | 刷新页面后图标仍是用户设置的那张（启动即套用已保存设置） |

### 22.4 实现要点

| 文件 | 职责 |
|---|---|
| `src/utils/appIcon.ts` | canvas 把图标画成 32 / 180 / 192 / 512 四张 PNG；替换 favicon / apple-touch-icon；重建 manifest |
| `src/utils/installApp.ts` | 捕获 `beforeinstallprompt`；平台判定与分平台安装步骤；生产环境注册 Service Worker |
| `src/components/AppIconPanel.vue` | 设置页面板：预览（含标签页效果模拟）、来源切换、底色、应用到系统、恢复默认、发送到桌面 |
| `public/manifest.webmanifest` | PWA 清单（standalone、192/512/maskable 图标、快捷入口） |
| `public/sw.js` | Service Worker：导航 network-first（发新版不会卡旧版）、静态资源 stale-while-revalidate |
| `public/icons/*.png` | 默认图标（`scripts/gen-icons.py` 纯标准库生成，不依赖 Pillow） |
| `src/utils/brand.ts` | 新增 `appIcon` / `appIconBg` 配置项（localStorage，随备份与云同步走） |

### 22.5 踩坑记录

1. **组件里不能用页面的 scoped 类**：新建的 `AppIconPanel` 一开始用了 `.primary-btn / .ghost-btn / .btn-row`，
   而这三个类只定义在 `SettingsView.vue` 的 scoped 样式里 → 独立组件里**完全没有样式**，
   按钮文字挤在一起（截图可见）。改用全局设计系统类 `ui-btn / ui-btn-primary / ui-toolbar / ui-hint` 后正常。
   > 结论：**跨组件复用只能依赖 `theme.css` 的全局类**；页面 scoped 类名不要外借。
2. **图标要「启动即套用」**：只在设置页里替换 favicon 的话，用户刷新后图标会掉回默认，看起来像没保存。
   已在 `main.ts` 挂载后统一套用一次。
3. **manifest 里必须绝对化 URL**：动态 manifest 是 Blob URL，相对路径会解析失败，
   `start_url / scope / shortcuts` 全部用 `new URL(p, location.href).href` 展开。
4. **无 canvas 要优雅降级**：拿不到 2d 上下文时返回空串/false，保持原图标，绝不把图标弄成空白。
5. **Service Worker 只在生产注册**：开发时 vite 的 HMR 与缓存会互相打架。
6. **jsdom 没有 canvas**：`getContext` 会把 "Not implemented" 当错误抛给虚拟控制台，
   干扰「页面渲染无报错」类断言；已在 `tests/setup.ts` 统一桩成 `null`（同时覆盖降级分支）。

---

## 23. 第十七轮：库存概况卡片下移 + 默认打开库存作业（V1.0-6，2026-09-20）

### 23.1 需求

用户原话：

> 库存管理页面头部的将商品 SKU，库存总量，库存金额（进价），库存预警 这几个卡片移动到库存明细模块内，
> 位置放置到中的搜索框上面。
> 第二，库存管理->将 库存作业 作为默认打开页面

拆成两件事：
① **概览卡片归位** —— 四张卡从 Hub 页头搬进「库存明细」，落在搜索框**上方**；
② **默认 Tab 改为「库存作业」** —— 打开「库存管理」先看到日常收发货入口。

### 23.2 验收标准（可实验检查）

| # | 检查项 | 合格标准 |
|---|---|---|
| 1 | 卡片位置 | 进入「库存明细」，四张卡出现在**搜索框上方**（DOM 顺序：`ui-stat-grid` → `toolbar`） |
| 2 | 页头干净 | 不带 `tab` 参数进 `/stock` 时页面上**没有** `.stat-card`（卡片不再占页头） |
| 3 | 默认 Tab | 不带参数进 `/stock`，默认渲染**库存作业**（`WarehouseOpsView`），不渲染库存明细 |
| 4 | 明细 Tab 直达 | `?tab=detail` 落到库存明细，卡片与表格都在 |
| 5 | 数值口径 | 四张卡仍按**全量商品**统计（SKU 数 / 库存总量 / 按进价汇总的金额 / 低于预警线的条数），不随页内搜索变化 |
| 6 | 权限 | 「库存金额（进价）」卡按**进价**汇总 → 用 `canSeePurchasePrice` 判定：**销售、库房都看不到**（页面文本不含「进价 / 库存金额 / ¥」） |
| 7 | 手机端 | 四张卡 2×2 排列，仍在搜索框上方 |
| 8 | 二次进入 | 已在本页时从别处再点「库存管理」（URL 无 `tab`）→ Tab 回到库存作业 |

### 23.3 实现要点

| 文件 | 改动 |
|---|---|
| `src/views/stock/StockManageView.vue` | 删掉页头 `.stat-row` 与 `stats` / `money` / 权限判断；`normalize()` 兜底值 `detail` → **`ops`**；新增 `watch(() => route.query.tab, …)` |
| `src/views/stock/StockDetailView.vue` | 顶部新增 `.ui-stat-grid` + 四张 `ui/StatCard.vue`（在 `.toolbar` 之前）；自行按 props 计算 `stats` |

1. **卡片跟着子模块走**：统计口径本来就来自 `products / stock` 两个 props，与明细同源，
   放在 `StockDetailView` 里由它自己算，父页不再重复持有 `stats`（少一份重复计算）。
2. **金额卡改用进价权限**：卡是「按进价汇总」的，原先用 `canSeeAnyPrice` 判定，**销售也能看到**；
   改为 `canSeePurchasePrice`（老板 / 采购 / 财务）后与明细表的「进价」列权限一致。
3. **默认 Tab 变更会动到老用例**：原来有几组用例（明细搜索、汇总一致性、库房价格权限、示例数据）
   默认依赖「进 `库存管理` 就是库存明细」，现已显式带上 `?tab=detail` ——
   **是产品行为变更导致的断言过期，按新规则校准而非放宽**；同时新增「默认落在库存作业」等断言。
4. **同路径再次进入不重建组件**：Tab 只在 `setup` 时从 `route.query` 取一次，
   若用户已在 `/stock?tab=detail`，再点侧边栏「库存管理」（同路径无 query）组件不会重建、Tab 不会复位；
   补 `watch` 让 URL 始终是唯一事实源。

验收状态：**473 用例 / 43 文件全部通过**（新增 `tests/stock-cards-v17.test.ts` 8 例），`npm run build` 0 错误。

---

## 24. 第十八轮：登录系统重新设计（V1.0-7，2026-09-20）

### 24.1 需求

用户原话（截图登录页暴露明文账号密码）：

> 现在需要更新及合理重新设计登录系统，目前我只接能在界面上看见老板的账号和密码，而且账号不能只使用电话，员工信息也必须在拓展更新一部分。

拆成两件事：
① **登录安全加固** —— 登录页不再显示任何账号密码；密码由明文改为加盐哈希；连续 5 次失败锁定 5 分钟；登录态 7 天过期、活跃使用滑动续期。
② **账号体系扩展** —— 支持「用户名 / 手机号」双通道登录；员工档案扩展（工号、登录名、部门、职位、入职日期、备注、头像）；提供自助改密与老板重置密码。

### 24.2 验收标准

| # | 检查项 | 合格标准 |
|---|---|---|
| 1 | 登录页无凭证 | 页面文本不含 `13800000000` / `admin123`（内置账号仅写进《使用说明》） |
| 2 | 双通道登录 | 用用户名 `admin` 或手机号 `13800000000` 均可登录老板账号 |
| 3 | 密码哈希 | 库中 `users.password` 为 `pbkdf2$…` 格式，F12 无法直接读取明文 |
| 4 | 失败锁定 | 同一账号连错 5 次后锁定 5 分钟，提示剩余时间 |
| 5 | 会话过期 | 会话 7 天有效、活跃使用滑动续期；过期后回登录页并提示 |
| 6 | 员工档案 | 员工管理可填工号/登录名/部门/职位/入职日期/备注/头像，列表按工号排序、可按部门/状态筛选 |
| 7 | 改密与重置 | 员工「我的 → 修改密码」验旧改新；老板可重置员工密码并拿到一次性临时密码 |

### 24.3 实现要点

| 文件 | 改动 |
|---|---|
| `src/utils/password.ts`（新增） | PBKDF2-SHA256 加盐哈希：`hashPassword` / `verifyPassword`（老明文可校验并标 `needsUpgrade`）/ `generateTempPassword` |
| `src/utils/loginGuard.ts`（新增） | 连续失败锁定（localStorage）、会话读写（带 `kind` 防员工/经销商 id 串号）、7 天滑动续期、记住账号 |
| `src/utils/account.ts`（新增） | 登录名/手机号规则与部门/职位建议值 |
| `src/types/index.ts` | `User` 增加 `username/employeeNo/dept/position/joinDate/remark/avatar/lastLoginAt`；`usernameOf` / `displayNoOf` 兜底 |
| `src/db/index.ts` | v7：`users` 加 `username/employeeNo` 索引并迁移老数据；`initDefaultAdmin` 存哈希密码+档案字段；`nextEmployeeNo` |
| `src/stores/user.ts` | `login` 双通道+锁定+哈希+静默升级；`createUser/updateUser/resetPassword/changePassword/usesDefaultPassword` |
| `src/views/auth/LoginView.vue` | 账号输入框（用户名或手机号）、显示/隐藏密码、记住账号；移除明文凭证 |
| `src/views/boss/UsersManageView.vue` | 部门/状态筛选、三组编辑表单（账号/任职/补充）、重置密码临时密码弹窗 |
| `src/components/ChangePasswordModal.vue`（新增） | 自助改密弹窗 |
| `src/views/boss/BossHomeView.vue` / `BossMineView.vue` / `RoleMineView.vue` | 内置密码提醒条、自助改密入口、退出登录 |

### 24.4 验证

全量 **481 通过 / 44 文件全绿**，`npm run build` 0 错误。
新增 `tests/auth-v18.test.ts`（哈希、锁定、会话、双通道登录、重置密码）。
实机（老板账号）：登录页无任何账号密码；用 `admin` 与 `13800000000` 均可登录；7 天后需重登。

### 24.5 交付

| 项 | 结果 |
|---|---|
| 版本 | `src/version.ts` → **V1.0-7**；`package.json` → `1.0.0-7` |
| 文档 | `PRD.md` V2.2（第 24 节）、本文件第二十三章、`MENU_SPEC.md` 第十八轮标注 同步 |
| 仓库 | 提交（含 V1.0-6 未提交改动） + tag `v1.0-7` 推送 `main`；GitHub Pages 重新部署 |

**文档结束。** 开发过程中如有变更，请修改本文档并同步确认。


---

## 25. 第十九轮：打印模板头部 + 记一笔关联单据 + 阻塞弹窗（V1.0-8，2026-09-20）

### 25.1 需求

> ① 所有打印模板头部由两行改为一行：公司抬头（家电批发）与单据标题同行、抬头居左，标题保持居中，抬头字号不变；
> ② 「记一笔」新增关联单号：费用可关联到入库单 / 出库单 / 退换货单 / 采购单 / 销售单 / 调拨单 / 盘点单，便于追踪费用来源单据；
> ③ 全站弹出窗口改为阻塞式：点击遮罩不再关闭（避免误触丢失编辑内容），按 ESC 键可关闭。

### 25.2 行为定义

**① 打印模板头部（`src/utils/printTemplate.ts`）**
- 头部第一行 = 三列 grid：左列公司抬头（`hd-co`，字号不变、居左）、中列单据标题（`hd-title`，绝对居中）；
- 副标题 / 公司地址电话仍显示在下方，纸张与分页规则不变；
- 采购单、销售单、入库单、出库单共用一个模板，全部生效。

**② 记一笔关联单据（`src/views/finance/LedgerView.vue` 等）**
- 表单新增「关联单据（选填）」：先选类型（不关联 / 入库单 / 出库单 / 退换货单 / 采购单 / 销售单 / 调拨单 / 盘点单），再选或输入单号（datalist 列出该类型现有单号，支持搜索）；
- 记录新增字段 `linkDocType` / `linkDocNo`，保存后流水列表新增「关联单据」列，资金流水（财务 → 资金流水）摘要同步显示；
- 不选时为空字符串，老数据不受影响。

**③ 阻塞弹窗**
- 弹窗（打印预览、修改密码、员工编辑/重置结果、商品导入/批量编辑/重复检查）点击遮罩不再关闭；
- 打开状态下按 ESC 关闭；保存 / 导入进行中按 ESC 不响应，避免关掉正在提交的任务；
- 弹窗卸载后键盘监听一并移除。

### 25.3 涉及文件

| 文件 | 改动 |
|---|---|
| `src/utils/printTemplate.ts` | 头部 `.hd-row` 三列 grid；`hd-co` 居左、`hd-title` 居中 |
| `src/utils/ledger.ts` | 新增 `LEDGER_LINK_TYPES` / `linkTypeLabel()` |
| `src/types/index.ts` | `LedgerEntry` 增 `linkDocType` / `linkDocNo` |
| `src/stores/finance.ts` | `addLedger` 支持并落库关联字段，日志带关联单号 |
| `src/views/finance/LedgerView.vue` | 关联单据选择器、单号候选加载、列表「关联单据」列 |
| `src/views/finance/FinanceOpsView.vue` | 资金流水摘要显示关联单据 |
| `src/components/ChangePasswordModal.vue` / `PrintPreview.vue` | 点遮罩不关闭 + ESC 关闭 |
| `src/views/boss/UsersManageView.vue` / `ProductListView.vue` | 同上（员工编辑/重置结果、商品导入/批量/重复） |

### 25.4 验收

- 全量 **500 通过 / 45 文件**，`npm run build` 0 错误；新增 `tests/round19.test.ts`（13 例：头部单行结构 / 关联单据存取 / 弹窗阻塞与 ESC）。
- 实机（老板账号）：打印预览头部「家电批发」居左、「采购单」居中同一行；点击弹窗遮罩不关闭、ESC 关闭；
  记一笔选「采购单 CG20260920-640」保存后，流水显示「采购单 CG20260920-640」。

---

## 26. 第二十轮：赠品管理 + 报价单（V1.0-9，2026-09-20）

### 26.1 赠品（采购 / 销售随单赠送）

**需求背景**：采购时厂家随货送赠品、销售时也可给经销商 / 客户送赠品。赠品要走正常单据流程（验货入库、拣货出库、库存可查），但金额为 0。

- **建单**：采购单 / 销售单明细行新增「赠品」开关。勾选后：
  - 进价 / 售价输入禁用，价格恒为 0；金额列显示「🎁 赠品」；
  - 不计入合计金额（合计只统计非赠品行），合计行提示「含赠品 N 件」；
  - 销售切批发 / 零售价时赠品行价格不跟随切换。
- **库存**：赠品行照常验货入库 / 拣货出库——库存正常增减、占用库存、参与超库存校验。
- **流水**：出入库流水备注附加「赠品」，追溯时能看出哪些是随单赠品。
- **打印**：采购单 / 销售单（送货单）打印时赠品行单价列显示「—」、金额列显示「赠品」；出入库单打印按流水备注里的「赠品」标记同样处理。
- **详情**：明细行显示「🎁 赠品」徽标，单价「—」、金额「赠品」，合计行提示含赠品件数。
- **赠品台账**（独立统计报表：按供应商 / 按客户汇总赠品数量与金额）作为第二步，本轮不做。

### 26.2 报价单（客户询价 → 临时报价 → 转销售单）

**需求背景**：客户询价时给出临时报价单，订单确定后可直接转成销售单，避免重复录入。

- **入口**：销售管理页内 Tab「销售单 / 报价单」，不新增独立菜单。
- **单号**：`BJ` + 日期（yyyyMMdd）+ 3 位随机。
- **建单**：选客户（已有客户）或散客（直接填名字）；加商品、填数量与报价（默认带批发价可改）、有效期（可选）、备注。
- **报价不占库存**：报价单不生成应收、不影响库存与财务。
- **状态**：`draft` 待报价 / `sent` 已报价（预留）/ `converted` 已转销售单 / `void` 已失效。
- **一键转销售单**：详情页「转为销售单」——明细与价格整单带过去，走销售单库存校验；
  - **散客自动建档**：报价单上的客户名自动补建客户档案（备注「由报价单自动建档」，联系方式后续可补）；
  - 报价单回填销售单号并标记「已转销售单」；销售单备注带「来源报价单 BJ…」；双向可查；
  - 已转换的报价单不能重复转换，也不能删除。
- **打印**：报价单打印复用单据打印模板（title=报价单）。
- **操作日志**：创建报价单 / 删除报价单 / 报价转销售单 三类动作入操作日志。

### 26.3 验收

- 全量 **507 通过 / 46 文件**，`npm run build` 0 错误；新增 `tests/round20.test.ts`（7 例：采购赠品入库 / 销售赠品出库 / 打印赠品 / 报价单创建与单号 / 散客转单自动建档 / 老客户转单不重复建档 / 禁止重复转换）。
- 实机（销售账号）：新建销售单勾选赠品后金额合计不含赠品、打印预览赠品行显示「赠品」；报价单新建 → 详情 → 转为销售单 → 散客自动建档、报价单状态变「已转销售单」并回填单号。


---

## 二十七、第二十一轮（V1.0-10）：系统内置账户 + 记一笔关联框修复

### 27.1 系统内置账户 hawsystem

**需求背景**：老板需要一个最高权限、且不会被误改误删的系统账户长期使用。

- **凭据**：登录名 `hawsystem`，初始密码 `admina1b22c333`；角色 `boss`（与老板同级，全部菜单与数据可管）。
- **保护**：用户记录带 `system=true` 标记——
  - 员工管理页该行姓名旁显示「系统」徽标；「编辑」按钮置灰，「重置密码 / 停用」不渲染；
  - 数据层 `updateUser` / `resetPassword` / `changePassword` / `setUserStatus` 一律拒绝（UI 绕过也改不动）；
  - 不提供删除员工的入口，天然不可删。
- **老库自动补建**：每次登录走 `initDefaultAdmin()`，若库中尚无 hawsystem 则自动创建；重复调用幂等。
- **原有 admin / admin123 保留**，不强制禁用。

### 27.2 记一笔「关联订单」单号选择框过窄（bug）

- 关联单据字段原本落在 `auto-fit` 网格的窄列里，单号输入框被挤到约 300px，号码看不全。
- 修复：该字段 `grid-column: 1 / -1` 独占整行；类型列 176px，单号输入框 `minmax(0,1fr)` 撑满剩余（桌面端约 900px，约 3 倍宽）；移动端仍上下堆叠。

### 27.3 验收

- 全量 **517 通过 / 47 文件**（新增 `tests/round21.test.ts` 9 例：种子创建、密码哈希登录、资料/密码/停用三处拒绝、幂等、admin 保留、样式断言）。
- 实机：hawsystem 可登录；员工管理页该行带「系统」徽标且按钮不可用；记一笔关联框独占整行、单号完整可见。


---

## 二十八、第二十二轮（V1.0-11）：手机端商品列表简化 + 性能 + 员工表溢出

### 28.1 手机端商品卡片文字简化
- 商品档案手机端卡片：进价→「进」、批发→「批」、零售→「零」、库存→「库」、预警→「警」，一屏显示更多商品。
- 电脑端表头保持全称不变。

### 28.2 商品档案手机端加载优化
- 搜索输入加 250ms 防抖：商品上千时每敲一个字全量过滤不再卡输入。
- `stockMap()` 修正：多库位（总仓/门店）库存**汇总**为总库存（旧实现只保留最后一个库位，多库位时库存数错）。

### 28.3 员工管理表手机端溢出修复
- 表格外包横向滚动容器，手机端可左右滑动点到最右的「编辑」操作；
- 手机端隐藏最占宽度的「可见模块」列（模块权限在电脑端配置即可）。

### 28.4 验收
- 全量 **517 通过 / 47 文件**；`npm run build` 0 错误。

---

## 二十九、第二十三/二十四轮（V1.0-12）：手机端筛选布局 + 标题去重 + 弹窗滚动 + 报价单价格切换 + 库存深度优化

### 29.1 商品选择器：手机端隐藏「型号 / 规格」列
- `src/components/ProductPicker.vue`：`型号 / 规格` 列加 `col-spec` 钩子，手机端（<768px）隐藏。
- 该列与商品名（`品牌 + 型号`）重复，隐藏后本行信息更宽松；电脑端保留。
- 空行 `colspan` 改为 `colCount` 计算值（手机端隐藏一列后要跟着减 1），避免列数错位。

### 29.2 手机端筛选条统一换行
- `src/styles/theme.css` 新增 `@media (max-width: 767px)`：
  - 筛选条整体 `flex-wrap: wrap`；
  - 搜索框 `flex: 1 1 100%` 独占第一行；
  - 其余控件按内容收缩并排（状态 + 时间、分类 + 状态各占一行）；
  - 日期控件收窄（`flex: 1 1 120px; max-width: 145px`）腾出并排空间。
- 钩子用 `.app-layout.is-mobile`，一次性覆盖全部页面（桌面端规则仍在 `@media (min-width: 768px)`）。

### 29.3 手机端页头去重
- 手机端顶部 `MobileTopNav` 已显示模块名，页面内的 `PageHeader` 标题与之重复。
- 隐藏 `.ui-page-main`（标题 + 副标题），**保留 `.ui-page-actions`**（工作台页的「新建」等按钮仍在）；
- 没有操作按钮的页头用 `:has()` 整体收起，不留空行。

### 29.4 员工弹窗手机端可滚动
- `UsersManageView.vue` 的 `.modal` 补 `display: flex; flex-direction: column; max-height: 90vh`，
  `.modal-body` 补 `overflow-y: auto`：头部/底部固定、主体滚动，
  解决「新建员工」长表单溢出屏幕、确定按钮点不到的问题。
- 实测 390×844：弹窗高 759px（=90vh），主体内容 1179px 在 644px 区域内可滚动。

### 29.5 报价单批发 / 零售价切换
- `QuotesView.vue` 参照销售开单加 `priceMode` + `switchMode()`：切换时整单单价随之切换，个别行仍可手工改价。
- `ProductPicker` 的 `price-mode` 跟随 `priceMode`，商品列表直接显示对应价格。
- 「选择系统中已有客户 + 直接填写客户名称」双通道**原本已支持**（下拉 + 手填），本轮核对确认保留。

### 29.6 库存深度优化（调拨 / 盘点首屏卡顿）
- 根因：`inventory.ts` 的 `syncLocationStock()` 对**每个商品**发一次 Dexie 查询（N+1），
  商品上千时首屏卡 5 秒以上，且被 `TransferView` / `CountView` 的 `init()` 排在第一位调用，
  导致「调拨方向仓位下拉」和「选择商品列表」迟迟出不来。
- 修复：改为一次 `Promise.all` 把 `stock` / `locationStock` 两张表读进内存再比对，
  查询次数 N+1 → 2 次 `toArray`；写入语义完全不变（只补缺失行，不覆盖已有分布）。
- 实测：调拨页首屏就绪 **245ms**、盘点页 **46ms**（原先 5 秒以上）。

### 29.7 验收
- 新增 `tests/round24-mobile.test.ts`（5 例）：手机端 colspan / col-spec 钩子 / theme.css 规则 / 报价单价格切换。
- 全量 **522 通过 / 48 文件**；`npm run build` 0 错误。

---

## 三十、V2.0-2（2026-09-21）：云数据库迁移 + 多人共用地基

> 第一阶段（V1.0-12）功能冻结，进入第二阶段。本轮完成"50 人共用"的地基——
> 数据从单机 IndexedDB 迁到 Supabase 云数据库，所有客户端读写同一份数据。

### 30.1 架构变化
| 项 | V1.0-12（旧） | V2.0-2（新） |
|---|---|---|
| 数据存储 | 每台设备本地 IndexedDB | Supabase PostgreSQL（云端共享） |
| 多人共用 | 不支持（各自一份） | 支持（一份数据所有人读） |
| 实时性 | 本机即时 | 刷新后可见（Realtime 待加） |
| 部署 | GitHub Pages 静态站 | 不变（前端仍在 GitHub Pages，数据在 Supabase） |
| 保底 | — | V1.0-12 保留在 git 历史，可随时切回 |

### 30.2 本轮交付
- **Supabase 项目**：免费档、悉尼节点（ap-southeast-2）、24 张业务表全部建好。
- **数据访问层**：新增 `src/db/cloudDb.ts`，`CloudTable` 类模仿 Dexie Table 常用 API
  （`toArray / get / put / add / delete / update / count / where().equals() / anyOf()`），
  业务代码零改动切换。
- **数据源开关**：`src/db/supabaseClient.ts` 的 `USE_CLOUD`；测试环境（vitest）强制 false，
  单元测试仍跑本地 Dexie。
- **数据迁移**：`src/utils/cloudMigrate.ts` + 系统设置 → 数据备份区「☁️ 迁移到云端」按钮，
  一键把 IndexedDB 全量 upsert 到 Supabase（可重复执行，按 id 覆盖）。
- **id sequence**：迁移后已重置到 max(id)+1，新插入从 100 开始不冲突。
- **工作台标题动态化**：AppLayout / MobileTopNav 根据当前登录角色显示——
  `hawsystem`（system=true）→「管理员工作台」；其他角色 →「XX工作台」（采购/销售/财务/库房）。

### 30.3 已知边界（第二阶段后续）
- 刷新才同步：A 开单 B 需刷新才看到（Supabase Realtime 未接）。
- 无行级权限：销售仍能看所有销售单（RLS + Supabase Auth 待接）。
- 无冲突检测：两人同时改同一单，后保存覆盖先保存。
- 安全：第一版未开 RLS，应用层自有登录保护；接 Supabase Auth 后收紧。

### 30.4 验收
- 全量 **522 通过 / 48 文件**；`npm run build` 0 错误。
- 实机：hawsystem 登录显示「管理员工作台」；商品档案从云端读 6 条；新增商品写云端成功。
- 版本：V2.0-2 / 2.0.0-1。


---

## 第31节 V2.0-2（2026-09-21）云端性能与稳定性

### 修复
- 商品导入按钮无反馈 → 加成功/失败提示
- 6500 条商品只导 1000 条 → Supabase select 默认只返回 1000 行，改 insert 时直接返回 id
- 所有列表只显示前 1000 条 → 数据底层加自动分页，并行拉完所有行
- 清空业务数据按钮无反应 → CloudTable 加 clear() 方法
- CloudQuery 缺 count() → 补上
- CloudTable 缺 bulkGet/filter → 补上
- 导入时被跳过行无原因显示 → 加 lineErrors 展示

### 性能优化
- 列表查询从串行分页改并行分页（6281 条从 7 次串行请求变并行，~1秒→~150ms）
- 商品导入改批量插入（500条/批），5000 条从 5000 次请求降到约 20 次
- 扩大云端内存缓存：saleOrders/purchaseOrders/quoteOrders
- 采购单/销售单列表页改并行加载

### 功能调整
- 预采询价归到采购管理页内 Tab（和销售报价单一致）
- 版本号规则：V2.0-1 之后每次更新 +1
