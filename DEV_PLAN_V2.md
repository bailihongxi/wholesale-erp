# 家电批发进销存 ERP · UI重写开发计划（V2.0）

> 版本：V2.0（UI设计最终版 · 业务逻辑层已完成）
> 日期：2026-09-19
> 配套文档：PRD.md、UI设计稿.html（最终确认版）
> 前置状态：业务逻辑层已完成（74个测试全绿），本次重写UI层
>
> **产品版本：V2.1-1（2026-09-23 B1 经销商门户（上）：dealer 只开放报价单页 + 按 customerId 隔离 + 渠道入口）**　｜　历史：V2.0-29 全站按钮配色统一：打印=绿 / 删除·取消=红 / 返回=橘 / 修改=蓝，唯一来源 `src/styles/theme.css`；出入库单「撤回」与「删除」底层同为 `revertDoc`，已合并为受 `canDeleteDoc` 闸控的「删除本单」）**　｜　历史：V2.0-28 单据删除权限：新增 `canDeleteDoc`，仅老板与系统管理员能删单据，覆盖销售单 / 采购单 / 报价单 / 预采询价 / 出入库单；V2.0-27 新建/修改单据后回到列表页自动刷新：新增 `useReloadOnActivate`，修掉 `<keep-alive>` 导致列表停在旧数据的全站性问题；V2.0-26 修改销售单模块去橘色大边框 + 模块内删掉「返回」按钮，关闭统一走 closeEdit；V2.0-24 电脑端「修改销售单」明细卡片内改单行排列；V2.0-23 电脑端销售单详情/修改页同步改卡片式，明细表格整体退场；V2.0-22 手机端销售单详情/修改页的商品明细改卡片式；V2.0-21 手机端「选择商品」列表改卡片式 + 版本收口；V2.0-20 销售单页面级编辑模式；V2.0-19 未出/入库单据可修改 + 编辑弹窗重设计；V2.0-18 批量并行优化 + 合计行对齐；V2.0-16 手机端合计行通栏彻底修复；V2.0-15 启动白屏 3 秒修复；V2.0-14 手机端表格横向滚动修复；V2.0-13 价格规则一键应用提速；V2.0-12 整体提速 + 合计行通栏修复；V2.0-10 设置页自助启用云端；V2.0-9 首屏加载页跟随系统名称；V2.0-5 选商品 1000 条修复 + 服务端分页
> 本文档为开发执行计划，产品对外版本统一为 **V1.0-11**（`src/version.ts` / `package.json` 同步）。
> 历史基线见文末 **「十六、V1.0-1 发布基线」**；第十二轮见 **「十七、V1.0-2 · 第十二轮」**，第十三轮见 **「十八、V1.0-3 · 第十三轮」**，第十四轮见 **「十九、V1.0-4 · 第十四轮」**，第十五 / 十六轮见 **「二十、V1.0-5 · 第十五轮」「二十一、V1.0-5 · 第十六轮」**，第十七轮见 **「二十二、V1.0-6 · 第十七轮」**，第十八轮见 **「二十三、V1.0-7 · 第十八轮」**，第十九轮见 **「二十四、V1.0-8 · 第十九轮」**，第二十轮见 **「二十五、V1.0-9 · 第二十轮」**，第二十一轮见 **「二十六、V1.0-10 · 第二十一轮」**，第二十二轮见 **「二十七、V1.0-11 · 第二十二轮」**。

---

## 一、当前进度盘点

### ✅ 已完成（业务逻辑层）
- 12张Dexie数据库表 + 默认老板账号初始化
- 8个Pinia store（user/product/purchase/sales/finance/dashboard/dealer/sync）
- 单号生成工具、金额大写工具
- 打印模板生成器（带/不带价格）
- 74个单元测试全绿

### 🔄 本次要做（UI表现层重写）
- 按最终UI设计稿重写所有页面
- 实现手机/电脑双端响应式自适应
- 登录页简化版（头像+系统名+账号+密码）
- 手机端3个Tab（工作台/业务/我的），全模块分类折叠
- 电脑端左侧导航栏（15项，含库房管理入库/出库/库存），支持折叠/展开
- 老板10大模块全部入口打通
- 权限按角色严格隔离

---

## 二、技术栈（不变）

| 类别 | 选型 | 说明 |
|---|---|---|
| 框架 | Vue 3 + Vite + TypeScript | 已完成 |
| UI库 | Vant 4 | 已安装，重写组件样式 |
| 路由 | Vue Router 4 | 已完成，补全路由表 |
| 状态 | Pinia | 已完成 |
| 数据库 | Dexie (IndexedDB) | 已完成 |
| 响应式 | CSS媒体查询 + Vue响应式 | <768px手机版，≥768px电脑版 |

---

## 三、分阶段开发计划（共 8 个阶段）

### 阶段 1 · 全局布局框架重写（2天）

**目标**：搭好手机/电脑双端响应式骨架，所有页面在这个框架里跑

**任务清单**：
1. 重写 `App.vue`，加全局响应式容器
2. 新建 `components/AppLayout.vue` 全局布局组件
   - 电脑端：左侧导航栏 + 右侧内容区
   - 手机端：顶部导航 + 内容区 + 底部Tabbar
3. 新建 `composables/useResponsive.ts` 响应式判断（isMobile / isDesktop）
4. 重写路由表，补全老板全部12个模块路由
5. 侧边栏折叠状态管理（Pinia + localStorage持久化）
6. 全局配色变量定义（CSS变量）

**验收标准**：
- [ ] 窗口宽度<768px显示手机版布局（底部Tab）
- [ ] 窗口宽度≥768px显示电脑版布局（左侧导航）
- [ ] 拖动窗口宽度时自动切换布局
- [ ] 侧边栏折叠/展开按钮工作正常
- [ ] 折叠状态刷新后保持

---

### 阶段 2 · 登录页重写（1天）

**目标**：按设计稿重写登录页，简化为头像+系统名+账号+密码

**任务清单**：
1. 重写 `LoginView.vue`
   - 顶部渐变背景（深蓝色渐变）
   - 居中圆形头像（默认灰色人形）
   - 系统名称「家电批发ERP」+ 副标题
   - 手机号输入框（带手机图标）
   - 密码输入框（带锁图标）
   - 蓝色登录按钮
   - 默认账号提示
2. 登录后自动识别角色，跳转对应工作台
3. 电脑端登录页左右分栏（左品牌介绍，右表单）
4. 手机端登录页上下布局（渐变头+卡片表单）

**验收标准**：
- [ ] 手机端：头像→系统名→手机号→密码→登录按钮
- [ ] 电脑端：左右分栏，左侧品牌区右侧表单区
- [ ] 老板账号登录跳转老板工作台
- [ ] 其他角色登录跳转对应工作台
- [ ] 角色自动识别，无需手动选择

---

### 阶段 3 · 老板工作台重写（2天）

**目标**：老板端全部10大模块入口打通，数据看板正常

**任务清单**：
1. **工作台首页**
   - 欢迎语 + 待办提醒
   - 4个数据卡片：本月销售额、本月毛利、应收款、库存预警
   - 今日待办列表：待入库/待发货/待收款
   - 6个快捷操作入口
2. **手机端Tab结构**
   - Tab1 工作台：数据看板+待办+快捷操作
   - Tab2 业务：商品中心/采购管理/销售管理/库房管理 分类列表
   - Tab3 我的：财务管理/人事权限/报表中心/系统设置 分类列表
3. **电脑端侧边栏**
   - 12项完整菜单：工作台/商品档案/库存管理/采购管理/销售管理/客户管理/供应商/财务管理/员工管理/经销商/经营报表/系统设置
   - 支持折叠/展开
4. **退出登录功能**（右上角）

**验收标准**：
- [ ] 老板手机端3个Tab可切换
- [ ] 老板电脑端侧边栏12项菜单可点
- [ ] 侧边栏折叠/展开正常
- [ ] 数据卡片显示真实汇总数据
- [ ] 退出登录返回登录页

---

### 阶段 4 · 商品中心页面重写（2天）

**目标**：商品档案 + 库存管理 两个模块页面完成

**任务清单**：
1. **商品列表页**
   - 手机版：卡片列表，商品名+分类+批发价+库存
   - 电脑版：表格视图，多列（名称/分类/进价/批发价/库存/状态/操作）
   - 搜索栏：按品牌/型号搜索
   - 分类筛选下拉
   - 右下角FAB新增按钮
   - 库存预警红色标记
2. **商品新增/编辑页**
   - 表单：品牌、型号（名称实时预览）、分类、规格、单位
   - 价格字段：进价、批发价、零售价（权限控制显示）
   - 库存预警值
   - 保存按钮
3. **库存管理页**
   - 实时库存列表
   - 低库存预警商品列表
   - 出入库流水记录

**验收标准**：
- [ ] 新增商品，名称自动显示「品牌 型号」
- [ ] 商品列表手机版卡片、电脑版表格
- [ ] 搜索功能正常
- [ ] 库存预警红色标记
- [ ] 手机版和电脑版切换时布局正常

---

### 阶段 5 · 采购+销售模块重写（3天）✅ 已完成 2026-09-19

**目标**：采购管理 + 销售管理 两个模块全部页面完成

**任务清单**：
1. **采购单列表**
   - 手机版：卡片列表（单号+供应商+金额+状态）
   - 电脑版：表格视图
   - 状态筛选（待入库/部分入库/已完成）
   - FAB新建按钮
2. **新建采购单**
   - 选择供应商（弹窗选择）
   - 添加商品明细（弹窗选商品，带进价）
   - 数量编辑 + 自动算金额
   - 合计栏 + 提交按钮
3. **供应商管理**
   - 列表 + 新增/编辑
4. **销售单列表**
   - 同采购单列表结构
   - 状态：待出库/部分出库/已完成
5. **新建销售单**
   - 选择客户（弹窗选择）
   - 添加商品（只显示批发价，不显示进价）
   - 数量编辑 + 自动算金额
6. **客户管理**
   - 列表 + 新增/编辑
   - 经销商账号创建（loginPhone/loginPassword）

**验收标准**：
- [x] 新建采购单→提交→列表显示
- [x] 新建销售单→提交→列表显示
- [x] 销售选商品时看不到进价
- [x] 手机版表单/电脑版表单都能正常提交
- [x] 权限隔离：销售看不到进价字段

> 关联测试：`tests/stage5.test.ts`（9 个用例全绿）。修复了两处真实 bug：采购/销售单列表页模板误用 `list`（应为 `orders`）；并统一用 `vi.waitFor` 等待 Dexie 多步异步写入落库。

---

### 阶段 6 · 库房+财务模块重写（2天）

**目标**：库房管理 + 财务管理 两个模块页面完成

**任务清单**：
1. **待收货入库页**
   - 待入库采购单列表
   - 点进去入库详情：逐项确认实收数量
   - 确认入库按钮→库存自动增加
2. **待发货出库页**
   - 待出库销售单列表
   - 点进去出库详情：逐项确认实发数量
   - 显示当前库存数量
   - 确认出库按钮→库存自动扣减
3. **库存查询页**
   - 所有商品实时库存
   - 库房端看不到任何价格
4. **应收应付对账页**
   - 手机版：Tab切换应收/应付
   - 电脑版：双栏或Tab切换
   - 应收列表：客户欠款明细
   - 应付列表：供应商欠款明细
   - 登记收款/付款按钮
5. **毛利核算页**
   - 销售收入 - 成本 = 毛利
   - 按月/按品类统计

**验收标准**：
- [x] 库房账号登录，任何页面看不到价格
- [x] 入库操作后库存数量正确增加
- [x] 出库操作后库存数量正确减少
- [x] 财务能看到应收应付明细
- [x] 登记收款后单据状态更新

> 关联测试：`tests/stage6.test.ts`（6 个用例全绿）。
> 修复：入库/出库详情页 `inboundQty`/`outboundQty` 用「整体对象替换」而非「逐项赋值」初始化（避免响应式未捕获新键）；测试改为 `vi.waitFor` 等 `onMounted` 真正加载完成后再点击提交，避免 qtyMap 为空导致库存不更新。

---

### 补充 · 库房管理接入老板/财务左侧菜单栏（2026-09-19）

**背景**：阶段 6 已重写库房页面，但老板/财务的左侧菜单（SideBar）只有「库存管理」(`/boss/stock`)，无法进入入库/出库操作；且 `/warehouse/*` 路由仅对 `warehouse` 角色开放，老板点击会被守卫重定向回首页。

**改动**：
1. `src/router/navConfig.ts`：老板侧边栏新增「入库验货 `/warehouse/inbound`」「出库拣货 `/warehouse/outbound`」「库存查询 `/warehouse/stock`」三项（紧邻库存管理）；财务侧边栏新增「库存查询」。
2. `src/router/index.ts`：路由守卫 `meta.role` 支持 `string | string[]`；库房路由 `meta.role` 改为 `['warehouse','boss']`（库存查询额外对 `finance` 开放）。
3. 修复两处既有构建类型错误（不影响本次需求，但会阻断 `npm run build`）：`boss/StockView.vue` 库存预警列表误用 `item.product`/`item.quantity`（脚本实为 `Product[]`，已按 `list` 一致方式渲染）；`purchase/SuppliersView.vue` 新增供应商漏传 `remark`。

**验收**：
- [x] 老板左侧菜单含入库/出库/库存三项
- [x] 老板可正常进入入库/出库/库存页（守卫放行）
- [x] 财务可进入库存查询页
- [x] 销售等未授权角色访问库房页仍被拦截回首页
- [x] `vue-tsc -b` 通过，`npm test` 全绿（127 用例）

> 关联测试：`tests/warehouse-menu.test.ts`（7 个用例全绿）。

---

### 阶段 7 · 报表+打印+系统设置（2天）

**目标**：报表中心 + 打印 + 系统设置 三个模块完成

**任务清单**：
1. **经营报表页**
   - 本月销售额/毛利/应收应付 汇总卡片
   - 销售趋势图（简单柱状图）
   - 库存预警商品列表
   - 电脑版：多列布局
2. **打印中心**
   - 采购单打印预览
   - 销售单打印预览
   - 「带价格/不带价格」切换开关
   - 调用浏览器打印功能
3. **系统设置页**
   - 数据备份：导出JSON
   - 数据恢复：导入JSON
   - 手动云同步：上传/拉取按钮
   - 公司信息设置（名称/地址/电话）
   - 退出登录
4. **操作日志页**
   - 谁在什么时候做了什么操作

**验收标准**：
- [ ] 报表页显示真实汇总数据
- [ ] 打印预览带价格/不带价格切换正常
- [ ] 导出备份JSON文件成功
- [ ] 导入恢复JSON文件数据完整
- [ ] 云同步按钮工作正常

---

### 阶段 8 · 经销商端 + 全量验收（2天）

**目标**：经销商端完成 + 全量验收 + 部署

**任务清单**：
1. **经销商产品目录页**
   - 商品列表：品牌+型号、规格、批发价
   - 搜索功能
   - 产品详情：只显示名称+批发价+规格
   - 权限：看不到进价、库存、其他客户
2. **全量验收测试**
   - 按PRD第10节17条验收标准逐项检查
   - 手机版/电脑版双端测试
   - 6个角色权限测试
3. **部署到GitHub Pages**
   - 配置base路径
   - build生产版本
   - 推送部署
4. **最终交付**

**验收标准**：
- [ ] 经销商登录后只能看产品+批发价
- [ ] 经销商看不到进价和库存
- [ ] 手机/电脑双端全部功能正常
- [ ] PRD第10节17条全部通过
- [ ] GitHub Pages可正常访问

---

## 四、时间总览

| 阶段 | 内容 | 工期 |
|---|---|---|
| 阶段1 | 全局布局框架重写 | 2天 |
| 阶段2 | 登录页重写 | 1天 |
| 阶段3 | 老板工作台重写 | 2天 |
| 阶段4 | 商品中心页面 | 2天 |
| 阶段5 | 采购+销售模块 | 3天 |
| 阶段6 | 库房+财务模块 | 2天 |
| 阶段7 | 报表+打印+设置 | 2天 |
| 阶段8 | 经销商端+全量验收 | 2天 |
| **合计** | | **约16天（3周多）** |

---

## 五、开发顺序原则

1. **先搭骨架（阶段1-2）**：响应式布局框架和登录页必须先搞定，后面所有页面都在这个框架里跑
2. **老板端优先（阶段3-4）**：老板是超级用户，先把老板端全部页面跑通
3. **业务闭环跟进（阶段5-6）**：采购销售库房财务是核心业务流程
4. **增强功能收尾（阶段7-8）**：报表、打印、经销商、验收放最后
5. **每阶段必测**：每做完一个阶段，跑一遍相关测试，不攒到最后

---

## 六、关键设计原则（开发时必须遵守）

### 6.1 响应式断点
- 手机模式：窗口宽度 < 768px
- 电脑模式：窗口宽度 ≥ 768px
- 同一套代码，CSS媒体查询 + Vue响应式自动切换

### 6.2 配色规范
- 主色：#1a365d（深海军蓝）
- 强调色：#2563eb（科技蓝）
- 成功：#10b981（翠绿）
- 警告：#f59e0b（琥珀）
- 危险：#ef4444（朱红）
- 背景：#f8fafc（浅灰白）

### 6.3 权限铁律（代码层面必须实现）
- 销售：商品列表不显示进价字段
- 库房：所有页面不显示任何价格
- 经销商：只显示产品+批发价
- 老板：全部可见

### 6.4 交互规范
- 手机端按钮最小点击区域：44×44px
- 侧边栏折叠状态存localStorage
- 页面切换过渡动画：0.2s
- 表单提交loading状态

---

## 交互与验收补充（2026-09-19）

### 补充 1 · 侧边栏折叠（回弹）按钮移入菜单栏 ✅（2026-09-19 修正）

> ⚠️ 本节此前理解有误：把「回弹按钮」误做成了「返回按钮」，在菜单栏头部多加了一个「‹ 返回」按钮，
> 且真正要移动的折叠按钮仍留在功能页面左上角。老板已指出，现按正确做法修正。

- **正确做法**：把「侧边栏折叠 / 展开（回弹）按钮」放进左侧菜单栏头部，紧跟在「家电批发ERP」文字之后。
- **已删除**此前误加的「返回」按钮（菜单栏头部以及功能页面左上角均不再有返回按钮）。
- `src/components/AppLayout.vue`：移除顶部栏最左侧的折叠按钮（即截图红框内的按钮）；顶部栏现在只有 页面标题 / 用户 / 退出。
- `src/components/SideBar.vue`：头部顺序为 `ERP 图标 → 家电批发ERP → 折叠按钮(‹)`；折叠后按钮显示 `☰`，图标与文字隐藏、按钮居中。
- 功能页面左上角不再有任何返回 / 折叠按钮：手机端 `MobileTopNav` 无返回按钮；`boss/BossReportsView.vue` 的 `van-nav-bar` 已去掉 left-arrow 返回箭头。
- 手机端导航依靠底部 Tab；电脑端依靠常驻的左侧菜单栏。

> 关联测试：`tests/menu-order.test.ts`（14 个用例全绿，其中 6 个覆盖折叠按钮位置与行为）。

### 补充 2 · 侧边栏菜单支持自定义排序 ✅
- 新增 `src/stores/menuOrder.ts`：按角色把菜单顺序持久化到 localStorage（`erp_menu_order_<role>`）
- 侧边栏菜单项支持拖拽排序，顺序即时生效并持久化；底部提供「↺ 重置菜单顺序」恢复默认
- 健壮性：新增菜单自动补到末尾、已移除菜单自动剔除、顺序按角色隔离
- 顺序保存在响应式 ref 中，保证拖拽后侧边栏立即重渲染

### 补充 3 · 全量运行验证 ✅
- `npm run build`（含 `vue-tsc` 类型检查）通过；`npm run dev` 启动后 HTTP 200 可正常访问
- 新增 `tests/menu-functions.test.ts`（29 用例）：逐菜单校验
  - 所有角色的每个侧边栏菜单都能解析到已注册页面
  - 19 个菜单对应页面逐个挂载渲染，确认无报错、非空壳
  - 商品/供应商/客户/员工/采购单/销售单/毛利 等核心功能真实跑通并落库
- 新增 `tests/workflow.test.ts`（9 用例）：采购→入库→销售→出库→财务 全链路端到端

### 本轮修复的缺陷（运行验证中暴露，均已修复并补测试）
1. **库存管理页流水加载失败**：`StockView` 用 `orderBy('createdAt')` 查询 stockRecords，但该字段未建索引，Dexie 抛 `SchemaError`，导致该页最近流水一直为空。
   → 在 `src/db/index.ts` 增加 `version(2)`，为 `stockRecords` 补 `createdAt` 索引。
2. **分次入库永远无法完成**：`purchase.inbound` 只用「本次实收」与「订单量」比较，二次补收后仍判定 partial，采购单一直挂在待入库列表。
   → 改为累加该单该商品的历史已收数量后再判定。
3. **分次出库永远无法完成**：`sales.outbound` 存在同样问题（出库流水记为负数）。
   → 改为累加历史已发数量（取绝对值）后再判定。

### 补充 4 · 菜单功能清单评审（待老板确认）📋 2026-09-19

**背景**：老板反馈库房类等菜单「功能重复、临时占位、没有意义，不属于菜单中该有的功能」，要求**先列出每个菜单的功能 / 动作 / 作用**，再决定是否开发。

**产出**：`MENU_SPEC.md` —— 逐菜单列出「定位 / 页面动作 / 作用 / 现状（✅真实可用 ⚠️部分可用 🔁重复 ❌占位）/ 建议」，覆盖：
老板 15 项 + 手机端 2 个 Tab 页、采购 3 项、销售 3 项、财务 5 项、库房 4 项。

**盘点结论**：
- ✅ 真实可用：工作台(老板)、商品档案、库存管理、入库验货、出库拣货、采购单、销售单、客户管理、供应商、应收应付、系统设置（备份部分）
- 🔁 重复菜单：`/warehouse/stock` ↔ `/boss/stock`；`/boss/dealers` ↔ `/sales/customers`；`/finance/gross-profit` ↔ `/boss/reports`
- ❌ 占位无功能：采购 / 销售 / 财务 / 库房 4 个「工作台」+ 老板手机端「业务」「我的」
- ⚠️ 待补：采购单 / 销售单缺详情页（列表「操作」按钮跳错）；员工管理仅能新增；云同步为假实现
- 缺失功能 11 项：采购/销售单详情、打印中心、操作日志、简版盘点、经销商端、真实云同步、员工停用/改密、客户与供应商停用、3 个未注册 Tab 路由、客户等级定价

**待办**：老板在 `MENU_SPEC.md` 第五节拍板 4 个问题（库存入口合并、客户与经销商合并、工作台是否补真实待办、菜单是否重构）后，
据此更新 PRD.md 与本文档，并排出新的开发阶段。

### 补充 5 · 老板拍板「按推荐来」——第二轮改造 ✅ 2026-09-19

老板确认按推荐执行（P0 修错+补闭环 → P1 占位改真功能 → P2 增强），本轮完成以下内容。

#### 5.1 统一搜索组件：全项目搜索框自带清除按钮 ✅
- 新增 `src/components/SearchInput.vue`：
  - 有输入内容时右侧出现「×」清除按钮，无内容时隐藏（`v-show`，非 `v-if`，避免布局跳动）
  - 点击「×」立即清空并触发一次空搜索；按 **Esc** 也可清空
  - 支持防抖（默认 300ms，页面可传 `:debounce="0"` 立即触发）
  - 屏蔽浏览器原生搜索清除图标，避免与自定义按钮重复
  - `v-model` 双向同步，外部清空时输入框同步清空
- **已接入**：商品档案 `/boss/products`、库存管理 `/stock`、客户管理 `/customers`、操作日志 `/boss/audit-logs`
- 后续新增任何搜索框都必须复用该组件，不要手写 `<input>`

#### 5.2 库存查询增加搜索 + 与「库存管理」合并 ✅
- 新增 `src/views/stock/StockManageView.vue`（统一库存页）：
  - **搜索**（品牌/型号/分类，带清除按钮）
  - 分类下拉筛选（选项基于全量商品，不会因搜索变少）
  - 「仅看低库存」开关
  - 4 张汇总卡：SKU 数 / 库存总量 / 库存金额（进价）/ 预警数
  - 库存预警区 + 库存明细表 + 出入库流水（最近 20 条）
  - 响应式：手机端卡片、电脑端表格；**库房角色自动隐藏所有价格列**
- 路由：新增规范路由 `/stock`；`/boss/stock`、`/warehouse/stock` 保留 **302 重定向**，收藏夹不失效
- 已删除重复页面：`boss/StockView.vue`、`warehouse/StockQueryView.vue`

#### 5.3 客户管理与经销商合并；毛利并入经营报表 ✅
- 客户管理 `/customers`（原 CustomersView 增强）：搜索 + 清除按钮、「仅看经销商/仅看普通客户」筛选、
  经销商账号开关与登录号展示、电脑端表格 / 手机端卡片。旧路径 `/sales/customers`、`/boss/dealers` 重定向。
- 经营报表 `/boss/reports` 重写：日期区间筛选 + 快捷区间（本月/近3月/近半年/全部）、
  6 项指标（销售收入、销售成本、毛利、毛利率、应收未收、应付未付）、
  **近 6 月销售/毛利趋势柱图**、库存预警清单。旧路径 `/finance/gross-profit` 重定向。
- 财务 store 新增 `getProfitSummary(from, to)`、`getMonthlyTrend()`、`getReceivableTotal()`、`getPayableTotal()`
- 已删除重复页面：`boss/DealersView.vue`、`finance/GrossProfitView.vue`

#### 5.4 P0 补闭环：单据详情页 + 打印 + 操作日志 ✅
- **采购单详情页** `/purchase/orders/:id`（`views/purchase/PurchaseOrderDetailView.vue`）：
  单头（单号/状态/付款状态/供应商/联系人/日期/备注）、明细、金额合计、**入库进度条**、付款记录、**打印**
- **销售单详情页** `/sales/orders/:id`（`views/sales/SaleOrderDetailView.vue`）：同上，含**出库进度**、收款记录、**送货单打印**
- 打印：`src/utils/print.ts` 接上闲置的 `utils/printTemplate.ts`，A4 输出；可勾选「打印含单价」，
  取消后打印不含单价（给供应商/客户的版本）。窗口被拦截时给出提示。
- **修复列表「操作」按钮跳错**：此前按钮文字为「新建」且跳到新建页，已改为「查看」并跳该单详情（采购/销售两处）
- **操作日志**：新增 `src/utils/audit.ts`（`writeLog` + `AUDIT_ACTIONS`），关键动作真实写入 `auditLogs`；
  新增查询页 `views/boss/AuditLogView.vue`（`/boss/audit-logs`，支持搜索与按动作筛选）
- 埋点位置：商品新增/修改、采购建单、采购入库、销售建单、销售出库、登记收款、登记付款、新增客户、新增供应商
  （`createCustomer` / `createSupplier` / `createProduct` / `updateProduct` 新增可选 `operatorId` 参数，向后兼容）

#### 5.5 P1：四个角色工作台改为真实待办看板 ✅
- 采购：待入库单数、本月采购额、供应商数、未付款金额 + 待入库单据列表（点击直达验货页）
- 销售：待出库单数、本月销售额、客户数、未收款金额 + 待出库单据列表（点击直达拣货页）
- 财务：应收未收、应付未付、本月已收、本月已付 + 待收款/待付款单据列表（点击直达对账页）
- 库房：待收货单数、待发货单数、今日入库量、今日出库量 + 待收货/待发货单据列表（直达验货/拣货页）
- **顺带修复**：四个工作台原本各自内嵌了一个 `van-tabbar`，与 `AppLayout` 的 `MobileTabBar` 重复，已移除
- 老板手机端「业务」Tab：入口改为**由老板侧边栏菜单自动生成**（排除 Tab 自身），新增菜单自动同步
- 老板手机端「我的」Tab：新增登录人卡片（姓名/角色）与「操作日志」入口

#### 5.6 本轮测试
| 测试文件 | 覆盖 | 用例 |
|---|---|---|
| `tests/search-and-merge.test.ts` | SearchInput 清除按钮行为、库存搜索/分类/仅看低库存/权限、客户搜索与筛选、报表日期筛选与趋势 | 22 |
| `tests/p0-order-detail.test.ts` | 操作按钮跳详情、两个详情页、打印（含/不含单价）、日志写入与查询 | 11 |
| `tests/p1-workbench.test.ts` | 四工作台待办数量/金额/跳转/完成后消失 | 12 |
| 既有测试改造 | stage1/3 菜单数量 15→14、stage3 业务页改为「覆盖全部菜单」、stage4 搜索改用 `.search-field`、stage6 指向合并页、menu-functions 指向合并页 | — |

**合计 229 用例 / 24 个测试文件 全部通过**；`npm run build`（含 `vue-tsc`）通过；`npm run dev` HTTP 200。

#### 5.7 文档同步
- `PRD.md` → **V1.2**：第 11 节改为改造后现状（11.2 已合并 / 11.3 已改真实数据 / 11.5 本轮增强 / 11.6 验收状态）
- `MENU_SPEC.md` → 重写为改造后的逐菜单清单（老板 14 项 + 其他角色 + 剩余待办 9 项 + 测试情况）

#### 5.8 下一批（P2，待排期）
1. 经销商端产品目录页（经销商登录后目前会被弹回登录页）
2. 简版盘点单（PRD 5.4）
3. 员工账号编辑 / 停用 / 改密
4. 客户 / 供应商停用
5. 手机端「我的」Tab 路由（`/purchase/mine`、`/sales/mine`、`/finance/mine` 未注册，点击跳登录页）
6. 真实云同步（或明确只保留 JSON 备份）
7. 客户等级 → 批发价档位
8. 出库「实发 > 库存」前端拦截

### 补充 6 · 修复「点开菜单页面空白/功能无效」事故 ✅ 2026-09-19

#### 6.1 现象
老板账号登录后，点开「采购管理 / 销售管理 / 供应商 / 财务管理」等菜单，
页面会闪一下就跳回工作台；入库验货、出库拣货、客户、库存、财务等页面则显示空白。

#### 6.2 根因（两个，互相叠加）

**根因 A：菜单路由权限与角色不匹配（功能"点了没用"）**

`navConfig` 里老板侧边栏有 4 项指向仅授权给其他角色的路由，而路由守卫会按 `meta.role` 拦截：

| 菜单项 | 路由 | 原 meta.role | 后果 |
|---|---|---|---|
| 采购管理 | `/purchase/orders` | `purchaser` | 老板点击被弹回 `/boss/home` |
| 销售管理 | `/sales/orders` | `sales` | 同上 |
| 供应商 | `/purchase/suppliers` | `purchaser` | 同上 |
| 财务管理 | `/finance/reconcile` | `finance` | 同上 |

**根因 B：新装数据库为空（页面"空白"）**

IndexedDB 全新时各业务表均为 0 条，采购/销售/客户/供应商/入库/出库/财务等列表
全部显示「暂无数据」。此前没有任何示例数据或引导，容易被判断为「功能没开发」。

**根因 C（附带）：手机端三个「我的」Tab 路由未注册**

`/purchase/mine`、`/sales/mine`、`/finance/mine` 未注册，命中通配规则被重定向到 `/login`，
表现为点击即退出登录。

#### 6.3 修复
1. `src/router/index.ts`：老板为最高权限角色，所有业务路由 `meta.role` 补入 `'boss'`
   （采购/销售/财务/库房全线），保留对其他角色的越权拦截。
2. `src/views/RoleMineView.vue`（新增）：采购/销售/财务共用的手机端「我的」页，
   入口直接取自该角色菜单，避免两处维护不同步；注册三条 `/xxx/mine` 路由。
3. `src/utils/demoData.ts`（新增）：一键生成覆盖完整链路的示例数据
   —— 6 商品 / 3 供应商 / 3 客户（含 1 个经销商登录号）/ 3 张采购单 / 3 张销售单，
   并连带产生库存、出入库流水、应收应付与操作日志；同时提供清空业务数据。
   所有数据经各业务 store 写入，与真实操作同一套逻辑。
4. `src/views/boss/SettingsView.vue`：新增「示例数据」区块（载入 / 清空 + 当前数据量）。
5. `src/views/boss/BossHomeView.vue`：空数据时显示「开始使用」引导卡片（载入示例数据 / 直接录入）。

#### 6.4 浏览器真实验证（agent-browser 实机操作）
登录老板账号后逐页核对，全部正常：

| 页面 | 验证结果 |
|---|---|
| 采购管理 | 3 张单，金额/状态/查看按钮齐全，URL 未被弹回 |
| 销售管理 | 3 张单，可进入详情 |
| 入库验货 | 2 张待收货单（部分入库/待入库），点「验货」进详情 |
| 出库拣货 | 2 张待发货单（部分出库/待出库） |
| 客户管理 / 供应商 | 3 条客户、3 家供应商 |
| 财务管理 | 应收列表 + 登记收款按钮 |
| 库存管理 | 6 SKU / 23 件 / ¥24,200 / 预警 5 项 + 搜索筛选 |

**实机跑通完整入库**：进详情 → 填实收 6 / 10 → 确认入库 → BCD-253 库存 6→12、
M3-L235 库存 10→20、出入库流水 +2 条，提交后自动返回列表。

#### 6.5 测试
- 新增 `tests/nav-permission.test.ts`（14 用例）：遍历**每个角色**的 sidebar + tabbar 每一项，
  断言路由已注册、且登录后能真正停留（不被守卫弹回首页、不跳登录页）；
  另覆盖老板全业务可进、越权拦截、未登录重定向。
  此用例在修复前**失败 8 项**，正是本次事故的回归防线。
- 新增 `tests/demo-data.test.ts`（12 用例）：示例数据生成后各表有数、
  入库/出库/采购/销售/客户/供应商/库存/财务各页面**均不再是空白**、
  库存与流水一致、经销商账号可登录、幂等保护、清空后账号保留、首屏引导显隐。

**合计 255 用例 / 26 个测试文件 全部通过**；`npm run build`（含 `vue-tsc`）通过。

#### 6.6 待办清单更新
- ~~手机端「我的」Tab 路由未注册~~ → 本轮已修复（补充 6.3 第 2 项）

---

## 补充 7 · 查询输入框 + 二级页面操作按钮 + 打印重排 + 明细历史（2026-09-19）

### 7.1 需求
1. 采购 / 入库 / 销售 / 出库 / 财务都要有查询输入框，并且能查历史；
2. 每个二级页面按业务补「返回」或「取消」按钮；
3. 打印页面重新排版（原排版太简陋），且必须在预览页提供「打印」与「取消」；
4. 财务管理看不到历史信息；
5. 每个功能模块都要有正常的明细历史，信息流转后必须留痕。

### 7.2 交付

**① 二级页面统一操作栏 —— 新增 `src/components/PageActions.vue`**

| 页面 | 按钮 | 行为 |
|---|---|---|
| 采购单详情 | 返回 | 回 `/purchase/orders` |
| 销售单详情 | 返回 | 回 `/sales/orders` |
| 新建采购单 | 取消 + 提交采购单 | 取消回列表，**不产生单据** |
| 新建销售单 | 取消 + 提交销售单 | 同上 |
| 新增 / 编辑商品 | 取消 + 保存 | 取消回商品档案 |
| 入库验货 | 取消 + 确认入库 | 取消回待收货，不写流水 |
| 出库拣货 | 取消 + 确认出库 | 取消回待发货，不写流水 |

组件支持 `loading`（显示「处理中…」并禁用，防重复提交）与 `confirmDisabled`。

**② 各模块查询输入框（全部使用统一 `SearchInput`，自带清除按钮）**

| 页面 | 搜索范围 | 其他筛选 |
|---|---|---|
| 采购单 | 单号 / 供应商 | 状态 + 日期区间 + 重置 |
| 销售单 | 单号 / 客户 | 状态 + 日期区间 + 重置 |
| 入库历史 | 单号 / 供应商 / 商品 | 日期区间 + 重置 |
| 出库历史 | 单号 / 客户 / 商品 | 日期区间 + 重置 |
| 财务应收/应付 | 单号 / 往来单位 | 结清状态（未结清 / 已结清 / 全部） |
| 财务流水 | 单号 / 客户 / 供应商 | 收付款类型 + 日期区间 + 重置 |

**③ 打印重排 —— `utils/printTemplate.ts` 整体重写 + 新增 `components/PrintPreview.vue`**

- 排版：公司抬头（读系统设置公司名）→ 单据名 → 单号/日期/制单行 → 往来单位信息卡 →
  明细表（序号/商品/型号/单位/数量/单价/金额，表头跨页重复）→ 合计行 →
  金额框（合计数量、金额合计、**大写金额**）→ 备注 → 签章栏 → 页脚；
  内置 `@page A4` 与 `page-break-inside: avoid`，明细行不会被切断。
- 流程改为**先预览再打印**：点「打印」弹出预览弹窗（iframe 渲染真实单据），
  底部固定「取消」「打印」两个按钮，可勾选「打印含单价」；
  库房等无价格权限的角色不显示该开关。
- 金额格式统一为「千分位 + 两位小数」（如 `27,600.00`）。

**④ 明细历史（信息流转后留痕）**

| 位置 | 历史内容 |
|---|---|
| 采购单详情 | 入库流水（时间 / 商品 / 数量 / 操作人）+ 付款记录（含操作人） |
| 销售单详情 | 出库流水（时间 / 商品 / 数量 / 操作人）+ 收款记录（含操作人） |
| 入库页 | 「待收货 / 入库历史」双页签，历史含时间 / 采购单号 / 供应商 / 商品 / 数量 / 操作人 |
| 出库页 | 「待发货 / 出库历史」双页签，历史含时间 / 销售单号 / 客户 / 商品 / 数量 / 操作人 |
| 财务对账 | 「应收 / 应付 / 收付款流水」三页签；**默认只看未结清，可切「已结清 / 全部」查历史**，流水含时间/类型/单据号/往来单位/金额/操作人/备注 |
| 入库验货页 | 本单入库记录（时间 / 商品 / 数量 / 操作人） |
| 出库拣货页 | 本单出库记录（时间 / 商品 / 数量 / 操作人） |

**⑤ store 新增查询方法**

- `purchase.listInboundHistory()`：关联采购单/供应商/商品/操作人的入库流水
- `sales.listOutboundHistory()`：关联销售单/客户/商品/操作人的出库流水
- `finance.listReceivables(includeSettled)` / `listPayables(includeSettled)`：可含已结清
- `finance.listPaymentHistory()`：全部收付款流水（关联单据号与往来单位）

**⑥ 顺带修复**

- 出库拣货：新增前端**超发拦截**（实发 > 待发、实发 > 当前库存均拦截并提示）
- 入库/出库详情默认填入「剩余待收 / 待发」数量，避免重复超收超发
- 财务默认筛选由「只显示有余额的单据」改为**显式「未结清」筛选**，
  并保留已结清入口，解决「结清后单据消失、查不到历史」

### 7.3 测试
新增 3 个测试文件 / 30 用例：
- `tests/page-actions.test.ts`（10 用例）：组件行为 + 7 个二级页面按钮存在性/跳转/不产生副作用
- `tests/history-search.test.ts`（10 用例）：5 个模块的搜索、日期区间、历史明细、结清筛选、类型筛选
- `tests/print-preview.test.ts`（8 用例）：预览渲染、打印/取消按钮、打印调用与回退、单价开关

**合计 283 用例 / 29 个测试文件 全部通过**；`npm run build`（含 `vue-tsc`）通过。
真实浏览器逐页验证：入库历史 4 条、出库历史 4 条、财务流水 4 笔，打印预览排版正常。

---

## 补充 8 · 打印模板重做：纸张 / 分页 / 表头与字段自定义 ✅ 2026-09-19

**需求**：单据按 A4 一半（A5 横版）打印；明细超页自动分页；条码/表头与明细字段可由用户自行编辑；
A4 竖版时尾部与表格之间空白过大；客户看不懂商品是什么。

**① 纸张（新增 `src/utils/printSettings.ts`）**

| 选项 | 尺寸 | 用途 |
|---|---|---|
| **A5 横版**（默认） | 210 × 148 mm（A4 的一半） | 日常送货单 / 出库单 |
| **A4 竖版** | 210 × 297 mm | 明细多、需归档 |

A3 已按要求取消。`@page { size: <w>mm <h>mm; margin: 0 }`，横竖由宽高决定。

**② 自动分页（`paginateItems`）**

- 首页预留往来单位区、末页预留合计/签章区，因此首页容量 `cap-4`、末页 `cap-5`，其余 `cap`；
- 余两页时**均分**，避免出现只有 1 行的尾页；
- 行数守恒（不丢行不重复），每页重复表头，meta 区带「第 X / Y 页」，**合计与签章只在末页**；
- 容量默认 A5 = 6 行 / A4 = 14 行，可在设置里手动覆盖。

**③ 表头与字段自定义**

- 表头：公司抬头 / 副标题 / 地址 / 电话 / 页脚 / 签章栏开关 / 每页行数；
- **明细字段**：序号、商品名称、**类别**、型号、单位、数量、单价、金额 ——
  可**勾选显隐、改列名、上下调顺序**，`合计` 行按数值列自动对齐；
- 新增「类别」列（洗衣机 / 冰箱 / 空调…），解决「客户不知道是什么东西」；
- 入口两处：打印预览弹窗「⚙ 表头设置」+ 系统设置 →「打印设置」；
- 持久化 `localStorage`，配置损坏/缺字段自动清洗补齐。

**④ 修复**

- **A4 尾部空白过大**：根因是签章栏用了 `margin-top: auto`，在固定页高的纸张里被顶到底部，
  中间塌出大片空白。改为 `margin-top: 14px` 让内容紧凑自上而下排列。
- **移除重复的「合计方框」**：数量/金额/大写方框与表格合计行重复，已删除，合计只保留在表格底部。
- **换单号不刷新（真 bug）**：4 个详情页只用 `onMounted` 加载，同一路由只换 `:id` 时组件被复用，
  会一直显示上一张单据。改为 `loadXxx()` + `onMounted` + `watch(() => route.params.id)`。

**⑤ 测试**：新增 `tests/print-paper.test.ts`（22 用例）——A5/A4 尺寸与 @page、A3 已移除、
小单据不分页、20 行自动分页（页数/表头/单号/页码/合计位置）、不丢行不重复、每页行数可覆盖、
类别列、字段勾选/改名/排序、无价格时自动剔除价格列、配置持久化与损坏回退、
预览弹窗纸张切换与表头编辑实时生效、**换单号必须重新加载（去掉 watch 会失败）**。

**合计 305 用例 / 30 个测试文件 全部通过**；`npm run build`（含 `vue-tsc`）通过。
真实浏览器验证：A5 横版 793×560px（210×148mm）、A4 竖版 794×1123px（210×297mm）尺寸正确；
20 行单据 → 5 页（3/6/6/3/2 行），每页带表头与页码，合计仅在末页；
字段改名/关闭/调序后打印表头同步变化；A4 表格与签章间距 39px（此前被顶到页底）。

---

## 十六、V1.0-1 发布基线（2026-09-19）

### 16.1 版本定义

| 项 | 值 |
|---|---|
| 产品版本 | **V1.0-1** |
| 版本来源 | `src/version.ts` → `APP_VERSION = 'V1.0-1'`；`package.json` → `"version": "1.0.0-1"` |
| 发布日期 | 2026-09-19 |
| 版本含义 | V<主版本>.<次版本>-<修订号>；V1.0-1 = 1.0 的第 1 个修订版 |
| 版本展示 | 系统设置页头徽标（灰色 `V1.0-1`） |
| 发版方式 | 改 `src/version.ts` 与 `package.json` 两处，再更新 PRD 头部「产品版本」行 |

### 16.2 阶段回顾（本计划执行至今）

| 轮次 | 主题 | 关键产出 | 测试 |
|---|---|---|---|
| 基础 | 业务逻辑层 | 12 张 Dexie 表、8 个 Pinia store、打印模板 | 74 |
| 打印 | A5/A4 纸张、自动分页、表头字段自定义 | `print-paper.test.ts` | 305 |
| 第 6 轮 | 库存作业合并 + 调拨 / 盘点 / 退换货 | `WarehouseOpsView`、`inventory.ts`、`returns.ts` | 337 |
| 第 7 轮 | 多库房 + GitHub 云同步 + Pages 部署 | `cloudSync.ts`、`LocationsView`、`deploy-gh-pages.sh` | 356 |
| 第 8 轮 | 角色权限开关 + 记一笔 + 界面精细化 | `permission.ts`、`ledgerEntries`、`theme.css` + `components/ui/` | 372 |
| 第 9 轮 | 设计系统采纳收口 | 嵌套页头降级、页头徽标、覆盖率 18/37 页 | 372 |
| **第 10 轮** | **版本基线 + Git + Pages 上线** | `src/version.ts`、`.gitignore`、首次提交 `324420c`、tag `v1.0-1`、仓库与 Pages | **372（0 error）** |

### 16.3 V1.0-1 功能总览

- **商品档案**：搜索 / 分类筛选 / 批量导入导出 / 去重 / 条码
- **采购管理**：开单、审核、分次入库、供应商、应付
- **销售管理**：开单、审核、分次出库、客户、应收
- **库存作业**（统一入口 `/warehouse`）：入库验货、出库拣货、库存调拨、库存盘点、退换货、库房管理
- **多库房**：库房自定义（增/改名/删）、出入库选库房、库存明细按库房分列展示、库房间调拨
- **财务中心**（`/finance`）：应收应付对账、经营报表、**记一笔**（送货/物流/快递/安装/辅材等费用）、资金流水
- **权限**：6 类角色 × 17 个模块的开关矩阵，三处同时生效（侧边栏 / 手机 Tab / 路由守卫）
- **打印**：A5 横版 / A4 竖版、自动分页、表头与字段用户自定义、含/不含价格
- **数据**：JSON 导出导入、GitHub 云同步（AES-GCM 加密 + gzip + 指纹去重）
- **界面**：设计系统 `theme.css` + 6 个通用组件，电脑/手机双端响应式

### 16.4 交付与部署

| 项 | 内容 |
|---|---|
| 源码仓库 | GitHub —— `bailihongxi/wholesale-erp`（主分支 `main`） |
| 在线访问 | GitHub Pages —— `https://bailihongxi.github.io/wholesale-erp/` |
| 部署方式 | `npm run deploy <仓库地址>` → 构建 `dist` 并推到 `gh-pages` 孤儿分支 |
| 部署细节 | 见 `docs/GITHUB_PAGES.md`（`.nojekyll` / `404.html` / `base: './'`） |
| 数据存放 | 浏览器 IndexedDB，不上传服务器；云同步由用户自备仓库与 Token |

### 16.5 已知边界（非缺陷）

1. **无服务端**：数据在各终端本地，换设备需手动「同步到云端 / 从云端恢复」。
2. **云同步需自行配置**：在系统设置填 GitHub 用户名 / 仓库名 / Token / 同步口令，详见 `docs/GITHUB_PAGES.md`。
3. **示例数据里的账号密码**：老板 `13800000000 / admin123`，各角色 `13700000001~13700000004 / 123456`。
4. **权限只管模块可见性**：字段级权限（销售不见进价、库房不见价格）由既有规则固定，不随开关变化。

### 16.6 后续建议（V1.1 起）

- 多端冲突合并（当前云同步为覆盖式，按「最后上传者为准」）
- 经营报表导出 Excel / PDF
- 条码扫描枪直连录入
- 采购建议（按库存预警自动生成补货单）

---

## 十七、V1.0-2 · 第十二轮（2026-09-19）

### 17.1 需求清单

| # | 需求 | 状态 |
|---|---|---|
| 1 | 库存作业菜单并入库存管理（Hub 页）；库存预警每页 50 条 + 斑马纹 | ✅ |
| 2 | 消除页面「闪现」与加载慢 | ✅ |
| 3 | 电脑端筛选条紧凑化（搜索框放大、其余收缩，收进 1～2 行，不动手机端） | ✅ |
| 4 | 快捷图标 / 登录页头像 / 各角色头像可自定义 | ✅ |
| 5 | 库房管理「新增库房」行同样紧凑化（截图红框） | ✅ |

### 17.2 改动明细

**A. 库存合并（信息架构）**
- `src/views/stock/StockManageView.vue` 重写为 Hub 页：`PageHeader` + `SegmentedTabs`（detail / alert / flow / ops）+ `.tab-pane`，数据统一加载后传给子模块。
- 新增 `StockDetailView.vue` / `StockAlertView.vue` / `StockFlowView.vue` 三个子页。
- `src/views/warehouse/WarehouseOpsView.vue` 新增 `embedded` 属性（内嵌时隐藏页头、Tab 收小）。
- `src/router/navConfig.ts`：`ALL_MODULES` 去掉独立「库存作业」（老板 12→11 项）、`DEFAULT_ROLE_PERMS.boss` 去掉 `/warehouse`、库房角色改 `[/warehouse/home, /stock]`、手机 Tab 改为「工作台 / 库存管理」。
- `/warehouse` 及全部子路由保留，收藏夹不失效。

**B. 库存预警**
- `src/composables/usePagination.ts` 新增 `PAGE_SIZE_ALERT = 50`。
- `StockAlertView` 用 `.data-table.alert-table`（全站斑马纹）+ `TablePager` + 搜索 / 分类 / 库房 / 重置。

**C. 加载性能**
- 新增 `src/components/ui/LoadingBlock.vue` 骨架屏；7 个列表首页接入（采购单、销售单、商品档案、操作日志、客户、对账、库存管理）。
- `src/stores/product.ts` `getLowStockProducts` 改批量（`Promise.all([listAll, stockMap])`）；`src/stores/inventory.ts` `reconcileProducts` 改「一次读全表 + Map 比对」，消除 N+1。
- `src/main.ts` 新增 `prefetchRoutes()`：`load` 后 `requestIdleCallback` 预取懒加载 chunk。
- `index.html` 加 `#app-boot` 品牌占位消除白屏。

**D. 电脑端筛选条（`@media (min-width:768px)`）**
- `theme.css`：toolbar 内 `input/select/button/label` → `flex:0 0 auto; max-width:230px`；`.search-wrap/.grow` → `flex:1 1 320px`；`input[type=date]` → `max-width:158px`；`.ui-card-extra` 占满靠右。
- 控件统一 40px / `--r-sm` / `--c-border-strong`；销售单两个 toolbar 合并为一个；库房管理 `.add-row` 对齐。

**E. 品牌可配置**
- 新增 `src/utils/brand.ts`（`useBrand()`，localStorage `erp_brand_config`，缺省回落 navConfig）、`src/utils/image.ts`（`fileToSquareDataUrl`，压 96px 方图）、`src/components/BrandSettingsPanel.vue`（挂系统设置页）。
- 接入：`SideBar` / `MobileTabBar` / `AppLayout` / `LoginView` / `BossBusinessView` / `RoleMineView` / `SettingsView`。

### 17.3 测试

| 项 | 结果 |
|---|---|
| 新增 | `tests/stock-hub.test.ts` —— 19 例（预警 50 条 / 斑马纹 / Hub 四 Tab / 骨架 / 筛选条媒体查询 / 品牌配置） |
| 校准 | `stage1`、`stage3`（12→11 菜单项）、`warehouse-menu`（去掉 `/warehouse` 断言 + 手机 Tab）、`permission`（库房权限 `[/warehouse/home, /stock]`）、`history-search`（对账页骨架用 `vi.waitFor`）、`stage2`（登录副标题文案） |
| 全量 | ✅ **392 通过 / 38 文件 / 0 error** |
| 构建 | ✅ `npm run build` 0 错误（2.94s） |

### 17.4 实机验证（agent-browser）

| 项 | 结果 |
|---|---|
| 库存预警 | ✅ 示例数据 6 商品 → 5 条预警，斑马纹交替，分页「共5条 · ‹ 1 ›」 |
| 电脑端筛选条 | ✅ 采购单 / 销售单 / 客户 / 审计 / 库存明细 / 商品档案 / 财务 均 1～2 行 |
| 手机端复核 | ✅ 390 视口 `navW=390 / contentX=0 / contentW=390`，布局未变 |

### 17.5 沉淀的约束

1. 列表页一律 `.data-table` + `TablePager`，页容量用常量（如 `PAGE_SIZE_ALERT`），不写死数字。
2. 有异步数据的首屏必须挂 `LoadingBlock`，不要直接渲染空态。
3. 筛选条子项不写死宽度，交给 `@media (min-width:768px)` 紧凑规则；只有搜索框可放大。
4. 增删菜单 / 路由必跑 `tests/nav-permission.test.ts` 并同步 `MENU_SPEC.md`。
5. 图标一律走 `utils/brand.ts` 缺省回落，不在模板里硬编码 emoji。

---

## 十八、V1.0-3 · 第十三轮（2026-09-20）

### 18.1 需求清单

| # | 需求 | 状态 |
|---|---|---|
| 1 | 客户 / 供应商资料增加开票信息等公户信息（含地址、备注），并修复「编辑无法保存开票信息」 | ✅ |
| 2 | 系统设置页各小模块可回弹收缩，保持页面清洁 | ✅ |

### 18.2 改动明细

**A. 开票与对公账户资料**

- `src/types/index.ts`：`Customer` / `Supplier` 各新增 **6 个可选字段** ——
  `invoiceTitle`（开票抬头）、`taxNo`（纳税人识别号）、`invoiceAddress`（开票地址）、
  `invoicePhone`（开票电话）、`bankName`（开户银行）、`bankAccount`（对公账号）。
  **全部设为可选**，老数据不需要迁移。
- 新增 `src/utils/invoice.ts`：`INVOICE_FIELDS`、`emptyInvoice()`、`hasInvoice()`、
  `invoiceComplete()`（抬头 + 税号都填才算完整）、`invoiceOf()`（安全读取，缺省空串回落）、`invoiceSummary()`。
- 新增 `src/components/InvoiceFieldset.vue`：三列网格的六个输入框，客户与供应商共用。
- `src/views/sales/CustomersView.vue`：
  - 表单重组为 **基础资料 / 开票与打款资料 / 备注 + 经销商账号** 三组；
  - **修复缺陷**：`openEdit()` 现在完整回填（含 `address` / `remark` / 六个开票字段），
    `save()` 完整回写（原来把 `remark` 写死为空串，编辑一次就丢备注）；
  - 列表新增「开票资料」列（`.inv-badge` 绿「完整」/ 灰「未填」）；
  - 搜索框扩展为 **名称 / 联系人 / 电话 / 开票抬头 / 税号**。
- `src/views/purchase/SuppliersView.vue`：整体重做 —— 补齐**备注**字段、新增**搜索框**、
  接入 `InvoiceFieldset`；列表显示「开票资料完整」徽标 + 开票摘要 + 地址 + 备注。
- `src/utils/demoData.ts`：3 个客户与 3 个供应商补齐全套开票与对公资料。

**B. 系统设置折叠**

- 新增 `src/components/ui/CollapseCard.vue`：受控折叠卡片（`v-model`），
  **CSS 折叠而非 `v-if`（DOM 保留）**，箭头回弹动画、`aria-expanded`、键盘 Enter / 空格、
  `prefers-reduced-motion` 支持。
- `src/views/boss/SettingsView.vue`：8 个模块全部改为 `CollapseCard`，
  默认只展开「公司信息」；页头新增「全部展开 / 全部收起」；
  折叠状态存 `localStorage['erp_settings_panels']`（损坏时静默回落默认值）。
- `src/components/BrandSettingsPanel.vue`：改为 `v-model:open` 受控，
  折叠状态交给设置页统一托管，保证一键操作能一起动。

### 18.3 测试

| 项 | 结果 |
|---|---|
| 新增 | `tests/invoice-info.test.ts` —— 11 例（六字段定义 / 完整度判定 / 老数据安全 / 能存能回填 / 备注不丢 / 搜索 / 示例数据） |
| 新增 | `tests/settings-collapse.test.ts` —— 12 例（组件交互 / 键盘 / DOM 保留 / 8 模块 / 默认态 / 单选展开 / 全部展开收起 / localStorage 持久化 / 坏数据回落 / 退出登录不折叠） |
| 全量 | ✅ **419 通过 / 40 文件 / 0 error** |
| 构建 | ✅ `npm run build` 0 错误（2.68s） |

### 18.4 实机验证（agent-browser）

| 项 | 结果 |
|---|---|
| 设置页折叠 | ✅ 默认 8 个模块仅「公司信息」展开（bodyH=206，其余 =0） |
| 页面高度 | ✅ **4118px（全展开）→ 810px（默认）→ 588px（全收起）** |
| 一键操作 | ✅ 「全部展开 / 全部收起」按钮生效，`localStorage` 同步写入全部 8 个键 |
| 客户开票资料 | ✅ 列表「完整」徽标；编辑表单六字段回填正确、分组清晰；清除商品表后重播示例数据即落库 |
| 供应商开票资料 | ✅ 「开票资料完整」徽标 + 抬头 / 税号 / 开户行账号摘要 + 地址 + 备注 |

## 十九、V1.0-4 · 第十四轮（2026-09-19）

### 19.1 需求清单

| # | 需求 | 状态 |
|---|---|---|
| 1 | 搜索框输入信息后，默认放大镜未自动隐藏、遮挡输入文字 | ✅ |
| 2 | 财务管理 → 经营报表排版左右未对齐；库存预警使用 50 条/页分页 + 斑马纹防差行 | ✅ |
| 3 | 财务「登记收款」行确认 / 取消按钮汉字挤在一起（竖排） | ✅ |
| 4 | 库存管理 →「库存作业」Tab 移到本行按钮最前端 | ✅ |
| 5 | 系统内所有「返回」「取消」按键统一橘红底色 | ✅ |

### 19.2 改动明细

- **全局设计系统** `src/styles/theme.css`：
  - 新增 `--c-amber: #f97316` / `--c-amber-hover: #ea580c` 橘红令牌；
  - 原生控件基线 `:where(input:not(...):not(...):not(...))` 把类型排除整体写进 `:where()`，**特异性降为 0**，不再压过组件 scoped 单类规则（修搜索框放大镜压字 + 金额框吃满整行）；
  - `.data-table` 斑马纹加强对比（奇数 `#fff` / 偶数 `#f4f7fc`）；
  - 新增 `.ui-btn-cancel`（橘红实底白字，定义在 `.ui-btn` 之后靠源码顺序覆盖）；
  - `.tab-pane > div:is(...)` 兜底纳入 `.reports-page` 等，消除宽屏逐层缩进。
- **搜索框** `src/components/SearchInput.vue`：输入后 `🔍` 加 `.faded` 淡出；`.search-field` 改 `.search-wrap .search-field` 双类选择器保住左内边距。
- **经营报表** `src/views/boss/BossReportsView.vue`：`.reports-page` 去 `max-width:1100` 改 `width:100%`；低库存预警内嵌 `<TablePager>` 50 条分页 + 斑马纹（序号 / 缺口列）。
- **财务对账** `src/views/finance/ReconcileView.vue`：`.edit-row .amt-input{width:120px}` 双类保宽、确认 / 取消按钮 `white-space:nowrap`；取消按钮套橘红。
- **库存管理** `src/views/stock/StockManageView.vue`：`tabOptions` 把「库存作业」移到第一位（`TAB_KEYS` 同步）。
- **全站取消 / 返回橘红**：`PageActions.vue` `.pa-cancel`、`CustomersView`/ `SuppliersView` `.cancel`、`PrintPreview` `.pp-btn.ghost`、`LocationsView` `.link-btn.muted`、`UsersManageView` `.ui-btn.ui-btn-cancel`、`ProductListView` 弹窗 `.ghost-btn.dismiss` 全部橘红实底白字。
- 新增回归测试 `tests/ui-polish-v14.test.ts`（9 例）。

### 19.3 测试

| 项 | 结果 |
|---|---|
| 新增 | `tests/ui-polish-v14.test.ts` —— 9 例（放大镜淡出 + 基线特异性 / 报表对齐 + 预警分页斑马纹 / 金额框 nowrap / Tab 顺序 / 橘红令牌与各页取消按钮） |
| 全量 | ✅ **428 通过 / 41 文件 / 0 error** |
| 构建 | ✅ `npm run build` 0 错误 |

### 19.4 实机验证（agent-browser，桌面 1600 / 移动 390）

| 项 | 结果 |
|---|---|
| 搜索框放大镜 | ✅ 输入后淡出、文字不再被压 |
| 经营报表 | ✅ 左右对齐（与父级同宽）；预警分页 50 条 + 斑马纹 |
| 财务登记收款 | ✅ 金额框固定 120px，确认 / 取消按钮横排不挤字、取消橘红 |
| 库存管理 Tab | ✅ 库存作业排第一位 |
| 取消 / 返回 | ✅ 各页取消 / 返回按钮均为橘红实底白字 |

### 18.5 踩坑记录

1. **`CollapseCard` 初版模板里写成了 `open`，而 prop 是 `modelValue`** —— Vue 只给 `[Vue warn]`，
   不中断构建，表现是**所有模块永远收起、点不开**。由挂载测试第 1 例抓出，
   已改为 `computed(() => props.modelValue)` + 独立 `toggle()`。
   **结论：模板引用不存在的属性不会报错，这类组件必须靠挂载测试断言真实渲染结果。**
2. **受控组件的测试不能直接断言点击后 props 变化** —— 没有父级 `v-model` 回写时 props 不会变，
   要断言派发的事件（`emitted('update:modelValue')`）或用 `setProps` 模拟父级回写。
3. **`seedDemoData()` 有「已有商品就跳过」的保护** —— 实机验证时若库里还有上一轮的旧记录，
   直接调用会静默返回 `ok:false`，看到的是旧数据（表现为「开票字段没生效」）。
   正确做法：先 `clearBusinessData()` 并清 `customers` / `suppliers`，再播种。

### 18.6 沉淀的约束

1. 让用户填的表单，保存时必须**完整回写全部字段**，编辑时必须**完整回填**；改一个字段不许把别的清空。
2. 表单字段超过 6 个应**分组**（基础资料 / 开票与打款资料 / 备注），不要平铺一大片。
3. 开票相关判定统一走 `utils/invoice.ts`，不在页面里各写一套。
4. 可折叠模块一律用 `ui/CollapseCard.vue`，**CSS 折叠不用 `v-if`**（`v-if` 会让测试拿不到节点、也会清掉未提交的表单）。
5. 页面级折叠状态由页面统一持有并配「全部展开 / 全部收起」，子组件用 `v-model:open` 接入。

### 18.7 交付

| 项 | 结果 |
|---|---|
| 版本 | `src/version.ts` → **V1.0-3**；`package.json` → `1.0.0-3` |
| 文档 | `PRD.md` V1.8（新增第 20 节）、`DEV_PLAN_V2.md`（第十八章）、`MENU_SPEC.md` 同步 |
| 仓库 | 提交 + tag `v1.0-3` 推送 `main`；GitHub Pages 重新部署 |

---

## 二十、V1.0-5 · 第十五轮（2026-09-20）：全站列表统一 20 条/页 + 斑马纹

### 20.1 需求与确认

- 需求：**系统中所有列表统一 20 条/页分页 + 斑马纹**；点名清单：商品档案、采购单、销售单、首付款（收付款）列表、
  操作日志、员工管理、客户管理、供应商列表。
- 确认（用户选择）：**全部统一 20 条**（商品档案 100 → 20、库存预警 50 → 20，不留例外）。

### 20.2 改动明细

| 文件 | 改动 |
|---|---|
| `composables/usePagination.ts` | `PAGE_SIZE_PRODUCT / PAGE_SIZE_LIST / PAGE_SIZE_ALERT` 三者统一 20；文件头注释同步 |
| `views/purchase/PurchaseOrdersView.vue` | 接入 `usePagination` + `TablePager`；表格改 `data-table`；手机端卡片加 `zebra-list` |
| `views/sales/SalesOrdersView.vue` | 同上 |
| `views/sales/CustomersView.vue` | 客户列表接入分页；卡片补 `zebra-list` |
| `views/purchase/SuppliersView.vue` | 供应商卡片列表接入分页 + 斑马纹 |
| `views/boss/AuditLogView.vue` | 操作日志接入分页；去私有 `border-collapse`；卡片补斑马纹 |
| `views/boss/UsersManageView.vue` | 员工列表接入分页 |
| `views/finance/FinanceOpsView.vue` | 资金流水接入分页；**合计仍按全部数据**汇总 |
| `views/finance/LedgerView.vue` | 「记一笔」明细接入分页 |
| `views/finance/ReconcileView.vue` | 应收应付 / 收付款各一个独立 pager（切 Tab 各记各的页码）；卡片补斑马纹 |
| `views/warehouse/LocationsView.vue` | 库房列表接入分页；序号改 `startIndex + i`；「默认」徽标只在第 1 页首行 |
| `views/stock/StockFlowView.vue` | 出入库流水接入分页；取数上限改 `FLOW_FETCH_LIMIT` |
| `views/warehouse/{Inbound,Outbound,Count,Returns,Transfer}View.vue` | 手机端卡片列表补 `zebra-list` |
| `styles/theme.css` | `.zebra-list` 错行色由 `--c-surface-alt(#fafbfd)` 提升为 `#eef2f9`（与表格同档，肉眼可辨） |

### 20.3 测试

- 新增 `tests/app-icon-install.test.ts` 中的分页章节：12 类列表的接线校验、卡片斑马纹校验、分页切页（50 条 → 3 页）。
- 校准过期断言：`tests/product-batch.test.ts`（100 → 20）、`tests/stock-hub.test.ts`（50 → 20，翻页行数 50/10 → 20/20/20）。
- 全量：**462 通过 / 42 文件 / 0 error**；`npm run build` 0 错误。

### 20.4 实机验证（agent-browser，1440 视口）

| 页面 | 实测结果 |
|---|---|
| 商品档案 | 20 行 · 共 51 条 第 1/3 页 · 斑马纹 `#fff | #eef2f9` |
| 采购单 / 销售单 | 20 行 · 共 29 条 第 1/2 页 · 斑马纹 OK |
| 客户 / 供应商 | 20 条 · 共 29 条 第 1/2 页（供应商为卡片列表，斑马纹 OK） |
| 操作日志 / 员工 | 20 行 · 共 27 条 第 1/2 页 · 斑马纹 OK |
| 财务·资金流水 / 记一笔 / 收付款 | 20 行 · 共 56 / 26 / 30 条 · 斑马纹 OK |
| 库存·出入库流水 / 库存预警 | 20 行 · 共 42 / 50 条 · 斑马纹 OK |
| 手机端（390 视口）销售单 | 20 张卡片 · 共 29 条 第 1/2 页 · 卡片斑马纹 OK |

### 20.5 交付

| 项 | 结果 |
|---|---|
| 版本 | `src/version.ts` → **V1.0-5**（与第十六轮同批发布）；`package.json` → `1.0.0-5` |
| 文档 | `PRD.md` V2.0（第 21 节）、本文件第二十章、`MENU_SPEC.md` 同步 |

---

## 二十一、V1.0-5 · 第十六轮（2026-09-20）：应用图标自定义 + 发送到桌面

### 21.1 需求与确认

- 需求：**可以更改 icon，并把改好的图标发送到桌面做快捷方式**。
- 确认（用户选择）：**完整方案** —— 自定义图标 + PWA 安装到桌面 + 离线可用。

### 21.2 改动明细

| 文件 | 职责 |
|---|---|
| `src/utils/appIcon.ts`（新增） | canvas 渲染 32/180/192/512 PNG；替换 `link[rel=icon]`、`apple-touch-icon`；重建 manifest（Blob） |
| `src/utils/installApp.ts`（新增） | `beforeinstallprompt` 捕获与 `promptInstall()`；平台判定 `platformOf` + 分平台步骤 `installSteps`；生产环境注册 SW |
| `src/components/AppIconPanel.vue`（新增） | 设置页面板：预览（含标签页模拟）、emoji/文字/图片三种来源、6 种底色、应用/恢复默认、发送到桌面 + 手动步骤 |
| `src/utils/brand.ts` | 新增 `appIcon`（builtin/text/image）与 `appIconBg`，含旧配置兼容 |
| `public/manifest.webmanifest`（新增） | PWA 清单：standalone、192/512/maskable、三个快捷入口 |
| `public/sw.js`（新增） | Service Worker：导航 network-first、静态资源 stale-while-revalidate、缓存名 `erp-shell-v5` |
| `public/icons/*.png`（新增 5 张） | 默认图标；由 `scripts/gen-icons.py` 纯标准库（zlib）生成 |
| `index.html` | 补 favicon / apple-touch-icon / manifest / theme-color / 可添加到主屏幕 meta |
| `src/main.ts` | 启动即套用已保存图标；`initInstall()`；生产注册 SW |
| `src/views/boss/SettingsView.vue` | 新增第 9 个折叠模块「应用图标与桌面快捷方式」 |
| `src/vite-env.d.ts`（新增） | 补 `vite/client` 类型（`import.meta.env`） |
| `tests/setup.ts` | 统一把 canvas `getContext` 桩为 `null`（jsdom 缺 canvas，会污染「渲染无报错」断言） |

### 21.3 测试

新增 `tests/app-icon-install.test.ts`（33 例）：分页章节 + 图标工具 + 安装能力 + PWA 静态资源 + 面板渲染。
全量 **462 通过 / 42 文件 / 0 error**；`npm run build` 0 错误。

### 21.4 实机验证（agent-browser）

| 项 | 实测结果 |
|---|---|
| 标签页图标 | 选 🏪 后 `32×32` / `192×192` 的 `link[rel=icon]` href 变为 `data:image/png;base64,…` |
| 苹果主屏图标 | `apple-touch-icon` 同步变为 dataURL |
| manifest | `link[rel=manifest]` 变为 `blob:`；`start_url / scope` 已绝对化 |
| 恢复默认 | 三个 link 全部回到 `public/icons/*.png` |
| 安装状态 | Chrome 显示「可一键安装」（说明 manifest + SW 满足安装条件） |
| Service Worker | `registered:true, scope=http://localhost:4173/, active:true, controller:true`；缓存 `erp-shell-v5` 命中 110 项 |
| 断网打开 | 拦截全部请求后刷新，登录页仍完整渲染（缓存兜底） |

### 21.5 踩坑

1. **页面 scoped 类名不能跨组件用**：面板初版用了 `SettingsView` 的 `.primary-btn / .ghost-btn / .btn-row`，
   独立组件里完全没有样式 → 按钮文字挤在一起。改用全局 `ui-btn / ui-btn-primary / ui-toolbar / ui-hint`。
   > **新组件只能用 `theme.css` 的全局类。**
2. **图标要启动即套用**：只在设置页替换 favicon，用户刷新后会掉回默认图（像没保存）。
3. **动态 manifest 必须绝对化 URL**：Blob manifest 的相对路径解析不了。
4. **无 canvas 要降级**：拿不到上下文时保持原图标，别把图标弄空白。
5. **SW 只在生产注册**：开发期与 HMR 冲突。
6. **jsdom 无 canvas**：会往虚拟控制台抛 "Not implemented"，干扰「页面渲染无报错」类断言。

### 21.6 交付

| 项 | 结果 |
|---|---|
| 版本 | `src/version.ts` → **V1.0-5**；`package.json` → `1.0.0-5` |
| 文档 | `PRD.md` V2.0（第 22 节）、本文件第二十一章、`MENU_SPEC.md` 同步 |
| 仓库 | 提交 + tag `v1.0-5` 推送 `main`；GitHub Pages 重新部署 |

---

## 二十二、V1.0-6 · 第十七轮（2026-09-20）：库存概况卡片下移 + 默认打开库存作业

### 22.1 需求

> ① 库存管理页面头部的 商品 SKU / 库存总量 / 库存金额（进价）/ 库存预警 卡片，移到「库存明细」模块内、搜索框**上方**；
> ② 「库存管理」把「**库存作业**」作为默认打开页面。

### 22.2 改动

| 文件 | 改动 |
|---|---|
| `src/views/stock/StockManageView.vue` | 移除页头 `.stat-row` 及其 `stats` / `money` 计算与权限判断（这些随卡片一起下移）；`normalize()` 兜底值 `'detail'` → **`'ops'`**；新增 `watch(() => route.query.tab, …)` 让 URL 始终是 Tab 的唯一事实源 |
| `src/views/stock/StockDetailView.vue` | 顶部（`.toolbar` 之前）新增 `.ui-stat-grid` + 四张 `ui/StatCard.vue`；`stats` 由本组件按 `products / stock` props 计算 |

### 22.3 关键决策

1. **统计口径跟着模块走**：`stats` 原本按 `products / stock` 算，与明细同源；下移后由 `StockDetailView` 自算，
   父页不再持有重复的 `stats`（少一处重复计算，也少一处 props 传递）。
2. **「库存金额（进价）」卡的权限收紧**：该卡按 `purchasePrice` 汇总，原来的可见性用的是 `canSeeAnyPrice`
   （只有库房被排除）→ **销售也能看到，等于泄露进价**。改为 `canSeePurchasePrice`（老板 / 采购 / 财务），
   与明细表「进价」列一致。
3. **默认 Tab 变更会牵动老用例**：原有多组用例隐含「进 `/stock` 就是库存明细」
   （`search-and-merge` 整组、`demo-data` 示例数据、`stage6` 库房价格权限、`stock-hub` 默认落点），
   统一显式带上 `?tab=detail` —— 属**产品行为变更导致的断言过期**，按新规则校准，断言强度不降；
   同时新增三条断言（默认落库存作业 / 卡片在搜索框上方 / 销售与库房看不到金额卡）。

### 22.4 验证

全量 **473 通过 / 43 文件 / 0 error**；`npm run build` 0 错误。
新增 `tests/stock-cards-v17.test.ts`（8 例：卡片位置 + 默认 Tab + 版本一致性）。
实机（老板账号）：直接进 `/stock` 落在**库存作业**；`?tab=detail` 四张卡在**搜索框上方**；
手机端 390 视口卡片 2×2。

### 22.5 交付

| 项 | 结果 |
|---|---|
| 版本 | `src/version.ts` → **V1.0-6**；`package.json` → `1.0.0-6` |
| 文档 | `PRD.md` V2.1（第 23 节）、本文件第二十二章、`MENU_SPEC.md` 第十六章 同步 |
| 仓库 | 提交 + tag `v1.0-6` 推送 `main`；GitHub Pages 重新部署 |

---

## 二十三、V1.0-7 · 第十八轮（2026-09-20）：登录系统重新设计

### 23.1 需求

> 现在需要更新及合理重新设计登录系统，目前我只接能在界面上看见老板的账号和密码，而且账号不能只使用电话，员工信息也必须在拓展更新一部分。

① **登录安全加固**：登录页不再显示任何账号密码；密码明文 → 加盐哈希；连续 5 次失败锁定 5 分钟；登录态 7 天过期、活跃滑动续期。
② **账号体系扩展**：「用户名 / 手机号」双通道登录；员工档案扩展（工号、登录名、部门、职位、入职日期、备注、头像）；自助改密 + 老板重置密码（返回一次性临时密码）。

### 23.2 改动

| 文件 | 改动 |
|---|---|
| `src/utils/password.ts`（新增） | PBKDF2-SHA256 加盐哈希 + 临时密码 |
| `src/utils/loginGuard.ts`（新增） | 失败锁定、会话（带 `kind`）、7 天续期、记住账号 |
| `src/utils/account.ts`（新增） | 登录名/手机号规则、部门/职位建议 |
| `src/types/index.ts` | `User` 增 `username/employeeNo/dept/position/joinDate/remark/avatar/lastLoginAt` |
| `src/db/index.ts` | v7 索引+迁移；`initDefaultAdmin` 存哈希+档案；`nextEmployeeNo` |
| `src/stores/user.ts` | 双通道 `login`、锁定、哈希升级；`createUser/updateUser/resetPassword/changePassword/usesDefaultPassword` |
| `src/views/auth/LoginView.vue` | 账号框（用户名/手机号）、显隐密码、记住账号；移除明文凭证 |
| `src/views/boss/UsersManageView.vue` | 部门/状态筛选、三组表单、重置密码临时密码弹窗 |
| `src/components/ChangePasswordModal.vue`（新增） | 自助改密弹窗 |
| `BossHomeView / BossMineView / RoleMineView` | 内置密码提醒、自助改密、退出登录 |

### 23.3 关键决策

1. **会话带 `kind`**：原 `saveSession(userId)` 只存裸 id，员工表与经销商表 id 可能撞车 → 刷新后以他人身份登录；改为 `{id, kind, expiresAt}` 按 kind 取表。
2. **老明文渐进迁移**：`verifyPassword` 对明文返回 `needsUpgrade`，登录成功后静默重写为哈希，员工无感。
3. **哈希不可逆 → 只能重置**：老板无法查看员工密码，`resetPassword` 返回只出现一次的临时密码，关窗后不可再查。
4. **登录页凭证移除**：内置账号（`admin` / `admin123`）仅写进《使用说明》，登录页文本不再含任何账号密码。

### 23.4 验证

全量 **481 通过 / 44 文件全绿**，`npm run build` 0 错误。
新增 `tests/auth-v18.test.ts`（哈希 / 锁定 / 会话 / 双通道 / 重置密码）。

### 23.5 交付

| 项 | 结果 |
|---|---|
| 版本 | `src/version.ts` → **V1.0-7**；`package.json` → `1.0.0-7` |
| 文档 | `PRD.md` V2.2（第 24 节）、本文件第二十三章、`MENU_SPEC.md` 第十八轮标注 同步 |
| 仓库 | 提交（含 V1.0-6 未提交改动） + tag `v1.0-7` 推送 `main`；GitHub Pages 重新部署 |

---

**文档结束。** 每完成一个阶段，在对应阶段后打勾并记录实际完成日期。


## 二十四、V1.0-8 · 第十九轮（2026-09-20）：打印模板头部 + 记一笔关联单据 + 阻塞弹窗

### 24.1 需求

> ① 所有打印模板头部两行改一行：公司抬头（家电批发）与单据标题（入库单等）同行、抬头居左，标题居中，抬头字号不变；
> ② 「记一笔」新增关联单号：费用可关联入库单 / 出库单 / 退换货单 / 采购单 / 销售单 / 调拨单 / 盘点单，便于追踪费用来源；
> ③ 全站弹窗改阻塞式：点遮罩不再关闭（避免误触丢失编辑内容），按 ESC 关闭。

### 24.2 改动明细

| 文件 | 改动 |
|---|---|
| `src/utils/printTemplate.ts` | 头部 `.hd-row`：`grid-template-columns: 1fr auto 1fr`，`hd-co` 居左（字号不变）、`hd-title` 绝对居中；副标题/地址电话仍在下行 |
| `src/utils/ledger.ts` | 新增 `LEDGER_LINK_TYPES`（7 类单据 + 前缀）与 `linkTypeLabel()` |
| `src/types/index.ts` | `LedgerEntry` 增 `linkDocType` / `linkDocNo` |
| `src/stores/finance.ts` | `addLedger` 落库关联字段；操作日志带关联单号 |
| `src/views/finance/LedgerView.vue` | 关联单据选择器（类型 select + 单号 datalist 候选）、按类型加载单据号、列表新增「关联单据」列 |
| `src/views/finance/FinanceOpsView.vue` | 资金流水摘要追加「关联类型+单号」 |
| `ChangePasswordModal / PrintPreview / UsersManageView / ProductListView` | 移除 `@click.self` 关闭，改为阻塞；加 window keydown ESC 监听（保存/导入中不响应），卸载时移除 |

### 24.3 关键决策

1. **标题绝对居中**：公司抬头和单据标题在同一行，若用「左+中」两列，标题会偏左；改三列 grid（`1fr auto 1fr`）让中列真正落在页面中线，左右留白对称。
2. **抬头字号不变**：`hd-co` 仍用纸张对应的 `co` 字号（A5 16px / A4 19px），仅从独行改为行内居左。
3. **关联单据不强校验单号存在**：允许手动输入任意单号（用户可能想关联历史纸质单），datalist 只做候选提示；类型切换时清空单号并重新加载候选。
4. **ESC 不打断进行中任务**：保存 / 导入中按 ESC 不关闭，避免关掉正在提交的表单；纯展示类弹窗（重置密码结果）随时可 ESC。
5. **不迁移数据库**：`linkDocType/linkDocNo` 不加索引，直接随记录存储；老记录无字段，读取为空字符串，不破坏现有数据。

### 24.4 测试与实机验证

- 全量 **500 通过 / 45 文件**，`npm run build` 0 错误；新增 `tests/round19.test.ts`（13 例）。
- 实机（老板账号）：
  - 采购单打印预览：头部「家电批发」居左、「采购单」居中同一行（OCR 坐标同 y）；
  - 预览弹窗点击遮罩不关闭，按 ESC 关闭；
  - 记一笔选「采购单」类型 → 输入 CG20260920-640 → 保存后流水显示「采购单 CG20260920-640」。

### 24.5 交付

| 项 | 结果 |
|---|---|
| 版本 | `src/version.ts` → **V1.0-8**；`package.json` → `1.0.0-8` |
| 文档 | `PRD.md` V2.3（第 25 节）、本文件第二十四章、`MENU_SPEC.md` 第十九轮标注 同步 |
| 仓库 | 提交 + tag `v1.0-8` 推送 `main`；GitHub Pages 重新部署 |

---

## 二十五、V1.0-9 · 第二十轮（2026-09-20）：赠品管理 + 报价单

### 25.1 需求

> ① **赠品**：采购时厂家随货送赠品、销售时给经销商 / 客户送赠品——赠品行走正常单据（验货入库、拣货出库、库存可查），金额计 0、不参与合计，打印标「赠品」；
> ② **报价单**：客户询价给临时报价单（BJ 前缀），订单确定后一键转销售单——明细价格带过去、散客自动建档、双向可查、不能重复转换；报价单收进「销售管理」页内 Tab，不新增独立菜单。

### 25.2 改动明细

| 文件 | 改动 |
|---|---|
| `src/types/index.ts` | `PurchaseOrderItem` / `SaleOrderItem` 增 `isGift?`；新增 `QuoteOrder` / `QuoteOrderItem` 接口 |
| `src/db/index.ts` | 库升级 v8：新增 `quoteOrders` / `quoteOrderItems` 表（索引：orderNo / customerId / status / quoteDate / salesId、quoteOrderId / productId） |
| `src/utils/orderNo.ts` | 新增 `genQuoteNo()`：BJ + yyyyMMdd + 3 位随机 |
| `src/utils/audit.ts` | `AUDIT_ACTIONS` 增 `QUOTE_CREATE` / `QUOTE_DELETE` / `QUOTE_CONVERT` |
| `src/stores/purchase.ts` | `createOrder` 支持 isGift（价格 0、不计 totalAmount）；`inbound` 流水备注附加「赠品」 |
| `src/stores/sales.ts` | `createOrder` 支持 isGift（按模式取价时赠品恒 0）；`outbound` 流水备注附加「赠品」 |
| `src/views/purchase/PurchaseCreateView.vue` | 明细表新增「赠品」列（勾选后进价禁用、金额显示 🎁 赠品）、合计过滤赠品 + 含赠品件数 |
| `src/views/sales/SalesCreateView.vue` | 同上；切批发/零售价时跳过赠品行 |
| `src/views/purchase/PurchaseOrderDetailView.vue` / `src/views/sales/SaleOrderDetailView.vue` | 明细行「🎁 赠品」徽标、单价「—」/金额「赠品」、合计提示含赠品件数、打印 isGift 透传 |
| `src/utils/printTemplate.ts` | `PrintItem` 增 `isGift?`；打印单价列「—」/金额列「赠品」 |
| `src/stores/stockDoc.ts` / `src/views/warehouse/StockDocDetailView.vue` | `StockDocItem` 增 `isGift?`（按流水备注「赠品」标记）；出入库单打印识别赠品行 |
| `src/stores/quotes.ts` | **新建**：createQuote / listQuotes / getQuote / getQuoteItems / removeQuote / convertToSale（散客自动建档、回填销售单号、防重复转换） |
| `src/views/sales/QuotesView.vue` | **新建**：列表 / 新建 / 详情三态，页内 Tab「销售单 / 报价单」，详情「转为销售单」+ 打印 + 删除 |
| `src/views/sales/SalesOrdersView.vue` | 页头下新增「销售单 / 报价单」Tab 切换 |
| `src/router/index.ts` | 新增路由 `/sales/quotes`（sales + boss） |
| `tests/round20.test.ts` | 新增 7 例（采购赠品入库 / 销售赠品出库 / 打印赠品 / 报价单创建与单号 / 散客转单建档 / 老客户不重复建档 / 禁止重复转换） |

### 25.3 要点与约定

- 赠品行：`isGift=true` → price=0、subtotal=0、不入 totalAmount；库存照常增减；出入库流水备注含「赠品」；打印 price='—' / amount='赠品'。
- 报价单：`genQuoteNo()` = BJ+日期-3位随机；status `draft/sent/converted/void`；转单字段 `convertedSaleOrderId` / `convertedSaleNo` 回填；散客 `customerId=0` + `customerName`，转单时自动建档（sales store 已有 createCustomer 同表直写）。
- 赠品台账（按供应商 / 客户汇总）按方案留作第二步，本轮不做。

### 25.4 验收

| 项 | 结果 |
|---|---|
| 测试 | 全量 **507 通过 / 46 文件**（含新增 `tests/round20.test.ts` 7 例） |
| 构建 | `npm run build` 0 错误 |
| 版本 | `src/version.ts` → **V1.0-9**；`package.json` → `1.0.0-9` |
| 文档 | `PRD.md` V2.4（第 26 节）、本文件第二十五章、`MENU_SPEC.md` 第二十轮标注 同步 |
| 仓库 | 提交 + tag `v1.0-9` 推送 `main`；GitHub Pages 重新部署 |


---

## 二十六、V1.0-10 · 第二十一轮：系统内置账户 + 记一笔关联框修复（2026-09-20）

### 26.1 背景

老板提出：① 建立一个不可改、不可删、最高权限的系统账户 `hawsystem / admina1b22c333`，以后用它登录；② 修复记一笔「关联订单」单号选择框过窄（截图红框），号码看不全。

### 26.2 改动清单

| 文件 | 改动 |
|---|---|
| `src/types/index.ts` | `User` 增 `system?: boolean`（系统内置账户标记） |
| `src/db/index.ts` `initDefaultAdmin()` | 老库自动补建 hawsystem（E000、boss、system=true、PBKDF2 哈希），幂等 |
| `src/stores/user.ts` | `updateUser` / `resetPassword` / `changePassword` / `setUserStatus` 四处拦截 `user.system` |
| `src/views/boss/UsersManageView.vue` | 系统行姓名旁「系统」徽标；编辑按钮置灰；重置密码/停用 `v-if` 排除 system |
| `src/views/finance/LedgerView.vue` | 关联单据字段加 `link-field`（grid-column: 1/-1 独占整行）；列宽 176px + `minmax(0,1fr)`；移动端仍堆叠 |
| `tests/round21.test.ts` | **新建** 9 例 |
| `tests/phase1.test.ts` | 旧断言 users.length 2→3（新增系统账户） |
| 版本 / 文档三件套 | V1.0-10 / 1.0.0-10 |

### 26.3 要点与约定

- 系统账户保护分两层：UI 置灰（防误点）+ store 拒绝（防绕过）；密码锁死初始值，老板也无法重置。
- `initDefaultAdmin()` 在 `login()` 内调用，老用户下次任意登录即自动补建 hawsystem，无需迁移脚本。
- 记一笔框是纯样式修复，不影响关联数据结构（linkDocType/linkDocNo 不变）。

### 26.4 验收

| 项 | 结果 |
|---|---|
| 测试 | 全量 **517 通过 / 47 文件**（含新增 round21 9 例） |
| 类型 | `npx vue-tsc -b` 0 错误 |
| 版本 | `src/version.ts` → **V1.0-10**；`package.json` → `1.0.0-10` |
| 文档 | `PRD.md` V2.5（第 27 节）、本文件第二十六章、`MENU_SPEC.md` 第二十一轮标注 同步 |


---

## 二十七、V1.0-11 · 第二十二轮：手机端三项优化（2026-09-20）

### 27.1 改动
| 文件 | 改动 |
|---|---|
| `src/views/boss/ProductListView.vue` | 手机卡片文字进/批/零/库/警；搜索 250ms 防抖（kwDebounced）；防抖后回第一页 |
| `src/stores/product.ts` | `stockMap()` 多库位库存汇总（旧实现互相覆盖） |
| `src/views/boss/UsersManageView.vue` | 表外包 `.table-scroll` 横向滚动；手机端 min-width 660、隐藏 col-mod 列；v-if 移到容器上保持 v-else 相邻 |
| `tests/stage4.test.ts` | 搜索用例补 sleep(350)；预警断言改「警」 |
| `tests/stock-cards-v17.test.ts` | 版本断言 V1.0-12 |

### 27.2 验收
| 项 | 结果 |
|---|---|
| 测试 | 全量 **517 通过 / 47 文件** |
| 版本 | V1.0-11 / 1.0.0-11 |

## 二十八、第二十三/二十四轮（V1.0-12）：手机端体验 + 库存深度优化

| 文件 | 改动 |
|---|---|
| `src/components/ProductPicker.vue` | 型号/规格列加 `col-spec`，手机端隐藏；空行 `colspan` 改用 `colCount` |
| `src/styles/theme.css` | 新增 `@media (max-width:767px)`：筛选条换行 + 搜索独占一行；`.ui-page-main` 隐藏（页头去重） |
| `src/views/boss/UsersManageView.vue` | `.modal` 限高 90vh + `.modal-body` 滚动，修复手机端长表单溢出 |
| `src/views/sales/QuotesView.vue` | 加批发/零售价切换（`priceMode` / `switchMode`），picker 跟随 |
| `src/stores/inventory.ts` | `syncLocationStock()` N+1 → 批量读（2 次 `toArray`） |
| `tests/round24-mobile.test.ts` | 新增 5 例回归 |

### 28.1 验收
| 项 | 结果 |
|---|---|
| 测试 | 全量 **522 通过 / 48 文件** |
| 版本 | V1.0-12 / 1.0.0-12 |
| 实测（390×844） | 调拨首屏 245ms、盘点 46ms（原 5 秒+）；员工弹窗 759px 高可滚动 |

---

## 二十九、V2.0-2（2026-09-21）：云数据库迁移 + 工作台标题动态化

| 文件 | 改动 |
|---|---|
| `src/db/supabaseClient.ts` | 新增。Supabase client + `USE_CLOUD` 开关（vitest 环境强制 false） |
| `src/db/cloudDb.ts` | 新增。`CloudTable` 适配 Dexie 常用 API，`createCloudDb()` 输出与 Dexie db 同构对象 |
| `src/db/index.ts` | `USE_CLOUD=true` 时导出 cloudDb，否则导出本地 Dexie（保底） |
| `src/utils/cloudMigrate.ts` | 新增。24 张表批量 upsert 到 Supabase，可重复执行 |
| `src/views/boss/SettingsView.vue` | 数据备份区加「☁️ 迁移到云端」按钮 |
| `src/components/AppLayout.vue` | 工作台标题按当前用户动态显示（system → 管理员工作台） |
| `src/components/MobileTopNav.vue` | 手机端顶栏同步动态标题 |
| `supabase/schema.sql` | 24 张表 DDL + 索引 + anon 授权（BY DEFAULT identity） |
| `src/version.ts` / `package.json` | V2.0-2 / 2.0.0-1 |
| 测试 | 版本断言同步 V2.0-2；其余 522 全绿 |

### 29.1 验收
| 项 | 结果 |
|---|---|
| 测试 | 全量 **522 通过 / 48 文件** |
| 版本 | V2.0-2 / 2.0.0-1 |
| 实机 | hawsystem 登录标题「管理员工作台」；商品读云端正常；新增写云端成功 |
| 部署 | main 已推送（df1efd2），gh-pages 已部署 |

### 29.2 下阶段待办（第二阶段 P1）
1. Supabase Realtime 实时多人同步（A 开单 B 自动刷新）。
2. Supabase Auth 接管员工登录 + RLS 行级权限（销售只看自己的单）。
3. 审批流、多档客户价、业务员提成、赊销额度（业务深化）。


---

## 第30节 V2.0-2 完成记录（2026-09-21）

### 已完成
1. Supabase 1000 行限制修复：所有查询自动并行分页
2. 商品导入批量优化：500 条/批，insert 直接返回 id
3. CloudTable 补全方法：clear/count/bulkGet/filter/bulkDelete invalidate
4. 导入结果显示被跳过行原因
5. 预采询价归到采购管理页内 Tab
6. 扩大云端缓存范围
7. 版本号规则确认：V2.0-N 每次 +1
8. 522 个测试全部通过


## 三十、V2.0-3（2026-09-21）：性能优化（服务端分页 + 缓存预热）

- 根因：新加坡 Supabase 节点高延迟（1000 行商品 ≈2.4s），几乎所有列表进页面就全表 `toArray()`，且 `count()/orderBy()` 不走缓存重复请求。
- 改动：`cloudDb.ts` 新增 `queryPage` / `count` 缓存 / `distinct` / `warmUp`；`user.ts` 登录后 `warmUp()`；`product.ts` 新增 `listPage` / `distinctCategories`（云端走 `queryPage`，本地走内存过滤）；`ProductListView.vue` 服务端分页重写。
- 验证：`npm run build` 0 错误；`npx vitest run` 522 全绿。


## 三十一、V2.0-4（2026-09-21）：列表服务端分页推广

- 目标：把商品档案（V2.0-3）验证过的服务端分页范式推广到其余大列表，消除「进页面就全表 toArray()」。
- 新增 `src/db/serverPage.ts`：`serverPage(table, opts)` 云端调 `queryPage`、本地 `toArray()` + `localApplyFilters` 后切片，双模语义一致。
- `useServerPager` 增加 `loading` + `onMounted` 自动首拉；接口与 `usePagination` 完全对齐，`pager.paged.value` / `pager.total.value` / `TablePager` 用法不变。
- 已转换：销售单、采购单、销售报价单、预采询价单、客户档案、供应商档案、出入库流水、操作日志。
- 踩坑：模板里 `pager.loading` 是嵌套 ref、Vue **不会自动解包**，必须写 `pager.loading.value`，否则骨架永驻、列表永远不显示（曾一次红 12 个用例）。
- 未转换（原因记录）：出入库单据 / 库存明细 / 库存预警 / 经营报表 / 财务流水 / 对账为多表聚合结果，`queryPage` 不支持 group by；`ProductPicker` 由调用方传 rows，需连带改所有下单页。
- 验证：`npm run build` 0 错误；`npx vitest run` 522 全绿。

## 三十二、V2.0-5（2026-09-21）：选商品 1000 条根因修复 + 选商品服务端分页

- 现象：商品 6281 条，「选择商品」弹窗只显示 1000 条，无法做单。
- 根因：PostgREST 的 `{ count }` / `{ head }` 只在 `from().select()` 这一步生效，链式到 FilterBuilder 后再 `.select()` 会被**静默忽略** → `count` 为 null → `CloudQuery.toArray` 误判 `total = 1000` 直接返回；`count()` 因 head 失效恒返回 0。
- 修复：`CloudQuery.build({ count, head })` 按需从 `from()` 重建；新增 `scanNarrow`（窄字段并行翻页）与 `distinct` 改用它；补 `CloudTable.bulkGet(ids)`（原缺失，生产路径有 6 处调用，运行时必崩），并保证入参顺序。
- 优化：`ProductPicker` 增加 `loader` 服务端模式（关键词 / 分类 / 只看有货下推，只留当前页），`pick` 事件带库存；`productStore.pickerPage()`；4 个开单/报价页切换；出入库明细页改 `bulkGet`。
- 关键词升级为「空格分词、词内 AND、字段间 OR」。
- 新增 `tests/cloud-page-limit.test.ts`（23 例，含 PostgREST 语义模拟；换回旧实现必红 5 例）。
- 验证：`npm run build` 0 错误；`npx vitest run` 545 全绿。

## 三十三、V2.0-6（2026-09-22）：性能优化 + 打印模板调整 + UI 统一

### 性能
- stock 表加内存缓存（CACHED_TABLES），翻页查库存零网络请求。
- 加 Service Worker（public/sw.js）：静态资源缓存优先，二次打开秒开；Supabase API 不缓存。

### 打印模板
- 单号移到头部右上角（与单据类型同一行）。
- 日期移到公司名后面。
- 页码移到签章行最后（供应商确认后面）。
- 列名"商品名称"→"品牌"，表头顺序统一为 序号/品牌/型号/类别/单位/数量/单价/金额。

### UI 统一
- 全系统 15 个商品列表表头统一为"商品名称"。
- 全模块 12 个页面手机端合计行通栏。
- 手机端已选商品表改卡片式布局。

### Bug 修复
- 入库确认卡住：CloudQuery 缺 `and()` 方法 → 全部改成 `.filter()`。
- 业务表数据全清，从头做单验证。


---

## 三十五、V2.0-7（2026-09-22）：手机端登录居中 + 品牌配置云端化准备

- 手机端登录页居中适配。
- 抽出 `src/utils/settingsSync.ts`：本机读写（localStorage）与云端同步分层，
  品牌配置先行接入 `systemSettings` 表。

---

## 三十六、V2.0-8（2026-09-22）：系统设置云端同步

**目标**：公司抬头 / 打印模板 / 价格规则 / App 图标 / 菜单排序跨设备共用一份。

| 环节 | 做法 |
| --- | --- |
| 存储 | `systemSettings` 表（key 唯一 + jsonb value + updatedAt） |
| 读 | 各模块照旧读 localStorage，零侵入 |
| 写 | `touchSetting(key)` → 记时间戳 + 异步 upsert |
| 启 | `initSettingSync()`：先拉云端，再补推本机独有（老用户历史设置不丢） |
| 内存 | `onSettingsReloaded()` 让模块级 ref（brand / menuOrder）重载 |
| 冲突 | last-write-wins（比 updatedAt） |
| 降级 | 表没建 / 离线 → 纯本机模式，设置页明确提示 |

同步白名单**封闭**：登录态、会话、记住账号、侧边栏折叠、面板展开一律不同步。

上线前需在 Supabase 执行 `supabase/migrate_v2.0-6_system_settings.sql`（幂等）。

顺带修两个既存缺陷：打印多页时只有末页有页码；sw.js 缓存优先导致新版首页不生效（改 network-first）。

验证：新增 21 例同步测试；`npm run build` 0 错误；`npx vitest run` 566 全绿。
---

## 三十七、V2.0-9（2026-09-22）：首屏加载页跟随「系统名称」

**问题**：刷新页面时一闪而过的加载页（`#app-boot`）写死「家电批发ERP」，
与设置里的「系统名称」不一致。

**根因**：加载页是静态 HTML，在 JS 解析前就要渲染，拿不到品牌配置。

**做法**：

| 环节 | 做法 |
| --- | --- |
| 首屏 | `index.html` 加同步内联脚本（早于 `main.ts`），读 `erp_brand_config.loginTitle` 就地改名 |
| 标题 | 同时同步 `<title>` 与 `apple-mobile-web-app-title` |
| 兜底 | 无配置 / 脏 JSON / 隐私模式 → 回落写死的默认名，不会空白 |
| 长名 | >10 字缩到 18px、>16 字缩到 15px，不换行挤掉呼吸条 |
| 运行时 | `brand.ts` 新增 `syncDocumentTitle()`：import / 改名 / 云端拉回三处刷新标题 |
| 单点 | 默认名抽为 `DEFAULT_SYSTEM_NAME`，设置页留空也回落它 |

验证：新增 `tests/boot-brand.test.ts`（11 例，直接执行 index.html 里的真实脚本）；
`npm run build` 0 错误；`npx vitest run` 579 全绿 / 51 文件。
---

## 三十八、V2.0-10（2026-09-22）：云端设置一键自助启用

**问题**：多设备设置同步依赖云端表 `systemSettings`，建表是 DDL、前端公开密钥无权限，
必须用户手工执行一次 SQL。可页面只写了个文件名，部署站点里没有这个文件 →
用户（尤其手机端）无从下手，「立即同步」点了没反应。

**做法**：把这一步搬进设置页，让页面自己讲清楚、自己给工具。

| 环节 | 做法 |
| --- | --- |
| SQL 原文 | `src/utils/settingsSql.ts` 内嵌 `SYSTEM_SETTINGS_SQL`，与 `supabase/migrate_v2.0-6_system_settings.sql` 逐字符一致（测试比对） |
| 直达链接 | `SUPABASE_SQL_EDITOR_URL` 由 `supabaseClient` 的项目地址推导 → 本项目 SQL Editor |
| 复制 | `copyText()`：clipboard 优先 → execCommand 回退 → 都失败返回 false 并提示手动复制 |
| 页面 | 未连云端时：四步指引（复制 → 打开 → 粘贴 Run → 刷新）+ 三个按钮 |
| 反馈 | 复制成功/失败都有 toast；建完表点「刷新页面」即变「✅ 已连接云端」 |

验证：`tests/settings-sql-help.test.ts` 12 例；`npm run build` 0 错误；`npx vitest run` 全绿。
---

## 三十九、V2.0-11（2026-09-22）：四步指引补回序号

V2.0-10 的四步指引在实机上渲染成没有序号的四行字 —— Vant base 样式抹平了 `ol` 的
`list-style`，scoped 样式只写了 `padding-left`。补 `list-style: decimal` + `::marker` 样式，
并加一条用例锁住（源码块必须含 `list-style: decimal`）。

---

## 四十二、V2.0-14（2026-09-22）：手机端表格「降权过头」导致选商品列表滑不动

问题：新建采购单 / 预采询价 / 销售单 / 报价单里，选商品列表右列被裁（「＋ 添加」在屏幕外），
手机端怎么滑都不动。

根因：上一版为兼容开单页卡片模式，把手机端 `.data-table` 的 `overflow-x: auto` 包进了
`:where()`，特异性降到 0；基础规则 `.data-table { overflow: hidden }`（(0,1,0)，为圆角裁剪）
于是压过它 → 内容被裁且不可滚。实测 640px 内容挤在 340px 容器，`overflowX` 算出来是
`hidden`，按钮 x=582（视口 390）。

修法：`overflow-x` 单独提到 `.app-layout.is-mobile .data-table`（0,2,0，只提 overflow
不提 display，保住卡片模式可覆盖）；`ProductPicker` 手机端 `.pk-scroll { overflow-x: visible }`
去掉双层滚动容器。补 4 条防回归用例。

## 四十一、V2.0-13（2026-09-22）：价格规则一键应用性能修复

问题：商品量大（6281），用「系统设置 → 价格规则 → 一键应用到全部商品」套规则时，
半小时才重算几百个，几乎不可用。

根因：旧 `applyRuleToAll` 拉全量后 `for (const p of list) await db.products.update(p.id, …)`，
6281 次**串行**云端 PATCH（新加坡节点高延迟），慢在必然。

修法：新增 `src/utils/applyPriceRule.ts`（`applyPriceRuleToAllImpl` 纯逻辑 + `applyPriceRuleToAll`
云端入口），分批（500/批）+ 受限并发（6 路）调用已有 `CloudTable.bulkPut`（一次请求整批 upsert）。
设置页 `applyRuleToAll` 改为调用它并实时回传进度；模板加 `<progress>` 进度条。
新增 `tests/apply-price-rule.test.ts` 5 例防回归。

## 四十、V2.0-12（2026-09-22）：整体提速 + 表格合计行通栏修复

问题：①打开/加载/切后台再回来都慢；②商品表还想再快一点；③采购/销售开单等页面的
**合计行与表头错位**（只占表格半宽），且用户要求「有通栏显示问题的页面全改」。

### 合计行通栏（全站）

- **14 个页面的手机端 CSS 写在 `</style>` 之后**（浏览器收不到，等于没写）→ 全部移进 `<style>`。
- **theme.css 拆表漏管 tfoot**：手机端 `thead`/`tbody` 各自 `display:table` 而 tfoot 没跟上，
  三张表各算各的列宽（644 / 650 / 303px）。现在 tfoot 用 flex 通栏铺满容器、空 `<td>` 隐藏；
  全局规则统一 `:where()` 降权，不压页面自己的卡片布局。
- **colspan 与表头列数不符**：采购/销售开单 6→5，退换货 3→4。

新增 `tests/table-layout-and-perf.test.ts` 全量扫描：CSS 必须在 style 块内、
每个 tfoot 的 colspan（含 v-if/v-else 两条路径）必须等于表头列数、
手机端 tfoot 与 thead/tbody 必须同进同出。

### 性能

- 首屏：Vant 改按需（`src/` 里没有任何 `<van-*>` 组件）→ 主包 726KB → **505KB**（gzip 158KB）；
  挂载不再等云端品牌设置；`brand.ts` 惰性引 `settingsSync`（把 supabase-js 挪出首屏）；
  手机底栏去 `backdrop-filter`（滚动不再每帧算模糊）。
- Service Worker 重写：缓存键用请求自身 URL（旧版写死 `'/'`，站点在 `/wholesale-erp/`
  子路径下永远命中不了）+ 导航 network-first 带超时回退 → 二次打开秒开。
- 空闲预取 `src/utils/idle.ts`：等「网络安静」再串行预取路由分包，`visibilitychange`
  隐藏时暂停，不跟首屏抢带宽。

### 商品表再优化

- 分类下拉三级缓存：内存 → 并发去重 → `localStorage`（仅云端，10 分钟 TTL，
  过期先给旧值再后台刷新）。
- 低库存 / 库存汇总 / 商品名映射改窄字段扫描（5 / 2 / 3 列），别再为了几列拉整表。
- 库存管理页按需加载：默认 Tab（库存作业）不拉全表，切到明细 / 预警才拉且只拉一次。
- 老板工作台毛利去掉二重 N+1（每单查明细 + 每条明细查商品）→ 明细一次拉完 + `bulkGet` 一次取回。

### 验证

`npx vitest run` 611 例全绿（53 文件）；`npm run build`（`vue-tsc -b`）0 错误；
tag `v2.0-12` 已部署线上复核。

## 四十三、V2.0-15（2026-09-22）：启动白屏 3 秒——身份恢复别挂在启动导航上等云端

**现象**：每次启动都要先在白屏停 3 秒才跳登录页 / 工作台。

**根因**：`router/index.ts` 的 `beforeEach` 对每次导航（含启动那次）都
`await userStore.restoreSession()`，而它要用 `db.users.get(id)` 去云端
（新加坡）把会话里的裸 id 还原成用户 —— 往返 1~3 秒，期间 `<router-view>` 是空的。
V2.0-12 只摘掉了「品牌配置」的云端等待，这条身份恢复的等待一直还在。

**改动**

| 文件 | 改动 |
| --- | --- |
| `src/utils/loginGuard.ts` | 新增 `StaffProfile` / `DealerProfile` 本地快照；`saveSession(id, kind, profile?)` 存快照；`readSession()` **必须原样带出 `profile`**（否则 `touchSession()` 续期会用 `undefined` 覆盖、把快照抹掉） |
| `src/stores/user.ts` | 登录时存快照；`restoreSession` 有快照就地恢复（零库访问）；新增 `validateSessionInBackground`（后台非阻塞校验，仅云端模式）；老会话回源一次并升级成带快照格式 |
| `tests/restore-session-instant.test.ts` | 新增 7 例防回归 |

**踩坑**：改 `readSession()` 只改了返回类型、没改 return 语句体，导致 profile 取不到，
且 `touchSession()` 续期把快照擦掉 —— 老会话「升级」永远不生效。
**改类型别忘了改 return。**

**实测**：生产构建带快照启动 380ms；剥掉快照模拟旧逻辑 804ms（本机云端往返约 420ms，
手机弱网即 1~3 秒）。`npx vitest run` **627 例全绿（55 文件）**；`npm run build` 0 错误。
---

## 四十四、V2.0-16（2026-09-22）：手机端合计行通栏「治了一半」——页面里那 12 份副本一直在盖住全局

**现象**（用户反馈 + 截图）：入库验货 → 验货明细的合计行仍然是坏的：
每个合计格自己画一条 2px 深色横线、数字被推到线下，整行像「一串横线夹着数字」，
还有一块 28px 的空格子。390px 视口实测行高 **75px**（正常应为 48px）。

**根因（两个叠在一起，缺一不可）**

1. **`:where()` 降权过头** —— 和 V2.0-14 的 `overflow-x` 是同一个坑的第二次。
   全局「合计行通栏」那几条全包在 `:where()` 里（特异性 0），
   而基础规则 `.data-table tfoot td`（**(0,1,2)**）会给出 `padding: 12px 14px` +
   `border-top: 2px solid var(--c-primary)` + `background: #f4f7fb`，
   `.data-table th, .data-table td`（(0,1,1)）再补一条 `border-bottom: 1px` ——
   `padding: 0 / border: none / background: none` 全部失守。这就是那两条横线的来源。
2. **12 个页面各留着一份重复副本** —— V2.0-12 把通栏规则收进全局时只搬走了表头/表体那部分，
   页面里那 12 份**没删**。scoped 选择器 `.data-table tfoot td[data-v-x]` 特异性 (0,2,3)、
   比全局的 :where() 高，但它**只声明 display/width/margin**，
   不管 padding/border/background —— 所以「只改全局」在浏览器里看不出任何变化。

**改动**

| 文件 | 改动 |
| --- | --- |
| `src/styles/theme.css` | 合计行的 **td 层** 从 `:where()` 提出来提权（`.app-layout.is-mobile .data-table > tfoot > tr > td` = (0,3,1)），真正清掉 padding/border/background；`td:empty`、`td.ui-hint` 同步提权（否则被上一条的 `display:inline-block` 压过）；`> tfoot` 与 `> tfoot > tr` **继续 :where() 降权**（开单页卡片合计行走 tr 层，必须还能覆盖）；把原先只在「记一笔」页里的 `td[colspan]{flex:none}`、`td.ui-hint{display:none}` 收进全局 |
| 12 个页面 | 删掉重复的手机端 tfoot 通栏副本，各留一行指引注释（通栏只写在 theme.css 一处） |
| `tests/table-layout-and-perf.test.ts` | 23 → 29 例：新增「不能降权到压不过基础规则」（自带**特异性计算器**，断言提权规则严格强于基础规则）、「通栏只允许一份实现」（扫描全部 .vue 禁止再复制）；并改写原先过粗的「display 一律不许提权」断言 —— 卡片模式怕的是**表格/表头/表体** 的 display 被提权，tfoot 的 td 提权与卡片模式无关 |

**实测（390px 视口）**

- 修复前后：合计行 `h=75 → 48px`；单元格 `padding 12px 14px → 0`、
  `border-top 2px → 0`、`border-bottom 1px → 0`；空占位格 `28px 可见 → display:none`。
- **全站 14 种 tfoot 结构逐一注入真实页面实测**（含没有单据数据、点不进去的页面）：
  全部 `h=48 w=326`，无一残留内边距/边框/背景、无空格未隐藏。
- 真实数据页面逐个复核 + 截图：入库验货、盘点作业、新建采购单（卡片模式）、预采询价、退换货。
- 桌面端（1280px）：`is-mobile=false`、tfoot 仍是 `table-row`、单元格按列对齐 —— 未受影响。
- 两个单据详情页（销售单/采购单）手机端走 `item-cards`、根本不渲染 `<table>`，
  原先那两段 `.data-table tfoot` 是**死代码**，一并清掉。

**自检（防假绿）**：把修复改回 bug 形态 → 3 条新用例报红；把重复副本加回 InboundDetailView →
第 4 条报红并指名文件。

`npx vitest run` **633 例全绿（55 文件）**；`npm run build`（`vue-tsc -b`）0 错误。

---

## 四十五、V2.0-17（2026-09-22）：Service Worker 把老用户钉在了旧版本上

**现象**：V2.0-16 已把手机端合计行通栏修好、线上产物也逐字节核对过，
用户仍然反馈「入库验货 → 验货明细 的通栏显示还是有问题」。

**根因**：页面代码没问题 —— 用户手机上跑的**一直是旧版本**。

浏览器判断「Service Worker 有没有更新」**只看 `sw.js` 文件自身的内容**。
该文件最后一次修改停在 V2.0-12（`b76b748`，把 `CACHE_NAME` 由 `erp-v2` 改成 `erp-v3`），
此后四轮发版只改源码、一次都没碰过它。于是：

1. 浏览器认为 SW 从未更新 → **永远不触发 `install` / `activate`** →
   `activate` 里「删掉非当前版本的缓存」从来没执行过 → 旧缓存 `erp-v3` 一直留着；
2. 导航请求是「网络优先，超过 1.5s 回退缓存」。GitHub Pages 在国内经常超过 1.5s，
   一旦回退，返回的就是缓存里的**旧 index.html**，旧 HTML 又引用旧的 `index-xxxx.css`
   （缓存里有、文件名带 hash 于是永远命中）→ **用户看到的始终是修复前的界面**。

网络好时拿新页面、网络一慢退回旧版，所以表现为「有时好有时不好」。

**改动**

| 文件 | 改动 |
| --- | --- |
| `public/sw.js` | `CACHE_NAME = 'erp-' + BUILD_VERSION`（构建期注入的占位符，不再写死）；`isCacheable()` 排除 `sw.js` 自身 |
| `scripts/inject-sw-version.mjs` | **新增**。构建后把 `dist/sw.js` 的 `__BUILD_VERSION__` 换成 `package.json` 的版本号 |
| `package.json` | `build` → `vue-tsc -b && vite build && node scripts/inject-sw-version.mjs` |
| `src/utils/installApp.ts` | `register(..., { updateViaCache: 'none' })`；注册后主动 `reg.update()`；`controllerchange` 后自动刷新一次，但**只在用户尚未交互时**（避免冲掉正在录的采购单），并用 `sessionStorage` 防无限刷新 |
| `tests/sw-cache-version.test.ts` | **新增** 11 例，锁死上述约束 |

> 为什么注入做成独立脚本而不是 vite 插件：`vite.config.ts` 归 `tsconfig.node.json` 管，
> 而本项目没装 `@types/node`，在配置里 `import 'node:fs'` 会让 `vue-tsc -b` 直接报错。

**实测**

- 造出真实旧缓存 `erp-v3` → 新 SW 装上后 `caches.keys()` 变为 `['erp-2.0.16']`（旧缓存被清）。
- **模拟发版**：已有 `erp-2.0.17` → 服务器 `dist/sw.js` 改成 `2.0.18` → 重新打开应用
  → `caches.keys()` 变为 `['erp-2.0.18']`。全链路打通。
- 自检：`sw.js` 改回硬编码 `'erp-v3'` → 新用例 2 条报红。

**顺带完成的手机端显示全站巡检**（用户要求「不要跳过、不要删除」）

- 手机端**所有列表页走卡片布局**（`.data-table` 为 0），表格只在开单页与库房作业页出现。
- 有表格的页面逐个注入真实商品数据实测：入库验货 `h=48 w=326`、新建采购单 `h=45 w=340`、
  预采询价 `h=54 w=340`，以及出库拣货 / 盘点 / 调拨 / 退换货 —— 合计行均无内边距、边框、
  背景外泄，无空占位格残留，无横向溢出；桌面端 1280px 复核 `table-row` 未受影响。
- 巡检脚本：`/tmp/audit-page.js`。

`npx vitest run` **644 例全绿（56 文件）**；`npm run build`（`vue-tsc -b`）0 错误。

---

## 四十六、V2.0-22（2026-09-23）：手机端销售单详情 / 修改销售单的商品明细改卡片式

用户要求：手机端单据里，销售单详情页的商品明细、以及修改销售单里的明细，都要更适合手机浏览；电脑端不动。

- **不再拆表**：这两处原来都用 `nth-child` 把 `<table>` 拆成卡片（`tr` 改 flex、`td` 改 block）。
  实测手机端商品名被裁、输入框无标签、合计行错位 —— 与你 V2.0-18 推翻的旧方案同源。
  现在改为模板层 `v-if="isMobile"` 渲染真正的 `<ul>` 卡片，`v-else` 保留原表格。
- **详情页商品明细**：一行读清 `数量 × 单价 = 金额`；并补上**手机端合计条** ——
  此前手机端没有合计（合计只在桌面表格 `tfoot` 里）。出库流水 / 收款记录一并改卡片。
- **修改销售单**：手机端整屏覆盖（`position: fixed`）+ 内容区独立滚动 + 底部按钮常驻；
  每行卡片「商品名 + 实时金额 + 带标签的数量/单价输入框（44px）」。
  改前编辑区在详情页下方 y=983 处，点「修改」界面毫无反应，得翻很远才能编辑。
- **电脑端零改动**：DOM 与样式一字未动，1280px 实测详情页 `<table>` 与修改页内联橙框区域均照旧。
- **防回归**：新增 `tests/sale-order-mobile-cards.test.ts`（16 例），含「禁止再出现 `nth-child` 拆表」。

`npx vitest run` **660 例 / 647 通过**（13 条红为既有基线，非本次引入）；`npm run build`（`vue-tsc -b`）0 错误。

> 附：本页 `</style>` 之外的死 CSS（V2.0-19 遗留）一并清除；
> `src/views/purchase/PurchaseOrderDetailView.vue` 存在同类问题（同样是 CSS 写在 `</style>` 外，
> 且其编辑页依赖那段从未生效的样式），尚未处理，待确认。

## 四十七、V2.0-23（2026-09-23）：电脑端销售单详情 / 修改销售单「用同一套卡片」，表格整体退场

用户要求：把 V2.0-22 的手机端做法同样用到**电脑版**上。

- **改动的主线：不按设备分岔，改「同一套 DOM、两端各自排布」**。
  V2.0-22 是 `v-if="isMobile"` 卡片 / `v-else` 表格两支并存；这次两支合一 ——
  商品明细、出库流水、收款记录、编辑明细四处**全部共用卡片 DOM**，模板里 `<table>/<thead>/<tbody>/<tfoot>` 全部删除。
- **电脑端排布**（`min-width: 768px`）：商品明细**单列铺满整行 + 卡片内容排成一行**
  （序号 + 商品名 …… 数量 × 单价 = 金额），实测 978×45（两行版 77 高）、商品名 718px；
  **手机端保持上下两行**（窄屏放不下）。内容短的编辑明细 / 流水 / 收款仍用多列网格
  （`.ec-list` ≥340px、`.rc-list` ≥260px，`repeat(auto-fill, minmax(…, 1fr))`）。
- **手机端编辑页去掉「返回」**（编辑页整屏覆盖，返回在逻辑上多余），底栏只留「取消 / 保存修改」各 178px；
  详情页返回按钮保留，电脑端编辑底栏也保留。
- **备注框改 `textarea` 自增高度**（`autoGrowRemark()`：`height='auto'` → 按 `scrollHeight`，56~240px），
  进入编辑态先撑开一次；并收紧间距 —— 全局 `.d-meta i` 的 62px 固定宽被编辑页继承后白空 36px，
  改 `width: auto`（实测 62 → 26px），网格列 `auto 1fr`、`gap` 8 → 6px。
- **卡片补序号**（行次对应）：电脑端显示、手机端 `display: none`（单列天然有序）。
- **合计条升级为汇总卡**：电脑端浅灰底 + 边框 + 圆角，金额 19px；它是全页唯一合计来源（`tfoot` 已退场）。
- **顺手修电脑端老毛病**：编辑区在详情页下方（y=1627，视口 900），点「修改」后视口停在顶部像没反应。
  现在 `startEdit()` 里 `await nextTick()` 后 `scrollIntoView`（仅 `>= 768` 时滚；手机端是整屏 fixed，无影响）。
  实测 `scrollTop` 0 → 602。
- **手机端零退化**：`.mc-list` 仍 `display: block` 单列、宽 328、序号隐藏；编辑页仍整屏 fixed、输入框 152×44。
- **防回归**：`tests/sale-order-mobile-cards.test.ts` 重写为 21 例。
  原「电脑端仍要保留原表格」断言已被需求作废 → 替换为「全页无 `<table>`/`<tfoot>`」「两端共用卡片 DOM」（需求变更，非放宽）；
  新增电脑端必须 `grid` + `auto-fill`、列宽 ≥320px、手机端不得套用网格、序号仅电脑端、`startEdit` 必带 `nextTick + scrollIntoView`。
  自检：`mc-list`→`item-cards`、`.rc-list` 的 `grid`→`block` → 3 条报红。
- 顺带清掉不再使用的 `colspan` computed 与 `useResponsive` 导入（否则 `noUnusedLocals` 会拦构建）。

`npx vitest run` **665 例 / 650 通过**（15 条红 = 既有 13 条基线 + 文档版本 1 条[写完即绿] + `stage6 6.2` 1 条，
单跑该文件 6 例全绿，属既知并行干扰）；`npm run build`（`vue-tsc -b`）0 错误。

## 四十八、V2.0-24（2026-09-23）：电脑端「修改销售单」商品明细卡片内改为单行排列

用户要求：电脑端「修改销售单」页的商品明细卡片，原来上下两行（上：序号 + 商品名 + 金额；下：「数量 / 单价」两个输入框），改成**单行排列**——序号 + 商品名 + 数量 + 单价 + 金额 一行读完。手机端不变。

- **只改电脑端排布，DOM 不动**：仍是 V2.0-23 那套 `ul.ec-list > li.ec-item`（`.ec-top` + `.ec-row`）。
- `.ec-list` 由多列网格（`repeat(auto-fill, minmax(340px, 1fr))`）→ **单列铺满**（`display: block`）。
- `.ec-item` → `display: flex`；用 **`display: contents` 拆开 `.ec-top`**，序号 / 商品名 / 金额直接成为本行 flex 子项，商品名 `flex: 1`（超长省略号），输入框固定 `152px`、高 `40px`。
- `.ec-row`（`margin-top: 0`、`flex: none`）并到本行；`.ec-amount` 用 `order: 1` 挪到行尾右对齐。顺序 = 序号 商品名 [数量 单位] [单价] 金额。
- 实测 1280px：单行高约 56px（原两行约 77px）。

**防回归**：`tests/sale-order-mobile-cards.test.ts` 改写原「`.ec-list` 必须多列网格」断言为「电脑端 `display: block` 无 `grid-template-columns`」「`.ec-item` flex」「`.ec-top { display: contents }」「`.ec-row { margin-top: 0 }」「`.ec-amount { order: 1 }」「`.ec-field { width: 152px }」，并补「手机端不得套用单行 flex / 不得拆 `.ec-top` / 输入行保留上边距」。该文件 **29 例全绿**（原 21 例）。

**本次仅本地收口、未部署**：改动已提交为 `V2.0-24`（`package.json` → `2.0.24`，SW 缓存名随之变化），待用户下令后再 `push` + 部署 GitHub Pages（否则老用户拿不到新版）。

## 四十九、V2.0-25（2026-09-23）：修改销售单模块去橘色大边框 + 模块内删掉「返回」按钮

**诉求**：电脑端「修改销售单」区块外面套了一圈 4px 橘色粗边框，看着像弹窗，里面的卡片也因此比详情页其它卡片左右各窄一圈；且模块内按钮条是「返回 / 取消 / 保存修改」三个键，返回与取消语义重复。要求：模块内只留「取消 / 保存修改」，去掉橘色大边框让模块融进版面，并且点「取消」或「保存修改」后模块自动关闭、详情页原本那颗橘色「返回」键自动回来。

**改动**（`src/views/sales/SaleOrderDetailView.vue`）：

| 位置 | 改前 | 改后 |
| --- | --- | --- |
| `.edit-footer` 模板 | 返回 / 取消 / 保存修改 三键 | **取消 / 保存修改** 两键（`btn-back` 从模板与 CSS 一并删除） |
| `.edit-page` | `border: 4px solid --c-amber` + `background: --c-bg` + `padding: 12px 0 90px` | `padding: 0; background: transparent`（无边框） |
| `.edit-page .block` | 覆盖 `margin: 12px`（比其它卡片窄 24px） | 删除该覆盖，沿用全局 `.block`（同宽同圆角同阴影） |
| `.edit-footer` | `border-top` + `border-radius: 0 0 12px 12px`（容器下半截） | 整圈 `border: 1px solid` + `border-radius: 12px` + 卡片同款阴影 |
| 关闭逻辑 | `取消` 直接 `showEdit = false`；保存成功分支各自 `showEdit = false` | 新增 `closeEdit()`，两处统一调用 |

- `closeEdit()`：`showEdit = false` → `await nextTick()` → `.page-actions`（详情页橘色返回键所在的操作栏）`scrollIntoView({ behavior: 'smooth', block: 'nearest' })`。模块消失后页面变短，这一步避免视口停在空白处。
- 手机端媒体查询里补 `border: none; border-top: 1px solid #e8eaef`，保持贴底通栏；手机端本身是整屏覆盖，行为不变。

**防回归**：`tests/sale-order-mobile-cards.test.ts` 原「手机端隐藏返回按钮、电脑端保留」断言作废（需求变更），改写为「模板与样式都不得出现 `btn-back`」「按钮条含 `btn-cancel` / `btn-save`」「`<PageActions v-if="!showEdit"`」「取消必须 `@click="closeEdit"`」「保存成功分支 `await closeEdit()` 后才 `loadOrder`」「`.edit-page` 不得有 4px 边框 / `--c-amber`」「`.edit-page .block` 不得有 `margin: 12px`」「`.edit-footer` 整圈边框 + 12px 圆角」。该文件 **31 例全绿**（原 29 例）。
自检：把 `btn-back` 塞回模板 → 对应断言立即报红，还原后复绿。

**本次仅本地收口、未部署**：按用户要求（「更改完后先不用部署，等我命令再进行部署」），改动提交为 `V2.0-25`（`package.json` → `2.0.25`，SW 缓存名随之变化），待用户下令后再 `push` + 部署 GitHub Pages。

## 五十、V2.0-26（2026-09-23）：三张单据页统一加上「修改」

### 背景

V2.0-25 把销售单「修改」归纳成了两个可复用件（`src/composables/useEditMode.ts` 状态机 +
`src/components/EditModePanel.vue` 排版与按钮条），本轮把它们铺到另外三张单据页。

### 改动

1. **采购单 `PurchaseOrderDetailView.vue`**（改造旧版）
   - 旧版问题：`.edit-page` 无条件 `position:fixed`（电脑端也被强制全屏、与详情页版面脱节）；
     底部 3 颗按钮（含 `btn-back` 返回）；明细用 `<table>` + `mini-input`；
     `</style>` 之后还残留一段 `.modal-*` 死样式（从未生效）。
   - 现在：改用 `<EditModePanel>`（电脑端内联 / 手机端整屏，底部只留「取消 / 保存修改」），
     明细换成 `.ec-*` 可编辑卡片，死样式全部清除，`btn-back` 删除。
2. **报价单 `QuotesView.vue`**（新增功能）：详情加「✏️ 修改」，可改数量 / 报价 / 备注。
3. **预采询价 `PurchaseQuotesView.vue`**（新增功能）：详情加「✏️ 修改」，可改数量 / 询价 / 备注。
4. **`src/stores/quotes.ts`** 新增 `updateQuote(quoteId, items, remark, operatorId)`：
   明细整单替换（先删后加）、金额重算、`converted` 单禁止修改、写审计日志。
   **`src/utils/audit.ts`** 新增 `QUOTE_UPDATE`。

### 约定（四张单据页必须一致）

- 进入编辑前先拷副本（`beginEdit`），不要直接裸调 `startEdit`；
- 取消与保存成功都走 `closeEdit()`（模块关闭 → 详情页橘色「返回」键自动回来并滚进视野）；
- 编辑模块内不放返回按钮；
- 电脑端内联卡片、手机端整屏覆盖，共用一份 DOM，不另开模板。

### 验证

- `npm run build` 0 错误；新增 `tests/quote-edit.test.ts` 8 例全绿。
- 全量 683 例 / 671 通过 / 12 红（均为既有基线，无新增）；清掉采购单 `</style>` 外死样式后，
  基线里那条「CSS 写在 `</style>` 之外」由红转绿（13 红 → 12 红）。
- 实机双端验证采购单修改（含保存生效后改回原值，数据保持原样）。
- **仅本地收口，未 push、未部署**（等用户命令）。

## 五十一、V2.0-27（2026-09-23）：新建 / 修改单据后回到列表页自动刷新

### 问题

「新建采购单后不会直接添加到列表，必须刷新后等 6~10 秒才出现」。

### 根因

`App.vue` 的 `<keep-alive>` 无差别缓存所有路由组件。列表页 → 新建页 → 提交 →
`router.push` 回列表页时，组件是**复活（activated）**而非**重新挂载（mounted）**，
`onMounted` 里的加载不会再跑，列表停在旧数据。

用户感知的「6~10 秒」是**整页刷新**重新拉云端数据的时间，不是同步慢 ——
提交成功那一刻数据已落云端。

### 修法

新增 `src/composables/useReloadOnActivate.ts`（`onDeactivated` / `onActivated`）：

- **首次挂载不触发**：keep-alive 下首次挂载 `onActivated` 也可能调用，不加判断会与
  `onMounted` 各拉一次。用「曾经离开过」标志挡掉。
- **非 keep-alive 环境不触发**：测试里 `mount()` 直接挂组件时两个钩子都不调用，
  既有测试零影响。

接入：

1. `useServerPager` 内置 —— 一处覆盖采购单 / 销售单 / 报价单 / 预采询价 / 客户 /
   供应商 / 出入库流水 / 操作日志。
2. 23 个未走 `useServerPager` 的页面各自调用（仓库、财务、boss、各详情页）。

顺带删掉采购单 / 销售单列表页 `onMounted` 里多余的 `pager.reload()`（首屏原本发两遍相同请求）。

### 铁律补充

- ⚠️ 以后**任何新页面只要在 `onMounted` 里拉数据，就必须同时挂 `useReloadOnActivate`**，
  否则同样会出现「改完回到本页还是旧数据」。
- 判断依据：全站路由组件都在 `<keep-alive>` 里，`onMounted` 只会跑一次。

### 验证

- `npm run build` 0 错误；新增 `tests/keep-alive-refresh.test.ts` 8 例全绿（真实 KeepAlive 往返）。
- 全量 691 例 / 678 通过 / 12 红（既有基线，无新增；另有 1 例偶发红单跑全绿）。
- 实机：新建采购单后未刷新即出现（共 4 → 5 张）；删掉测试单后回到列表立刻变回 4 张。
- **仅本地收口，未 push、未部署**（等用户命令）。

## 五十二、V2.0-28（2026-09-23）：单据删除权限 —— 仅老板与系统管理员

### 需求

给「管理员」与「老板」增加单据删除功能，其余角色一律没有删除权；
凡是系统能编辑的单据，都要给这两个角色配删除按钮。

管理员＝`hawsystem`（工号 E000，`system: true`），老板＝`admin`（工号 E001）。
两者 `role` 都是 `boss`。

### 改动

1. **`src/composables/usePermission.ts`**：新增 `isSystemAdmin` 与 `canDeleteDoc`
   （`role === 'boss' || system === true`）—— 删除权限的单一来源，不要在各页面自己判角色。
2. **`src/utils/audit.ts`**：新增 `SALE_DELETE` / `PURCHASE_DELETE` / `STOCK_DOC_DELETE`。
3. **`src/stores/sales.ts` / `purchase.ts`**：各新增 `removeOrder(orderId, operatorId)`，
   只允许删 `pending`，先删明细再删主表，写审计日志。
4. 五个页面接删除入口：销售单 / 采购单（新增按钮，`canDeleteDoc && status==='pending'`）、
   报价单 / 预采询价（既有删除按钮补 `canDeleteDoc` 闸）、出入库单（新增「🗑 删除本单」）。

### 铁律

- ⚠️ **删除权限只认 `canDeleteDoc`，不要在页面里散写 `role === 'boss'`**，
  否则以后加角色会漏改。
- ⚠️ **已经动过库存的单据不能直接删**：销售单 / 采购单限定 `pending`；
  出入库单的删除必须走 `revertDoc`（退回库存 → 删流水 → 重算来源单据状态）。
   store 层保留状态校验，不依赖界面控制。
- ⚠️ 报价单 / 预采询价原本的删除按钮**没有任何权限控制**，是本轮补的闸 ——
  以后新增删除类操作，默认就要带 `canDeleteDoc`。

### 验证

- `npm run build` 0 错误；新增 `tests/doc-delete-permission.test.ts` 12 例全绿。
- 全量 703 例 / 691 通过 / 12 红（既有基线，无新增）。
- **仅本地收口，未 push、未部署**（等用户命令）。

## 五十三、V2.0-29（2026-09-23）：全站按钮配色统一 + 出入库单撤回合并进删除

### 1. 出入库单：撤回合并进删除

`stockDoc.revertDoc()` 是「撤回」与「删除」的共同底层（退回库存 → 删流水 → 重算来源单据状态），
两者效果完全相同，而「撤回」没有任何权限闸 → 库房点撤回就等于删单，V2.0-28 的 `canDeleteDoc` 形同虚设。

做法：删除「↩ 撤回本单」按钮与 `handleRevert()`，只保留 `canDeleteDoc` 闸下的「🗑 删除本单」；
页头副标题去掉「也可整单撤回」。库房仍可改数量纠错，只是不能整单作废。

### 2. 按钮配色（唯一来源 `src/styles/theme.css`）

| 按钮 | 色值 | 语义类 |
| --- | --- | --- |
| 打印 | `#16a34a` | `.btn-print` |
| 删除 | `#ef4444` | `.btn-delete`（含 `.btn.danger`） |
| 取消 | `#ef4444` | `.btn-cancel` / `.cancel` / `.cancel-btn` / `.ui-btn-cancel` |
| 返回 | `#f97316` | `.btn-back` / `.pa-cancel.tone-back` |
| 修改 | `#2f6bff` | `.btn-edit` |

### 铁律

- ⚠️ **新增按钮配色只准改 theme.css 的「全站按钮配色统一」段**，不要在页面 scoped 里各写一份颜色
  —— 否则又一次散落各处、改一处漏一处。
- ⚠️ **只覆盖 `background` / `border-color` / `color`**，不许出现 font-size / border-radius / padding / height
  （有测试断言锁住）；「风格和字体样式不变」是用户的明确要求。
- ⚠️ 必须 **用 `!important`**：页面 scoped 带属性选择器，特异性高于全局单类，否则盖不住。
- ⚠️ 「返回」与「取消」共用 `PageActions` 同一个键，靠**文案**区分 tone（含「取消」→ 红，否则橘）。
  以后加页面传 `cancel-text` 时不用管配色，组件自动判断。
- ⚠️ 保存 / 确认 / 新增 / 查看 / 搜索 / 转换 **一律不动**；表格行内的文字链接型删除也不加实底。

### 验证

- `npm run build` 0 错误；新增 `tests/button-colors.test.ts` 13 例全绿。
- 全量 716 例 / 704 通过 / 12 红（既有基线，无新增）。
- 实机双端 `getComputedStyle` 实测：修改蓝 / 打印绿 / 删除红 / 返回橘，其余按钮未被波及。
- **仅本地收口，未 push、未部署**（等用户命令）。

## 五十四、V2.1-1（2026-09-23）：B1 经销商门户（上）—— 路由/菜单/防死循环 + 报价单页 dealer 分支 + 按 customerId 隔离 + 渠道入口

### 需求

老板要给经销商开「自助下单」入口。本期（B1 上）只做**渠道骨架**：经销商登录后
**只能进报价单页**，报价列表**只看自己的单**，内部操作（销售单 Tab / 客户下拉 / 零售价 / 修改 / 转单）对其隐藏；
经销商账号**不接受外部注册**，由老板在客户档案里「设为经销商」开通（登录手机号 + 密码）。

### 改动明细

| 文件 | 改动 |
| --- | --- |
| `src/router/index.ts` | `/sales/quotes` 的 `meta.role` 加 `dealer`（守卫第一道即按此放行）；`/sales/mine` 加 `dealer`（经销商「我的」页） |
| `src/stores/user.ts` | `homeRouteForRole('dealer')` 由**不存在的** `/dealer/catalog` 改为 `/sales/quotes` —— 修「登录后被弹到不存在页 → catch-all 踢回 /login」的**死循环** |
| `src/router/navConfig.ts` | 新增 `navConfig.dealer`（侧边栏仅「报价单」、手机端 Tab「报价单 / 我的」）。`/sales/quotes` 是**页内 Tab、不在 `ALL_MODULES`**，故 sidebar 手写而非 `modulesOf`（否则返回空）；同时**不放进 `DEFAULT_ROLE_PERMS`**（其权限固定、不接受老板配置，且非登记模块会通过不了模块自洽断言） |
| `src/views/sales/QuotesView.vue` | 新增 `isDealer` 分支：loader 云端加 `customerId` 过滤 + `extraFilter` 本地兜底；列表「销售单」Tab 对经销商隐藏；新建态客户区只读（显示自身账号）；价格按钮隐藏零售、锁批发价；详情页「修改 / 转为销售单」对经销商隐藏（删除本就受 `canDeleteDoc` 闸控）；`handleCreate` 自动写自身 `customerId`；`openDetail` 加归属校验防越权 |
| `src/stores/permission.ts` | `canAccess` 增加回退：角色不在模块矩阵里（routesOf 为空）时，以 `navConfig` 声明的入口为准 —— **否则经销商手机端 Tab 只剩一个报价单、丢失「我的」** |
| `tests/nav-permission.test.ts` | 「菜单项均可真正访问」用例超时 5s → 20s（**断言一字未改**） |

### 测试

- 新增 `tests/dealer-quotes.test.ts` 3 例：经销商首页路由=`/sales/quotes`；只能进报价单页（7 个内部路由全部弹回）；`navConfig.dealer` 菜单项均指向已注册路由。
- `tests/phase1.test.ts` / `tests/stage2.test.ts`：`homeRouteForRole('dealer')` 断言由 `/dealer/catalog` 校准为 `/sales/quotes`（**断言过期，非功能退化**）。
- 受影响的 5 个文件单独复跑全绿：nav-permission 14/14、phase1 8/8、stage2 6/6、dealer-quotes 3/3、permission 8/8。

### 踩坑

1. **`canAccess` 与路由守卫口径不一致**：守卫第一道看 `meta.role`、第二道才看 `canAccess`（模块矩阵）。
   经销商不在 `DEFAULT_ROLE_PERMS` 矩阵里 → `canAccess` 恒 false → `MobileTabBar` 把「我的」Tab 过滤掉。
   修法是把回退放进 `permission.ts`，**不要在组件里 import router 另算一套**（会引入循环依赖且同样没修好）。
2. **全量并行跑出现 15 文件 / 28 例红，但逐个单跑全绿** —— 环境累计耗时 10862s，机器负载极高。
   按既有结论判定为**跨文件并行干扰**（`fake-indexeddb` 共享），非功能退化；判定手法是单跑 + git worktree 起 HEAD 干净副本对照。
3. `tests/nav-permission.test.ts` 的 boss 用例实际需 **~11.6s**（它要逐个 push 全部菜单项、每项触发一次视图动态 import），
   而默认超时只有 5s → 超时会中断导航并残留 pending 跳转，**污染下一个角色的用例**造成误报（实测 boss 超时 → purchaser 被误判「守卫弹回」）。
   已把超时调到 20s（断言不变）；HEAD 副本同样复现该超时，属既有问题。
4. `agent-browser` 的 `type` 是**追加**不是替换 —— 连打两次会把账号拼成 `1380000099913800000999` 导致登录失败，
   误判成「移动端经销商登录坏了」。**填表单一律用 `fill`**。切视口是 `set viewport 390 844`（顶层没有 `viewport` 命令）。

### 验证

- `npm run build` 0 错误（`vue-tsc -b` 含 noUnusedLocals）。
- 实机双端验证（dev server + agent-browser，云库真实经销商账号）：
  电脑端 —— 经销商登录落地 `#/sales/quotes`、身份徽标「经销商」、列表 0 张（隔离正确）、
  手输 `boss/home`/`sales/orders`/`customers`/`stock`/`finance`/`purchase/orders`/`warehouse/home`/`boss/users` **全部弹回** `#/sales/quotes`、
  新建表单客户字段只读「验证经销商测试」+ 价格锁定批发价、「销售单」Tab 已隐藏。
  手机端（390×844）—— 同样落地报价单、路由封死一致、`documentElement.scrollWidth == innerWidth`（**无横向溢出**）。
- 版本收口 6 处：`src/version.ts` V2.1-1、`package.json` 2.1.1、`PRD.md`/`MENU_SPEC.md`/`DEV_PLAN_V2.md`、`tests/stock-cards-v17.test.ts` 版本断言。

### 约束 / 遗留

- 经销商账号由老板在「客户管理 → 设为经销商」开通（`customers.loginPhone` + `loginPassword`，**明文比对**）；
  登录走现有主登录页（同一入口，按表区分 staff/dealer），不新增公开注册。
- 验证期间在云端建过一个测试经销商（`id=9`，手机 13800000999 / 密码 test123，备注「V2.1-1 验证用，可删」），可自行删除或改造成真实经销商。
- 本期**未做**：销售侧「确认询价单」动作 + Realtime 实时回传、经销商点「转销售单」放开（属 V2.1-2 及以后）。
