# 家电批发进销存 ERP · UI重写开发计划（V2.0）

> 版本：V2.0（UI设计最终版 · 业务逻辑层已完成）
> 日期：2026-09-19
> 配套文档：PRD.md、UI设计稿.html（最终确认版）
> 前置状态：业务逻辑层已完成（74个测试全绿），本次重写UI层
>
> **产品版本：V1.0-1（2026-09-19 发布基线）**
> 本文档为开发执行计划，产品对外版本统一为 **V1.0-1**（`src/version.ts` / `package.json` 同步）。
> 发布基线章节见文末 **「十六、V1.0-1 发布基线」**。

---

## 一、当前进度盘点

### ✅ 已完成（业务逻辑层）
- 12张Dexie数据库表 + 默认老板账号初始化
- 8个Pinia store（user/product/purchase/sales/finance/dashboard/dealer/sync）
- 单号生成工具、金额大写工具
- 打印模板生成器（带/不带价格）
- 74个单元测试全绿

### 🔄 本次要做（UI表现层重写）
- 按最终UI设计稿重写所有页面
- 实现手机/电脑双端响应式自适应
- 登录页简化版（头像+系统名+账号+密码）
- 手机端3个Tab（工作台/业务/我的），全模块分类折叠
- 电脑端左侧导航栏（15项，含库房管理入库/出库/库存），支持折叠/展开
- 老板10大模块全部入口打通
- 权限按角色严格隔离

---

## 二、技术栈（不变）

| 类别 | 选型 | 说明 |
|---|---|---|
| 框架 | Vue 3 + Vite + TypeScript | 已完成 |
| UI库 | Vant 4 | 已安装，重写组件样式 |
| 路由 | Vue Router 4 | 已完成，补全路由表 |
| 状态 | Pinia | 已完成 |
| 数据库 | Dexie (IndexedDB) | 已完成 |
| 响应式 | CSS媒体查询 + Vue响应式 | <768px手机版，≥768px电脑版 |

---

## 三、分阶段开发计划（共 8 个阶段）

### 阶段 1 · 全局布局框架重写（2天）

**目标**：搭好手机/电脑双端响应式骨架，所有页面在这个框架里跑

**任务清单**：
1. 重写 `App.vue`，加全局响应式容器
2. 新建 `components/AppLayout.vue` 全局布局组件
   - 电脑端：左侧导航栏 + 右侧内容区
   - 手机端：顶部导航 + 内容区 + 底部Tabbar
3. 新建 `composables/useResponsive.ts` 响应式判断（isMobile / isDesktop）
4. 重写路由表，补全老板全部12个模块路由
5. 侧边栏折叠状态管理（Pinia + localStorage持久化）
6. 全局配色变量定义（CSS变量）

**验收标准**：
- [ ] 窗口宽度<768px显示手机版布局（底部Tab）
- [ ] 窗口宽度≥768px显示电脑版布局（左侧导航）
- [ ] 拖动窗口宽度时自动切换布局
- [ ] 侧边栏折叠/展开按钮工作正常
- [ ] 折叠状态刷新后保持

---

### 阶段 2 · 登录页重写（1天）

**目标**：按设计稿重写登录页，简化为头像+系统名+账号+密码

**任务清单**：
1. 重写 `LoginView.vue`
   - 顶部渐变背景（深蓝色渐变）
   - 居中圆形头像（默认灰色人形）
   - 系统名称「家电批发ERP」+ 副标题
   - 手机号输入框（带手机图标）
   - 密码输入框（带锁图标）
   - 蓝色登录按钮
   - 默认账号提示
2. 登录后自动识别角色，跳转对应工作台
3. 电脑端登录页左右分栏（左品牌介绍，右表单）
4. 手机端登录页上下布局（渐变头+卡片表单）

**验收标准**：
- [ ] 手机端：头像→系统名→手机号→密码→登录按钮
- [ ] 电脑端：左右分栏，左侧品牌区右侧表单区
- [ ] 老板账号登录跳转老板工作台
- [ ] 其他角色登录跳转对应工作台
- [ ] 角色自动识别，无需手动选择

---

### 阶段 3 · 老板工作台重写（2天）

**目标**：老板端全部10大模块入口打通，数据看板正常

**任务清单**：
1. **工作台首页**
   - 欢迎语 + 待办提醒
   - 4个数据卡片：本月销售额、本月毛利、应收款、库存预警
   - 今日待办列表：待入库/待发货/待收款
   - 6个快捷操作入口
2. **手机端Tab结构**
   - Tab1 工作台：数据看板+待办+快捷操作
   - Tab2 业务：商品中心/采购管理/销售管理/库房管理 分类列表
   - Tab3 我的：财务管理/人事权限/报表中心/系统设置 分类列表
3. **电脑端侧边栏**
   - 12项完整菜单：工作台/商品档案/库存管理/采购管理/销售管理/客户管理/供应商/财务管理/员工管理/经销商/经营报表/系统设置
   - 支持折叠/展开
4. **退出登录功能**（右上角）

**验收标准**：
- [ ] 老板手机端3个Tab可切换
- [ ] 老板电脑端侧边栏12项菜单可点
- [ ] 侧边栏折叠/展开正常
- [ ] 数据卡片显示真实汇总数据
- [ ] 退出登录返回登录页

---

### 阶段 4 · 商品中心页面重写（2天）

**目标**：商品档案 + 库存管理 两个模块页面完成

**任务清单**：
1. **商品列表页**
   - 手机版：卡片列表，商品名+分类+批发价+库存
   - 电脑版：表格视图，多列（名称/分类/进价/批发价/库存/状态/操作）
   - 搜索栏：按品牌/型号搜索
   - 分类筛选下拉
   - 右下角FAB新增按钮
   - 库存预警红色标记
2. **商品新增/编辑页**
   - 表单：品牌、型号（名称实时预览）、分类、规格、单位
   - 价格字段：进价、批发价、零售价（权限控制显示）
   - 库存预警值
   - 保存按钮
3. **库存管理页**
   - 实时库存列表
   - 低库存预警商品列表
   - 出入库流水记录

**验收标准**：
- [ ] 新增商品，名称自动显示「品牌 型号」
- [ ] 商品列表手机版卡片、电脑版表格
- [ ] 搜索功能正常
- [ ] 库存预警红色标记
- [ ] 手机版和电脑版切换时布局正常

---

### 阶段 5 · 采购+销售模块重写（3天）✅ 已完成 2026-09-19

**目标**：采购管理 + 销售管理 两个模块全部页面完成

**任务清单**：
1. **采购单列表**
   - 手机版：卡片列表（单号+供应商+金额+状态）
   - 电脑版：表格视图
   - 状态筛选（待入库/部分入库/已完成）
   - FAB新建按钮
2. **新建采购单**
   - 选择供应商（弹窗选择）
   - 添加商品明细（弹窗选商品，带进价）
   - 数量编辑 + 自动算金额
   - 合计栏 + 提交按钮
3. **供应商管理**
   - 列表 + 新增/编辑
4. **销售单列表**
   - 同采购单列表结构
   - 状态：待出库/部分出库/已完成
5. **新建销售单**
   - 选择客户（弹窗选择）
   - 添加商品（只显示批发价，不显示进价）
   - 数量编辑 + 自动算金额
6. **客户管理**
   - 列表 + 新增/编辑
   - 经销商账号创建（loginPhone/loginPassword）

**验收标准**：
- [x] 新建采购单→提交→列表显示
- [x] 新建销售单→提交→列表显示
- [x] 销售选商品时看不到进价
- [x] 手机版表单/电脑版表单都能正常提交
- [x] 权限隔离：销售看不到进价字段

> 关联测试：`tests/stage5.test.ts`（9 个用例全绿）。修复了两处真实 bug：采购/销售单列表页模板误用 `list`（应为 `orders`）；并统一用 `vi.waitFor` 等待 Dexie 多步异步写入落库。

---

### 阶段 6 · 库房+财务模块重写（2天）

**目标**：库房管理 + 财务管理 两个模块页面完成

**任务清单**：
1. **待收货入库页**
   - 待入库采购单列表
   - 点进去入库详情：逐项确认实收数量
   - 确认入库按钮→库存自动增加
2. **待发货出库页**
   - 待出库销售单列表
   - 点进去出库详情：逐项确认实发数量
   - 显示当前库存数量
   - 确认出库按钮→库存自动扣减
3. **库存查询页**
   - 所有商品实时库存
   - 库房端看不到任何价格
4. **应收应付对账页**
   - 手机版：Tab切换应收/应付
   - 电脑版：双栏或Tab切换
   - 应收列表：客户欠款明细
   - 应付列表：供应商欠款明细
   - 登记收款/付款按钮
5. **毛利核算页**
   - 销售收入 - 成本 = 毛利
   - 按月/按品类统计

**验收标准**：
- [x] 库房账号登录，任何页面看不到价格
- [x] 入库操作后库存数量正确增加
- [x] 出库操作后库存数量正确减少
- [x] 财务能看到应收应付明细
- [x] 登记收款后单据状态更新

> 关联测试：`tests/stage6.test.ts`（6 个用例全绿）。
> 修复：入库/出库详情页 `inboundQty`/`outboundQty` 用「整体对象替换」而非「逐项赋值」初始化（避免响应式未捕获新键）；测试改为 `vi.waitFor` 等 `onMounted` 真正加载完成后再点击提交，避免 qtyMap 为空导致库存不更新。

---

### 补充 · 库房管理接入老板/财务左侧菜单栏（2026-09-19）

**背景**：阶段 6 已重写库房页面，但老板/财务的左侧菜单（SideBar）只有「库存管理」(`/boss/stock`)，无法进入入库/出库操作；且 `/warehouse/*` 路由仅对 `warehouse` 角色开放，老板点击会被守卫重定向回首页。

**改动**：
1. `src/router/navConfig.ts`：老板侧边栏新增「入库验货 `/warehouse/inbound`」「出库拣货 `/warehouse/outbound`」「库存查询 `/warehouse/stock`」三项（紧邻库存管理）；财务侧边栏新增「库存查询」。
2. `src/router/index.ts`：路由守卫 `meta.role` 支持 `string | string[]`；库房路由 `meta.role` 改为 `['warehouse','boss']`（库存查询额外对 `finance` 开放）。
3. 修复两处既有构建类型错误（不影响本次需求，但会阻断 `npm run build`）：`boss/StockView.vue` 库存预警列表误用 `item.product`/`item.quantity`（脚本实为 `Product[]`，已按 `list` 一致方式渲染）；`purchase/SuppliersView.vue` 新增供应商漏传 `remark`。

**验收**：
- [x] 老板左侧菜单含入库/出库/库存三项
- [x] 老板可正常进入入库/出库/库存页（守卫放行）
- [x] 财务可进入库存查询页
- [x] 销售等未授权角色访问库房页仍被拦截回首页
- [x] `vue-tsc -b` 通过，`npm test` 全绿（127 用例）

> 关联测试：`tests/warehouse-menu.test.ts`（7 个用例全绿）。

---

### 阶段 7 · 报表+打印+系统设置（2天）

**目标**：报表中心 + 打印 + 系统设置 三个模块完成

**任务清单**：
1. **经营报表页**
   - 本月销售额/毛利/应收应付 汇总卡片
   - 销售趋势图（简单柱状图）
   - 库存预警商品列表
   - 电脑版：多列布局
2. **打印中心**
   - 采购单打印预览
   - 销售单打印预览
   - 「带价格/不带价格」切换开关
   - 调用浏览器打印功能
3. **系统设置页**
   - 数据备份：导出JSON
   - 数据恢复：导入JSON
   - 手动云同步：上传/拉取按钮
   - 公司信息设置（名称/地址/电话）
   - 退出登录
4. **操作日志页**
   - 谁在什么时候做了什么操作

**验收标准**：
- [ ] 报表页显示真实汇总数据
- [ ] 打印预览带价格/不带价格切换正常
- [ ] 导出备份JSON文件成功
- [ ] 导入恢复JSON文件数据完整
- [ ] 云同步按钮工作正常

---

### 阶段 8 · 经销商端 + 全量验收（2天）

**目标**：经销商端完成 + 全量验收 + 部署

**任务清单**：
1. **经销商产品目录页**
   - 商品列表：品牌+型号、规格、批发价
   - 搜索功能
   - 产品详情：只显示名称+批发价+规格
   - 权限：看不到进价、库存、其他客户
2. **全量验收测试**
   - 按PRD第10节17条验收标准逐项检查
   - 手机版/电脑版双端测试
   - 6个角色权限测试
3. **部署到GitHub Pages**
   - 配置base路径
   - build生产版本
   - 推送部署
4. **最终交付**

**验收标准**：
- [ ] 经销商登录后只能看产品+批发价
- [ ] 经销商看不到进价和库存
- [ ] 手机/电脑双端全部功能正常
- [ ] PRD第10节17条全部通过
- [ ] GitHub Pages可正常访问

---

## 四、时间总览

| 阶段 | 内容 | 工期 |
|---|---|---|
| 阶段1 | 全局布局框架重写 | 2天 |
| 阶段2 | 登录页重写 | 1天 |
| 阶段3 | 老板工作台重写 | 2天 |
| 阶段4 | 商品中心页面 | 2天 |
| 阶段5 | 采购+销售模块 | 3天 |
| 阶段6 | 库房+财务模块 | 2天 |
| 阶段7 | 报表+打印+设置 | 2天 |
| 阶段8 | 经销商端+全量验收 | 2天 |
| **合计** | | **约16天（3周多）** |

---

## 五、开发顺序原则

1. **先搭骨架（阶段1-2）**：响应式布局框架和登录页必须先搞定，后面所有页面都在这个框架里跑
2. **老板端优先（阶段3-4）**：老板是超级用户，先把老板端全部页面跑通
3. **业务闭环跟进（阶段5-6）**：采购销售库房财务是核心业务流程
4. **增强功能收尾（阶段7-8）**：报表、打印、经销商、验收放最后
5. **每阶段必测**：每做完一个阶段，跑一遍相关测试，不攒到最后

---

## 六、关键设计原则（开发时必须遵守）

### 6.1 响应式断点
- 手机模式：窗口宽度 < 768px
- 电脑模式：窗口宽度 ≥ 768px
- 同一套代码，CSS媒体查询 + Vue响应式自动切换

### 6.2 配色规范
- 主色：#1a365d（深海军蓝）
- 强调色：#2563eb（科技蓝）
- 成功：#10b981（翠绿）
- 警告：#f59e0b（琥珀）
- 危险：#ef4444（朱红）
- 背景：#f8fafc（浅灰白）

### 6.3 权限铁律（代码层面必须实现）
- 销售：商品列表不显示进价字段
- 库房：所有页面不显示任何价格
- 经销商：只显示产品+批发价
- 老板：全部可见

### 6.4 交互规范
- 手机端按钮最小点击区域：44×44px
- 侧边栏折叠状态存localStorage
- 页面切换过渡动画：0.2s
- 表单提交loading状态

---

## 交互与验收补充（2026-09-19）

### 补充 1 · 侧边栏折叠（回弹）按钮移入菜单栏 ✅（2026-09-19 修正）

> ⚠️ 本节此前理解有误：把「回弹按钮」误做成了「返回按钮」，在菜单栏头部多加了一个「‹ 返回」按钮，
> 且真正要移动的折叠按钮仍留在功能页面左上角。老板已指出，现按正确做法修正。

- **正确做法**：把「侧边栏折叠 / 展开（回弹）按钮」放进左侧菜单栏头部，紧跟在「家电批发ERP」文字之后。
- **已删除**此前误加的「返回」按钮（菜单栏头部以及功能页面左上角均不再有返回按钮）。
- `src/components/AppLayout.vue`：移除顶部栏最左侧的折叠按钮（即截图红框内的按钮）；顶部栏现在只有 页面标题 / 用户 / 退出。
- `src/components/SideBar.vue`：头部顺序为 `ERP 图标 → 家电批发ERP → 折叠按钮(‹)`；折叠后按钮显示 `☰`，图标与文字隐藏、按钮居中。
- 功能页面左上角不再有任何返回 / 折叠按钮：手机端 `MobileTopNav` 无返回按钮；`boss/BossReportsView.vue` 的 `van-nav-bar` 已去掉 left-arrow 返回箭头。
- 手机端导航依靠底部 Tab；电脑端依靠常驻的左侧菜单栏。

> 关联测试：`tests/menu-order.test.ts`（14 个用例全绿，其中 6 个覆盖折叠按钮位置与行为）。

### 补充 2 · 侧边栏菜单支持自定义排序 ✅
- 新增 `src/stores/menuOrder.ts`：按角色把菜单顺序持久化到 localStorage（`erp_menu_order_<role>`）
- 侧边栏菜单项支持拖拽排序，顺序即时生效并持久化；底部提供「↺ 重置菜单顺序」恢复默认
- 健壮性：新增菜单自动补到末尾、已移除菜单自动剔除、顺序按角色隔离
- 顺序保存在响应式 ref 中，保证拖拽后侧边栏立即重渲染

### 补充 3 · 全量运行验证 ✅
- `npm run build`（含 `vue-tsc` 类型检查）通过；`npm run dev` 启动后 HTTP 200 可正常访问
- 新增 `tests/menu-functions.test.ts`（29 用例）：逐菜单校验
  - 所有角色的每个侧边栏菜单都能解析到已注册页面
  - 19 个菜单对应页面逐个挂载渲染，确认无报错、非空壳
  - 商品/供应商/客户/员工/采购单/销售单/毛利 等核心功能真实跑通并落库
- 新增 `tests/workflow.test.ts`（9 用例）：采购→入库→销售→出库→财务 全链路端到端

### 本轮修复的缺陷（运行验证中暴露，均已修复并补测试）
1. **库存管理页流水加载失败**：`StockView` 用 `orderBy('createdAt')` 查询 stockRecords，但该字段未建索引，Dexie 抛 `SchemaError`，导致该页最近流水一直为空。
   → 在 `src/db/index.ts` 增加 `version(2)`，为 `stockRecords` 补 `createdAt` 索引。
2. **分次入库永远无法完成**：`purchase.inbound` 只用「本次实收」与「订单量」比较，二次补收后仍判定 partial，采购单一直挂在待入库列表。
   → 改为累加该单该商品的历史已收数量后再判定。
3. **分次出库永远无法完成**：`sales.outbound` 存在同样问题（出库流水记为负数）。
   → 改为累加历史已发数量（取绝对值）后再判定。

### 补充 4 · 菜单功能清单评审（待老板确认）📋 2026-09-19

**背景**：老板反馈库房类等菜单「功能重复、临时占位、没有意义，不属于菜单中该有的功能」，要求**先列出每个菜单的功能 / 动作 / 作用**，再决定是否开发。

**产出**：`MENU_SPEC.md` —— 逐菜单列出「定位 / 页面动作 / 作用 / 现状（✅真实可用 ⚠️部分可用 🔁重复 ❌占位）/ 建议」，覆盖：
老板 15 项 + 手机端 2 个 Tab 页、采购 3 项、销售 3 项、财务 5 项、库房 4 项。

**盘点结论**：
- ✅ 真实可用：工作台(老板)、商品档案、库存管理、入库验货、出库拣货、采购单、销售单、客户管理、供应商、应收应付、系统设置（备份部分）
- 🔁 重复菜单：`/warehouse/stock` ↔ `/boss/stock`；`/boss/dealers` ↔ `/sales/customers`；`/finance/gross-profit` ↔ `/boss/reports`
- ❌ 占位无功能：采购 / 销售 / 财务 / 库房 4 个「工作台」+ 老板手机端「业务」「我的」
- ⚠️ 待补：采购单 / 销售单缺详情页（列表「操作」按钮跳错）；员工管理仅能新增；云同步为假实现
- 缺失功能 11 项：采购/销售单详情、打印中心、操作日志、简版盘点、经销商端、真实云同步、员工停用/改密、客户与供应商停用、3 个未注册 Tab 路由、客户等级定价

**待办**：老板在 `MENU_SPEC.md` 第五节拍板 4 个问题（库存入口合并、客户与经销商合并、工作台是否补真实待办、菜单是否重构）后，
据此更新 PRD.md 与本文档，并排出新的开发阶段。

### 补充 5 · 老板拍板「按推荐来」——第二轮改造 ✅ 2026-09-19

老板确认按推荐执行（P0 修错+补闭环 → P1 占位改真功能 → P2 增强），本轮完成以下内容。

#### 5.1 统一搜索组件：全项目搜索框自带清除按钮 ✅
- 新增 `src/components/SearchInput.vue`：
  - 有输入内容时右侧出现「×」清除按钮，无内容时隐藏（`v-show`，非 `v-if`，避免布局跳动）
  - 点击「×」立即清空并触发一次空搜索；按 **Esc** 也可清空
  - 支持防抖（默认 300ms，页面可传 `:debounce="0"` 立即触发）
  - 屏蔽浏览器原生搜索清除图标，避免与自定义按钮重复
  - `v-model` 双向同步，外部清空时输入框同步清空
- **已接入**：商品档案 `/boss/products`、库存管理 `/stock`、客户管理 `/customers`、操作日志 `/boss/audit-logs`
- 后续新增任何搜索框都必须复用该组件，不要手写 `<input>`

#### 5.2 库存查询增加搜索 + 与「库存管理」合并 ✅
- 新增 `src/views/stock/StockManageView.vue`（统一库存页）：
  - **搜索**（品牌/型号/分类，带清除按钮）
  - 分类下拉筛选（选项基于全量商品，不会因搜索变少）
  - 「仅看低库存」开关
  - 4 张汇总卡：SKU 数 / 库存总量 / 库存金额（进价）/ 预警数
  - 库存预警区 + 库存明细表 + 出入库流水（最近 20 条）
  - 响应式：手机端卡片、电脑端表格；**库房角色自动隐藏所有价格列**
- 路由：新增规范路由 `/stock`；`/boss/stock`、`/warehouse/stock` 保留 **302 重定向**，收藏夹不失效
- 已删除重复页面：`boss/StockView.vue`、`warehouse/StockQueryView.vue`

#### 5.3 客户管理与经销商合并；毛利并入经营报表 ✅
- 客户管理 `/customers`（原 CustomersView 增强）：搜索 + 清除按钮、「仅看经销商/仅看普通客户」筛选、
  经销商账号开关与登录号展示、电脑端表格 / 手机端卡片。旧路径 `/sales/customers`、`/boss/dealers` 重定向。
- 经营报表 `/boss/reports` 重写：日期区间筛选 + 快捷区间（本月/近3月/近半年/全部）、
  6 项指标（销售收入、销售成本、毛利、毛利率、应收未收、应付未付）、
  **近 6 月销售/毛利趋势柱图**、库存预警清单。旧路径 `/finance/gross-profit` 重定向。
- 财务 store 新增 `getProfitSummary(from, to)`、`getMonthlyTrend()`、`getReceivableTotal()`、`getPayableTotal()`
- 已删除重复页面：`boss/DealersView.vue`、`finance/GrossProfitView.vue`

#### 5.4 P0 补闭环：单据详情页 + 打印 + 操作日志 ✅
- **采购单详情页** `/purchase/orders/:id`（`views/purchase/PurchaseOrderDetailView.vue`）：
  单头（单号/状态/付款状态/供应商/联系人/日期/备注）、明细、金额合计、**入库进度条**、付款记录、**打印**
- **销售单详情页** `/sales/orders/:id`（`views/sales/SaleOrderDetailView.vue`）：同上，含**出库进度**、收款记录、**送货单打印**
- 打印：`src/utils/print.ts` 接上闲置的 `utils/printTemplate.ts`，A4 输出；可勾选「打印含单价」，
  取消后打印不含单价（给供应商/客户的版本）。窗口被拦截时给出提示。
- **修复列表「操作」按钮跳错**：此前按钮文字为「新建」且跳到新建页，已改为「查看」并跳该单详情（采购/销售两处）
- **操作日志**：新增 `src/utils/audit.ts`（`writeLog` + `AUDIT_ACTIONS`），关键动作真实写入 `auditLogs`；
  新增查询页 `views/boss/AuditLogView.vue`（`/boss/audit-logs`，支持搜索与按动作筛选）
- 埋点位置：商品新增/修改、采购建单、采购入库、销售建单、销售出库、登记收款、登记付款、新增客户、新增供应商
  （`createCustomer` / `createSupplier` / `createProduct` / `updateProduct` 新增可选 `operatorId` 参数，向后兼容）

#### 5.5 P1：四个角色工作台改为真实待办看板 ✅
- 采购：待入库单数、本月采购额、供应商数、未付款金额 + 待入库单据列表（点击直达验货页）
- 销售：待出库单数、本月销售额、客户数、未收款金额 + 待出库单据列表（点击直达拣货页）
- 财务：应收未收、应付未付、本月已收、本月已付 + 待收款/待付款单据列表（点击直达对账页）
- 库房：待收货单数、待发货单数、今日入库量、今日出库量 + 待收货/待发货单据列表（直达验货/拣货页）
- **顺带修复**：四个工作台原本各自内嵌了一个 `van-tabbar`，与 `AppLayout` 的 `MobileTabBar` 重复，已移除
- 老板手机端「业务」Tab：入口改为**由老板侧边栏菜单自动生成**（排除 Tab 自身），新增菜单自动同步
- 老板手机端「我的」Tab：新增登录人卡片（姓名/角色）与「操作日志」入口

#### 5.6 本轮测试
| 测试文件 | 覆盖 | 用例 |
|---|---|---|
| `tests/search-and-merge.test.ts` | SearchInput 清除按钮行为、库存搜索/分类/仅看低库存/权限、客户搜索与筛选、报表日期筛选与趋势 | 22 |
| `tests/p0-order-detail.test.ts` | 操作按钮跳详情、两个详情页、打印（含/不含单价）、日志写入与查询 | 11 |
| `tests/p1-workbench.test.ts` | 四工作台待办数量/金额/跳转/完成后消失 | 12 |
| 既有测试改造 | stage1/3 菜单数量 15→14、stage3 业务页改为「覆盖全部菜单」、stage4 搜索改用 `.search-field`、stage6 指向合并页、menu-functions 指向合并页 | — |

**合计 229 用例 / 24 个测试文件 全部通过**；`npm run build`（含 `vue-tsc`）通过；`npm run dev` HTTP 200。

#### 5.7 文档同步
- `PRD.md` → **V1.2**：第 11 节改为改造后现状（11.2 已合并 / 11.3 已改真实数据 / 11.5 本轮增强 / 11.6 验收状态）
- `MENU_SPEC.md` → 重写为改造后的逐菜单清单（老板 14 项 + 其他角色 + 剩余待办 9 项 + 测试情况）

#### 5.8 下一批（P2，待排期）
1. 经销商端产品目录页（经销商登录后目前会被弹回登录页）
2. 简版盘点单（PRD 5.4）
3. 员工账号编辑 / 停用 / 改密
4. 客户 / 供应商停用
5. 手机端「我的」Tab 路由（`/purchase/mine`、`/sales/mine`、`/finance/mine` 未注册，点击跳登录页）
6. 真实云同步（或明确只保留 JSON 备份）
7. 客户等级 → 批发价档位
8. 出库「实发 > 库存」前端拦截

### 补充 6 · 修复「点开菜单页面空白/功能无效」事故 ✅ 2026-09-19

#### 6.1 现象
老板账号登录后，点开「采购管理 / 销售管理 / 供应商 / 财务管理」等菜单，
页面会闪一下就跳回工作台；入库验货、出库拣货、客户、库存、财务等页面则显示空白。

#### 6.2 根因（两个，互相叠加）

**根因 A：菜单路由权限与角色不匹配（功能"点了没用"）**

`navConfig` 里老板侧边栏有 4 项指向仅授权给其他角色的路由，而路由守卫会按 `meta.role` 拦截：

| 菜单项 | 路由 | 原 meta.role | 后果 |
|---|---|---|---|
| 采购管理 | `/purchase/orders` | `purchaser` | 老板点击被弹回 `/boss/home` |
| 销售管理 | `/sales/orders` | `sales` | 同上 |
| 供应商 | `/purchase/suppliers` | `purchaser` | 同上 |
| 财务管理 | `/finance/reconcile` | `finance` | 同上 |

**根因 B：新装数据库为空（页面"空白"）**

IndexedDB 全新时各业务表均为 0 条，采购/销售/客户/供应商/入库/出库/财务等列表
全部显示「暂无数据」。此前没有任何示例数据或引导，容易被判断为「功能没开发」。

**根因 C（附带）：手机端三个「我的」Tab 路由未注册**

`/purchase/mine`、`/sales/mine`、`/finance/mine` 未注册，命中通配规则被重定向到 `/login`，
表现为点击即退出登录。

#### 6.3 修复
1. `src/router/index.ts`：老板为最高权限角色，所有业务路由 `meta.role` 补入 `'boss'`
   （采购/销售/财务/库房全线），保留对其他角色的越权拦截。
2. `src/views/RoleMineView.vue`（新增）：采购/销售/财务共用的手机端「我的」页，
   入口直接取自该角色菜单，避免两处维护不同步；注册三条 `/xxx/mine` 路由。
3. `src/utils/demoData.ts`（新增）：一键生成覆盖完整链路的示例数据
   —— 6 商品 / 3 供应商 / 3 客户（含 1 个经销商登录号）/ 3 张采购单 / 3 张销售单，
   并连带产生库存、出入库流水、应收应付与操作日志；同时提供清空业务数据。
   所有数据经各业务 store 写入，与真实操作同一套逻辑。
4. `src/views/boss/SettingsView.vue`：新增「示例数据」区块（载入 / 清空 + 当前数据量）。
5. `src/views/boss/BossHomeView.vue`：空数据时显示「开始使用」引导卡片（载入示例数据 / 直接录入）。

#### 6.4 浏览器真实验证（agent-browser 实机操作）
登录老板账号后逐页核对，全部正常：

| 页面 | 验证结果 |
|---|---|
| 采购管理 | 3 张单，金额/状态/查看按钮齐全，URL 未被弹回 |
| 销售管理 | 3 张单，可进入详情 |
| 入库验货 | 2 张待收货单（部分入库/待入库），点「验货」进详情 |
| 出库拣货 | 2 张待发货单（部分出库/待出库） |
| 客户管理 / 供应商 | 3 条客户、3 家供应商 |
| 财务管理 | 应收列表 + 登记收款按钮 |
| 库存管理 | 6 SKU / 23 件 / ¥24,200 / 预警 5 项 + 搜索筛选 |

**实机跑通完整入库**：进详情 → 填实收 6 / 10 → 确认入库 → BCD-253 库存 6→12、
M3-L235 库存 10→20、出入库流水 +2 条，提交后自动返回列表。

#### 6.5 测试
- 新增 `tests/nav-permission.test.ts`（14 用例）：遍历**每个角色**的 sidebar + tabbar 每一项，
  断言路由已注册、且登录后能真正停留（不被守卫弹回首页、不跳登录页）；
  另覆盖老板全业务可进、越权拦截、未登录重定向。
  此用例在修复前**失败 8 项**，正是本次事故的回归防线。
- 新增 `tests/demo-data.test.ts`（12 用例）：示例数据生成后各表有数、
  入库/出库/采购/销售/客户/供应商/库存/财务各页面**均不再是空白**、
  库存与流水一致、经销商账号可登录、幂等保护、清空后账号保留、首屏引导显隐。

**合计 255 用例 / 26 个测试文件 全部通过**；`npm run build`（含 `vue-tsc`）通过。

#### 6.6 待办清单更新
- ~~手机端「我的」Tab 路由未注册~~ → 本轮已修复（补充 6.3 第 2 项）

---

## 补充 7 · 查询输入框 + 二级页面操作按钮 + 打印重排 + 明细历史（2026-09-19）

### 7.1 需求
1. 采购 / 入库 / 销售 / 出库 / 财务都要有查询输入框，并且能查历史；
2. 每个二级页面按业务补「返回」或「取消」按钮；
3. 打印页面重新排版（原排版太简陋），且必须在预览页提供「打印」与「取消」；
4. 财务管理看不到历史信息；
5. 每个功能模块都要有正常的明细历史，信息流转后必须留痕。

### 7.2 交付

**① 二级页面统一操作栏 —— 新增 `src/components/PageActions.vue`**

| 页面 | 按钮 | 行为 |
|---|---|---|
| 采购单详情 | 返回 | 回 `/purchase/orders` |
| 销售单详情 | 返回 | 回 `/sales/orders` |
| 新建采购单 | 取消 + 提交采购单 | 取消回列表，**不产生单据** |
| 新建销售单 | 取消 + 提交销售单 | 同上 |
| 新增 / 编辑商品 | 取消 + 保存 | 取消回商品档案 |
| 入库验货 | 取消 + 确认入库 | 取消回待收货，不写流水 |
| 出库拣货 | 取消 + 确认出库 | 取消回待发货，不写流水 |

组件支持 `loading`（显示「处理中…」并禁用，防重复提交）与 `confirmDisabled`。

**② 各模块查询输入框（全部使用统一 `SearchInput`，自带清除按钮）**

| 页面 | 搜索范围 | 其他筛选 |
|---|---|---|
| 采购单 | 单号 / 供应商 | 状态 + 日期区间 + 重置 |
| 销售单 | 单号 / 客户 | 状态 + 日期区间 + 重置 |
| 入库历史 | 单号 / 供应商 / 商品 | 日期区间 + 重置 |
| 出库历史 | 单号 / 客户 / 商品 | 日期区间 + 重置 |
| 财务应收/应付 | 单号 / 往来单位 | 结清状态（未结清 / 已结清 / 全部） |
| 财务流水 | 单号 / 客户 / 供应商 | 收付款类型 + 日期区间 + 重置 |

**③ 打印重排 —— `utils/printTemplate.ts` 整体重写 + 新增 `components/PrintPreview.vue`**

- 排版：公司抬头（读系统设置公司名）→ 单据名 → 单号/日期/制单行 → 往来单位信息卡 →
  明细表（序号/商品/型号/单位/数量/单价/金额，表头跨页重复）→ 合计行 →
  金额框（合计数量、金额合计、**大写金额**）→ 备注 → 签章栏 → 页脚；
  内置 `@page A4` 与 `page-break-inside: avoid`，明细行不会被切断。
- 流程改为**先预览再打印**：点「打印」弹出预览弹窗（iframe 渲染真实单据），
  底部固定「取消」「打印」两个按钮，可勾选「打印含单价」；
  库房等无价格权限的角色不显示该开关。
- 金额格式统一为「千分位 + 两位小数」（如 `27,600.00`）。

**④ 明细历史（信息流转后留痕）**

| 位置 | 历史内容 |
|---|---|
| 采购单详情 | 入库流水（时间 / 商品 / 数量 / 操作人）+ 付款记录（含操作人） |
| 销售单详情 | 出库流水（时间 / 商品 / 数量 / 操作人）+ 收款记录（含操作人） |
| 入库页 | 「待收货 / 入库历史」双页签，历史含时间 / 采购单号 / 供应商 / 商品 / 数量 / 操作人 |
| 出库页 | 「待发货 / 出库历史」双页签，历史含时间 / 销售单号 / 客户 / 商品 / 数量 / 操作人 |
| 财务对账 | 「应收 / 应付 / 收付款流水」三页签；**默认只看未结清，可切「已结清 / 全部」查历史**，流水含时间/类型/单据号/往来单位/金额/操作人/备注 |
| 入库验货页 | 本单入库记录（时间 / 商品 / 数量 / 操作人） |
| 出库拣货页 | 本单出库记录（时间 / 商品 / 数量 / 操作人） |

**⑤ store 新增查询方法**

- `purchase.listInboundHistory()`：关联采购单/供应商/商品/操作人的入库流水
- `sales.listOutboundHistory()`：关联销售单/客户/商品/操作人的出库流水
- `finance.listReceivables(includeSettled)` / `listPayables(includeSettled)`：可含已结清
- `finance.listPaymentHistory()`：全部收付款流水（关联单据号与往来单位）

**⑥ 顺带修复**

- 出库拣货：新增前端**超发拦截**（实发 > 待发、实发 > 当前库存均拦截并提示）
- 入库/出库详情默认填入「剩余待收 / 待发」数量，避免重复超收超发
- 财务默认筛选由「只显示有余额的单据」改为**显式「未结清」筛选**，
  并保留已结清入口，解决「结清后单据消失、查不到历史」

### 7.3 测试
新增 3 个测试文件 / 30 用例：
- `tests/page-actions.test.ts`（10 用例）：组件行为 + 7 个二级页面按钮存在性/跳转/不产生副作用
- `tests/history-search.test.ts`（10 用例）：5 个模块的搜索、日期区间、历史明细、结清筛选、类型筛选
- `tests/print-preview.test.ts`（8 用例）：预览渲染、打印/取消按钮、打印调用与回退、单价开关

**合计 283 用例 / 29 个测试文件 全部通过**；`npm run build`（含 `vue-tsc`）通过。
真实浏览器逐页验证：入库历史 4 条、出库历史 4 条、财务流水 4 笔，打印预览排版正常。

---

## 补充 8 · 打印模板重做：纸张 / 分页 / 表头与字段自定义 ✅ 2026-09-19

**需求**：单据按 A4 一半（A5 横版）打印；明细超页自动分页；条码/表头与明细字段可由用户自行编辑；
A4 竖版时尾部与表格之间空白过大；客户看不懂商品是什么。

**① 纸张（新增 `src/utils/printSettings.ts`）**

| 选项 | 尺寸 | 用途 |
|---|---|---|
| **A5 横版**（默认） | 210 × 148 mm（A4 的一半） | 日常送货单 / 出库单 |
| **A4 竖版** | 210 × 297 mm | 明细多、需归档 |

A3 已按要求取消。`@page { size: <w>mm <h>mm; margin: 0 }`，横竖由宽高决定。

**② 自动分页（`paginateItems`）**

- 首页预留往来单位区、末页预留合计/签章区，因此首页容量 `cap-4`、末页 `cap-5`，其余 `cap`；
- 余两页时**均分**，避免出现只有 1 行的尾页；
- 行数守恒（不丢行不重复），每页重复表头，meta 区带「第 X / Y 页」，**合计与签章只在末页**；
- 容量默认 A5 = 6 行 / A4 = 14 行，可在设置里手动覆盖。

**③ 表头与字段自定义**

- 表头：公司抬头 / 副标题 / 地址 / 电话 / 页脚 / 签章栏开关 / 每页行数；
- **明细字段**：序号、商品名称、**类别**、型号、单位、数量、单价、金额 ——
  可**勾选显隐、改列名、上下调顺序**，`合计` 行按数值列自动对齐；
- 新增「类别」列（洗衣机 / 冰箱 / 空调…），解决「客户不知道是什么东西」；
- 入口两处：打印预览弹窗「⚙ 表头设置」+ 系统设置 →「打印设置」；
- 持久化 `localStorage`，配置损坏/缺字段自动清洗补齐。

**④ 修复**

- **A4 尾部空白过大**：根因是签章栏用了 `margin-top: auto`，在固定页高的纸张里被顶到底部，
  中间塌出大片空白。改为 `margin-top: 14px` 让内容紧凑自上而下排列。
- **移除重复的「合计方框」**：数量/金额/大写方框与表格合计行重复，已删除，合计只保留在表格底部。
- **换单号不刷新（真 bug）**：4 个详情页只用 `onMounted` 加载，同一路由只换 `:id` 时组件被复用，
  会一直显示上一张单据。改为 `loadXxx()` + `onMounted` + `watch(() => route.params.id)`。

**⑤ 测试**：新增 `tests/print-paper.test.ts`（22 用例）——A5/A4 尺寸与 @page、A3 已移除、
小单据不分页、20 行自动分页（页数/表头/单号/页码/合计位置）、不丢行不重复、每页行数可覆盖、
类别列、字段勾选/改名/排序、无价格时自动剔除价格列、配置持久化与损坏回退、
预览弹窗纸张切换与表头编辑实时生效、**换单号必须重新加载（去掉 watch 会失败）**。

**合计 305 用例 / 30 个测试文件 全部通过**；`npm run build`（含 `vue-tsc`）通过。
真实浏览器验证：A5 横版 793×560px（210×148mm）、A4 竖版 794×1123px（210×297mm）尺寸正确；
20 行单据 → 5 页（3/6/6/3/2 行），每页带表头与页码，合计仅在末页；
字段改名/关闭/调序后打印表头同步变化；A4 表格与签章间距 39px（此前被顶到页底）。

---

## 十六、V1.0-1 发布基线（2026-09-19）

### 16.1 版本定义

| 项 | 值 |
|---|---|
| 产品版本 | **V1.0-1** |
| 版本来源 | `src/version.ts` → `APP_VERSION = 'V1.0-1'`；`package.json` → `"version": "1.0.0-1"` |
| 发布日期 | 2026-09-19 |
| 版本含义 | V<主版本>.<次版本>-<修订号>；V1.0-1 = 1.0 的第 1 个修订版 |
| 版本展示 | 系统设置页头徽标（灰色 `V1.0-1`） |
| 发版方式 | 改 `src/version.ts` 与 `package.json` 两处，再更新 PRD 头部「产品版本」行 |

### 16.2 阶段回顾（本计划执行至今）

| 轮次 | 主题 | 关键产出 | 测试 |
|---|---|---|---|
| 基础 | 业务逻辑层 | 12 张 Dexie 表、8 个 Pinia store、打印模板 | 74 |
| 打印 | A5/A4 纸张、自动分页、表头字段自定义 | `print-paper.test.ts` | 305 |
| 第 6 轮 | 库存作业合并 + 调拨 / 盘点 / 退换货 | `WarehouseOpsView`、`inventory.ts`、`returns.ts` | 337 |
| 第 7 轮 | 多库房 + GitHub 云同步 + Pages 部署 | `cloudSync.ts`、`LocationsView`、`deploy-gh-pages.sh` | 356 |
| 第 8 轮 | 角色权限开关 + 记一笔 + 界面精细化 | `permission.ts`、`ledgerEntries`、`theme.css` + `components/ui/` | 372 |
| 第 9 轮 | 设计系统采纳收口 | 嵌套页头降级、页头徽标、覆盖率 18/37 页 | 372 |
| **第 10 轮** | **版本基线 + Git + Pages 上线** | `src/version.ts`、`.gitignore`、首次提交 `324420c`、tag `v1.0-1`、仓库与 Pages | **372（0 error）** |

### 16.3 V1.0-1 功能总览

- **商品档案**：搜索 / 分类筛选 / 批量导入导出 / 去重 / 条码
- **采购管理**：开单、审核、分次入库、供应商、应付
- **销售管理**：开单、审核、分次出库、客户、应收
- **库存作业**（统一入口 `/warehouse`）：入库验货、出库拣货、库存调拨、库存盘点、退换货、库房管理
- **多库房**：库房自定义（增/改名/删）、出入库选库房、库存明细按库房分列展示、库房间调拨
- **财务中心**（`/finance`）：应收应付对账、经营报表、**记一笔**（送货/物流/快递/安装/辅材等费用）、资金流水
- **权限**：6 类角色 × 17 个模块的开关矩阵，三处同时生效（侧边栏 / 手机 Tab / 路由守卫）
- **打印**：A5 横版 / A4 竖版、自动分页、表头与字段用户自定义、含/不含价格
- **数据**：JSON 导出导入、GitHub 云同步（AES-GCM 加密 + gzip + 指纹去重）
- **界面**：设计系统 `theme.css` + 6 个通用组件，电脑/手机双端响应式

### 16.4 交付与部署

| 项 | 内容 |
|---|---|
| 源码仓库 | GitHub —— `bailihongxi/wholesale-erp`（主分支 `main`） |
| 在线访问 | GitHub Pages —— `https://bailihongxi.github.io/wholesale-erp/` |
| 部署方式 | `npm run deploy <仓库地址>` → 构建 `dist` 并推到 `gh-pages` 孤儿分支 |
| 部署细节 | 见 `docs/GITHUB_PAGES.md`（`.nojekyll` / `404.html` / `base: './'`） |
| 数据存放 | 浏览器 IndexedDB，不上传服务器；云同步由用户自备仓库与 Token |

### 16.5 已知边界（非缺陷）

1. **无服务端**：数据在各终端本地，换设备需手动「同步到云端 / 从云端恢复」。
2. **云同步需自行配置**：在系统设置填 GitHub 用户名 / 仓库名 / Token / 同步口令，详见 `docs/GITHUB_PAGES.md`。
3. **示例数据里的账号密码**：老板 `13800000000 / admin123`，各角色 `13700000001~13700000004 / 123456`。
4. **权限只管模块可见性**：字段级权限（销售不见进价、库房不见价格）由既有规则固定，不随开关变化。

### 16.6 后续建议（V1.1 起）

- 多端冲突合并（当前云同步为覆盖式，按「最后上传者为准」）
- 经营报表导出 Excel / PDF
- 条码扫描枪直连录入
- 采购建议（按库存预警自动生成补货单）

---

**文档结束。** 每完成一个阶段，在对应阶段后打勾并记录实际完成日期。
